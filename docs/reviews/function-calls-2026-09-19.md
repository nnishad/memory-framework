# Function-call inventory

Every Python file under personal_memory. Syntactic AST calls, not a dynamically resolved call graph. Callback targets, route dispatch and decorator execution require the flow guide. Defaults/decorators are grouped under their syntactic definition, not execution time. No call arguments or runtime private data included.

Commit: `e620fff80046aa5bbbe3ebf0074b7295398d04d2`. Modules: 58; named functions: 689; call expressions: 7889.

## personal_memory/__init__.py

Personal Memory: a profile-scoped memory service for Hermes.


## personal_memory/__main__.py

(Module entry point.)

- **settings** (L11): `load_settings` (L13)
- **main** (L16): `argparse.ArgumentParser` (L17); `parser.add_subparsers` (L18); `sub.add_parser` (L20); `p.add_argument` (L21); `os.environ.get` (L21); `str` (L21); `Path.home` (L21); `p.add_argument` (L23); `p.add_argument` (L24); `p.add_argument` (L26); `p.add_argument` (L27); `p.add_argument` (L28); `p.add_argument` (L29); `p.add_argument` (L31); `p.add_argument` (L33); `p.add_argument` (L34); `p.add_argument` (L36); `p.add_argument` (L37); `p.add_argument` (L38); `p.add_argument` (L39); `p.add_argument` (L42); `p.add_argument` (L43); `p.add_argument` (L44); `p.add_argument` (L45); `p.add_argument` (L47); `p.add_argument` (L48); `p.add_argument` (L49); `p.add_argument` (L51); `p.add_argument` (L52); `p.add_argument` (L54); `p.add_argument` (L55); `p.add_argument` (L57); `p.add_argument` (L58); `p.add_argument` (L59); `p.add_argument` (L60); `p.add_argument` (L62); `p.add_argument` (L63); `p.add_argument` (L64); `p.add_argument` (L66); `p.add_argument` (L67); `name.startswith` (L68); `p.add_argument` (L69); `p.add_argument` (L70); `p.add_argument` (L72); `p.add_argument` (L74); `p.add_argument` (L75); `p.add_argument` (L76); `p.add_argument` (L77); `p.add_argument` (L79); `p.add_argument` (L81); `p.add_argument` (L82); `p.add_argument` (L84); `p.add_argument` (L86); `p.add_argument` (L88); `p.add_argument` (L89); `p.add_argument` (L91); `p.add_argument` (L92); `p.add_argument` (L94); `p.add_argument` (L95); `p.add_argument` (L97); `p.add_argument` (L98); `p.add_argument` (L99); `p.add_argument` (L100); `parser.parse_args` (L101); `authorize` (L105); `atomic_json` (L106); `args.credentials_file.chmod` (L107); `str` (L108); `ValueError` (L115); `settings` (L116); `Store` (L117); `database_path` (L117); `functools.partial` (L118); `functools.partial` (L119); `awareness.get_consumer` (L120); `ValueError` (L122); `profile_matches_home` (L125); `ValueError` (L126); `process_once` (L140); `awareness.reconcile_deliveries` (L143); `deliver_once` (L144); `made_progress` (L149); `outcome.get` (L149); `time.sleep` (L150); `process_once` (L154); `awareness.reconcile_deliveries` (L157); `deliver_once` (L158); `retrieval.close` (L165); `settings` (L167); `Client` (L168); `client.call` (L170); `json.loads` (L170); `args.credentials_file.read_text` (L170); `client.call` (L173); `client.call` (L175); `install` (L177); `Path(args.hermes_home).expanduser` (L180); `Path` (L180); `json.loads` (L181); `path.read_text` (L181); `cfg.setdefault` (L181); `atomic_json` (L181); `settings` (L184); `cfg.setdefault` (L185); `Path(args.hermes_home).expanduser` (L186); `Path` (L186); `json.loads` (L187); `public_path.read_text` (L187); `atomic_json` (L189); `settings` (L192); `upload` (L193); `Client` (L193); `settings` (L196); `export_skill` (L197); `Client` (L197); `settings` (L200); `sync_files` (L201); `Client` (L201); `settings` (L205); `strict_json` (L206); `args.lineage_file.read_bytes` (L206); `HermesHistoryConnector` (L207); `sync_history` (L208); `Client` (L208); `Path(args.hermes_home).expanduser` (L209); `Path` (L209); `settings` (L211); `args.endpoint.startswith` (L212); `ValueError` (L212); `cfg.get` (L215); `p.get` (L215); `len` (L216); `ValueError` (L216); `strict_json` (L219); `args.file.read_bytes` (L219); `isinstance` (L220); `ValueError` (L220); `Client(cfg['url'], token, timeout=30).call` (L221); `Client` (L221); `rollback` (L223); `doctor` (L226); `create_key` (L229); `backup` (L232); `restore` (L235); `ValueError` (L237); `settings` (L238); `Client(cfg['url'], cfg['token'], timeout=120).call` (L239); `Client` (L239); `settings` (L242); `prune_stale_instances` (L243); `settings` (L246); `os.umask` (L249); `configure_logging` (L253); `uvicorn.run` (L257); `Application` (L257); `Runtime` (L262); `create_server` (L263); `Path` (L263); `cfg.get` (L264); `print` (L265); `json.dumps` (L265); `server.serve_forever` (L267); `server.server_close` (L271); `settings` (L274); `Client` (L275); `client.call` (L277); `client.call` (L280); `client.call` (L281); `client.call` (L283); `client.call` (L285); `importers.whatsapp` (L291); `importers.emails` (L292); `importers.health_csv` (L293); `importers.jsonl` (L294); `adapt_existing` (L301); `args.command.replace` (L301); `str` (L302); `args.file.resolve` (L302); `now` (L302); `client.call` (L303); `importers.typed_health` (L305); `int` (L306); `RuntimeError` (L308); `print` (L310); `json.dumps` (L310); `SystemExit` (L311)
- **main.current_retrieval** (L128): `ServiceRetrieval` (L133); `Client` (L133)

Module/class/lambda calls: `<module> → main` (L315)

## personal_memory/access.py

Recipient boundary for a single owner's profile; host metadata is trusted input.

- **session_allowed** (L3): `isinstance` (L4); `ValueError` (L4); `policy.get` (L5); `isinstance` (L6); `any` (L6); `isinstance` (L6); `any` (L6); `isinstance` (L6); `owners.values` (L6); `ValueError` (L7); `policy.get` (L8); `isinstance` (L9); `ValueError` (L9); `isinstance` (L11); `set` (L11); `isinstance` (L12); `isinstance` (L13); `isinstance` (L13); `ValueError` (L14); `context.get` (L15); `str(context.get('chat_type') or '').lower` (L16); `str` (L16); `context.get` (L16); `str` (L22); `context.get` (L22); `owners.get` (L22)

## personal_memory/adapters.py

Bundled deterministic and model-backed adapters. No untrusted code execution.

- **_reject_nonfinite** (L23): `ValueError` (L24)
- **_model_object** (L27): `ValueError` (L36); `isinstance` (L37); `content.strip` (L37); `ValueError` (L38); `content.strip` (L39); `content.startswith` (L40); `content.splitlines` (L41); `len` (L42); `lines[0].strip().lower` (L42); `lines[0].strip` (L42); `lines[-1].strip` (L42); `ValueError` (L43); `'\n'.join(lines[1:-1]).strip` (L44); `'\n'.join` (L44); `json.loads` (L45); `ValueError` (L47); `isinstance` (L48); `ValueError` (L48)
- **extractive** (L51): `quote.strip` (L55); `quotes.append` (L55); `parts.append` (L55); `'\n'.join` (L56)
- **openai_consolidate** (L59): `Client` (L61); `config.get` (L61); `config.get` (L61); `deepcopy` (L62); `sorted` (L63); `request.get` (L63); `isinstance` (L63); `isinstance` (L63); `entity.get` (L63); `json.dumps` (L76); `client.call` (L79); `client.call` (L83); `_model_object` (L84)
- **policy_fixture** (L87): `context.get` (L90); `context.get` (L91)
- **echo_capability** (L94): No direct calls.
- **openai_plan** (L99): `_structured_model` (L100)
- **openai_rerank** (L103): `_structured_model` (L104)
- **_structured_model** (L107): `Client(config['url'], config.get('token', ''), timeout=config.get('timeout', 2)).call` (L109); `Client` (L109); `config.get` (L109); `config.get` (L109); `json.dumps` (L110); `json.loads` (L111)
- **event_fixture** (L114): No direct calls.
- **openai_evaluate** (L119): `_structured_model` (L121)

## personal_memory/adaptive.py

Bounded progressive recall. Stopping is operational, never a proof of sufficiency.

- **validate_recall_config** (L8): `isinstance` (L11); `set` (L11); `ValueError` (L11); `config.values` (L12); `isinstance` (L13); `isinstance` (L13); `adapter.get` (L13); `set` (L13); `ValueError` (L13)
- **AdaptiveRecall.__init__** (L17): `validate_recall_config` (L19)
- **AdaptiveRecall.search** (L21): `required_text` (L22); `type` (L23); `ValueError` (L23); `type` (L24); `ValueError` (L24); `type` (L25); `ValueError` (L25); `time.monotonic` (L26); `self.config.get` (L29); `run_adapter` (L33); `planner.get` (L33); `isinstance` (L34); `set` (L34); `ValueError` (L34); `isinstance` (L36); `len` (L36); `ValueError` (L36); `required_text` (L37); `type` (L38); `s.strip` (L40); `re.split` (L40); `s.strip` (L40); `s.strip` (L40); `isinstance` (L43); `len` (L43); `ValueError` (L43); `list` (L44); `dict.fromkeys` (L44); `required_text` (L44); `set` (L45); `ValueError` (L45); `enumerate` (L48); `time.monotonic` (L49); `self.backend.search` (L51); `result.get` (L53); `result.get` (L57); `pool.items` (L58); `result.get` (L59); `result.get` (L60); `result.get('diagnostics', {}).get` (L61); `result.get` (L61); `trace.append` (L62); `len` (L62); `result.get` (L62); `self.config.get` (L67); `time.monotonic` (L68); `r.get` (L71); `pool.values` (L71); `run_adapter` (L72); `reranker.get` (L72); `isinstance` (L74); `len` (L74); `len` (L74); `set` (L74); `set` (L74); `ValueError` (L74); `type` (L76); `filters.get` (L78); `self.store.related_ids` (L78); `self.store.connect` (L79); `filters.get` (L82); `pool.items` (L83); `db.execute('SELECT text,source,occurred_at FROM records WHERE id=? AND deleted=0', (rid,)).fetchone` (L84); `db.execute` (L84); `lifecycle.live_and_visible` (L88); `filters.get` (L90); `filters.get` (L91); `filters.get` (L92); `' '.join` (L93); `current[0].split` (L93); `duplicates[normalized]['duplicate_record_ids'].append` (L95); `len` (L96); `min` (L97); `row.get` (L97); `len` (L97); `min` (L97); `len` (L97); `len` (L100); `episodes.append` (L101); `len` (L101); `db.execute("SELECT c.* FROM claims c JOIN records r ON c.record_id=r.id WHERE c.id=? AND r.deleted=0 AND c.status!='retracted'", (cid,)).fetchone` (L104); `db.execute` (L104); `lifecycle.live_and_visible` (L105); `dict` (L106); `len` (L107); `len` (L107); `item.get` (L107); `selected_claims.append` (L108); `round` (L110); `time.monotonic` (L110); `any` (L111); `t.get` (L111)

## personal_memory/asgi.py

Production ASGI application. Serve with one Uvicorn worker on loopback.

- **strict_json** (L16): `json.loads` (L24)
- **strict_json.object_pairs** (L17): `ValueError` (L20)
- **strict_json.nonfinite** (L23): `ValueError` (L23)
- **ProcessLease.__init__** (L28): `Path` (L30); `path.parent.mkdir` (L30); `path.open` (L31); `path.chmod` (L31); `self.file.seek` (L35); `self.file.write` (L36); `self.file.flush` (L36); `self.file.seek` (L37); `msvcrt.locking` (L38); `self.file.fileno` (L38); `fcntl.flock` (L41); `self.file.fileno` (L41); `self.file.close` (L43); `RuntimeError` (L43)
- **ProcessLease.close** (L44): `self.file.close` (L44)
- **Application.__init__** (L48): `ThreadPoolExecutor` (L50); `asyncio.Semaphore` (L51)
- **Application._release** (L53): `getattr` (L57); `resource.close` (L60); `LOG.warning` (L62); `setattr` (L64)
- **Application._release_all** (L67): `self._release` (L72)
- **Application.__call__** (L75): `receive` (L78); `self._release_all` (L83); `ProcessLease` (L84); `Path` (L84); `Runtime(self.settings).start` (L86); `Runtime` (L86); `MemoryService` (L87); `self.settings.get` (L88); `self.settings.get` (L88); `self.settings.get` (L89); `self.settings.get` (L89); `self.settings.get` (L89); `self.settings.get` (L89); `getattr` (L94); `callable` (L95); `prime` (L95); `getattr` (L97); `callable` (L98); `threading.Thread(target=warm, daemon=True, name='memory-warmup').start` (L99); `threading.Thread` (L99); `send` (L100); `self._release_all` (L104); `send` (L105); `type` (L105); `self._release` (L108); `self.executor.shutdown` (L110); `LOG.warning` (L112); `self._release` (L113); `self._release` (L114); `send` (L116); `send` (L120); `new_trace` (L124); `respond` (L136); `sanitize_trace` (L138); `headers.get` (L138); `respond` (L141); `self.service.authenticate` (L142); `headers.get(b'authorization', b'').decode` (L142); `headers.get` (L142); `respond` (L143); `respond` (L145); `respond` (L146); `headers.get(b'content-type', b'').split` (L147); `headers.get` (L147); `respond` (L148); `respond` (L149); `self.slots.locked` (L150); `respond` (L150); `asyncio.wait_for` (L161); `read_body` (L161); `strict_json` (L162); `isinstance` (L163); `ValueError` (L163); `asyncio.get_running_loop().run_in_executor` (L164); `asyncio.get_running_loop` (L164); `respond` (L166); `respond` (L167); `respond` (L169); `str` (L169); `respond` (L170); `respond` (L171); `respond` (L172); `respond` (L174); `respond` (L175)
- **Application.__call__.respond** (L125): `isinstance` (L126); `json.dumps(data, ensure_ascii=False, allow_nan=False).encode` (L127); `json.dumps` (L127); `send` (L128); `str(len(raw)).encode` (L130); `str` (L130); `len` (L130); `trace.encode` (L131); `send` (L132)
- **Application.__call__.read_body** (L153): `bytearray` (L154); `receive` (L156); `ConnectionError` (L157); `body.extend` (L158); `message.get` (L158); `len` (L159); `OverflowError` (L159); `message.get` (L160)

## personal_memory/awareness.py

Awareness consumers: deterministic attention policy, grouping, budgets and

- **ensure** (L72): `db.executescript` (L73); `db.execute` (L74); `db.execute` (L76); `db.execute` (L77); `db.execute` (L79)
- **initialize** (L82): `store.connect` (L83); `ensure` (L84)
- **_moment** (L87): `datetime.now` (L88)
- **_policy** (L91): `dict` (L92); `isinstance` (L93); `policy.update` (L94); `stored.items` (L94)
- **_delivery_config** (L98): `isinstance` (L101); `set` (L102); `ValueError` (L103); `isinstance` (L105); `ValueError` (L106); `delivery.get` (L107); `isinstance` (L108); `len` (L109); `ValueError` (L110); `delivery.get` (L111); `isinstance` (L113); `set` (L113); `ValueError` (L114); `window.values` (L115); `isinstance` (L116); `re.fullmatch` (L116); `ValueError` (L117)
- **configure_consumer** (L121): `required_text` (L123); `required_text` (L124); `ValueError` (L126); `isinstance` (L128); `any` (L128); `ValueError` (L129); `sorted` (L129); `policy.items` (L130); `ValueError` (L133); `isinstance` (L134); `isinstance` (L134); `ValueError` (L135); `isinstance` (L136); `any` (L137); `isinstance` (L137); `ValueError` (L138); `_delivery_config` (L139); `store.connect` (L140); `db.execute` (L141); `db.execute('SELECT * FROM awareness_consumers WHERE id=?', (id,)).fetchone` (L142); `db.execute` (L142); `now` (L143); `db.execute('SELECT COALESCE(MAX(sequence),0) FROM memory_changes WHERE memory_epoch<>?', (_epoch(db),)).fetchone` (L145); `db.execute` (L145); `_epoch` (L147); `db.execute` (L148); `json.dumps` (L151); `json.dumps` (L152); `json.dumps` (L153); `row.keys` (L155); `json.loads` (L155); `db.execute` (L156); `json.dumps` (L158); `json.dumps` (L159); `json.dumps` (L160); `get_consumer` (L161)
- **get_consumer** (L164): `store.connect` (L165); `_consumer_row` (L166)
- **_consumer_row** (L169): `db.execute('SELECT * FROM awareness_consumers WHERE id=?', (id,)).fetchone` (L170); `db.execute` (L170); `ValueError` (L172); `dict` (L173); `_policy` (L174); `json.loads` (L174); `json.loads` (L175); `json.loads` (L176); `consumer.get` (L176); `bool` (L177); `bool` (L178)
- **decide** (L182): No direct calls.
- **group_of** (L205): `event.get` (L208)
- **_event_chars** (L213): `max` (L216); `len` (L216); `json.dumps` (L216); `event.get` (L216); `min` (L219); `int` (L219); `event.get` (L219)
- **_epoch** (L222): `db.execute('SELECT value FROM memory_epoch WHERE id=1').fetchone` (L223); `db.execute` (L223)
- **_stalled_source_sql** (L232): No direct calls.
- **_claimable_sql** (L236): `_stalled_source_sql` (L251)
- **_invalid_batch_evidence** (L254): `db.execute('SELECT 1 FROM awareness_batch_events m JOIN memory_change_refs ref ON ref.event_id=m.event_id JOIN records rec ON rec.id=ref.record_id LEFT JOIN record_visibility vis ON vis.record_id=rec.id WHERE m.batch_id=? AND (rec.deleted!=0 OR COALESCE(vis.hidden,0)!=0) LIMIT 1', (batch_id,)).fetchone` (L256); `db.execute` (L256)
- **invalidate_record** (L264): `db.execute` (L266); `db.execute` (L270); `db.executemany` (L272); `db.executemany` (L274); `now` (L276)
- **pending** (L279): `store.connect` (L284); `_consumer_row` (L285); `now` (L286); `_epoch` (L287); `int` (L289); `db.execute('SELECT EXISTS(SELECT 1 FROM awareness_batches WHERE consumer_id=? AND memory_epoch=? AND ' + _claimable_sql(hold) + ')', (consumer_id, epoch, stamp, stamp)).fetchone` (L289); `db.execute` (L289); `_claimable_sql` (L291); `int` (L293); `db.execute('SELECT EXISTS(SELECT 1 FROM memory_changes WHERE sequence>? AND memory_epoch=?)', (consumer['cursor_seq'], epoch)).fetchone` (L293); `db.execute` (L293); `bool` (L296)
- **sweep** (L301): `store.connect` (L305); `db.execute` (L306); `_consumer_row` (L307); `changes.config` (L309); `max` (L314); `db.execute('SELECT MIN(sequence) FROM memory_changes WHERE memory_epoch=?', (_epoch(db),)).fetchone` (L315); `db.execute` (L315); `_epoch` (L316); `_epoch` (L320); `dict` (L321); `db.execute` (L321); `OrderedDict` (L325); `decide` (L328); `groups.setdefault((decision, event['source'], group_of(event)), []).append` (L336); `groups.setdefault` (L336); `group_of` (L336); `now` (L344); `db.execute` (L345); `db.execute` (L348); `"UPDATE awareness_batches SET state='held',lease_owner=NULL,lease_until=NULL,updated_at=? WHERE consumer_id=? AND state='pending' AND source IN ({})".format` (L348); `','.join` (L350); `len` (L350); `sorted` (L351); `db.execute("SELECT id,source FROM awareness_batches WHERE consumer_id=? AND state='held'", (consumer_id,)).fetchall` (L354); `db.execute` (L354); `db.executemany` (L357); `groups.items` (L385); `_event_chars` (L388); `len` (L391); `flush` (L392); `current.append` (L394); `part.append` (L396); `len` (L399); `flush` (L400); `current.append` (L402); `flush` (L404); `db.execute` (L408); `digest` (L410); `json.dumps` (L412); `db.execute` (L413); `int` (L414); `len` (L415); `bool` (L416)
- **sweep.flush** (L363): `'+'.join` (L369); `sorted` (L369); `sum` (L370); `_event_chars` (L370); `digest` (L371); `sorted` (L371); `db.execute` (L373); `min` (L377); `max` (L377); `digest` (L378); `sorted` (L378); `len` (L378); `db.executemany` (L380)
- **claim** (L419): `required_text` (L422); `type` (L423); `ValueError` (L424); `store.connect` (L425); `db.execute` (L426); `_consumer_row` (L427); `now` (L428); `_epoch` (L429); `db.execute` (L440); `db.execute` (L443); `db.execute` (L447); `db.execute` (L458); `db.execute('SELECT * FROM awareness_batches WHERE consumer_id=? AND memory_epoch=? AND ' + _claimable_sql(hold) + ' ORDER BY priority,seq_from', (consumer_id, epoch, stamp, stamp)).fetchall` (L459); `db.execute` (L459); `_claimable_sql` (L461); `(_moment() + timedelta(seconds=ttl)).isoformat` (L468); `_moment` (L468); `timedelta` (L468); `db.execute` (L469); `db.execute` (L472); `dict` (L474); `db.execute` (L474); `dict` (L478); `lease.update` (L479); `lease.pop` (L481)
- **_validated_lease** (L485): `ValueError` (L488); `db.execute('SELECT * FROM awareness_batches WHERE id=?', (lease['batch_id'],)).fetchone` (L489); `db.execute` (L489); `ValueError` (L491); `now` (L494); `ValueError` (L495); `_epoch` (L496); `ValueError` (L497)
- **_advance_done** (L501): `db.execute("SELECT MIN(seq_from) FROM awareness_batches WHERE consumer_id=? AND state NOT IN ('complete','cancelled','suppressed')", (consumer_id,)).fetchone` (L504); `db.execute` (L504); `db.execute("SELECT MAX(seq_to) FROM awareness_batches WHERE consumer_id=? AND state='complete'", (consumer_id,)).fetchone` (L509); `db.execute` (L509); `db.execute('SELECT done_through FROM awareness_consumers WHERE id=?', (consumer_id,)).fetchone` (L511); `db.execute` (L511); `max` (L513)
- **_validate_result** (L516): `len` (L519); `ValueError` (L520); `db.execute` (L521); `db.execute` (L524); `isinstance` (L532); `item.get` (L532); `isinstance` (L533); `ValueError` (L534); `cites.append` (L535); `isinstance` (L538); `item.get` (L538); `ValueError` (L539); `props.append` (L540); `len` (L541); `json.dumps` (L541); `ValueError` (L543)
- **complete** (L547): `store.connect` (L548); `db.execute` (L549); `_validated_lease` (L550); `now` (L551); `str` (L554); `isinstance` (L554); `summary.get` (L554); `_validate_result` (L555); `db.execute` (L556); `db.execute` (L559); `digest` (L561); `json.dumps` (L562); `list` (L562); `json.dumps` (L562); `list` (L562); `any` (L565); `_consumer_row` (L566); `_queue_delivery_db` (L567); `db.execute` (L569); `digest` (L571); `json.dumps` (L572); `_advance_done` (L574); `db.execute` (L575); `max` (L576); `max` (L577)
- **defer** (L583): `type` (L586); `ValueError` (L587); `store.connect` (L588); `db.execute` (L589); `_validated_lease` (L590); `_consumer_row` (L591); `now` (L592); `db.execute` (L595); `str` (L597); `(_moment() + timedelta(seconds=retry_in)).isoformat` (L598); `_moment` (L598); `timedelta` (L598); `db.execute` (L600); `digest` (L602); `json.dumps` (L603); `str` (L603)
- **prepare** (L608): `required_text` (L611); `store.connect` (L612); `db.execute` (L613); `_consumer_row` (L614); `_epoch` (L616); `db.execute("SELECT * FROM awareness_batches WHERE consumer_id=? AND decision='next_turn' AND state IN ('pending','leased','retry_wait') AND memory_epoch=? ORDER BY seq_from", (consumer_id, epoch)).fetchall` (L617); `db.execute` (L617); `db.execute("SELECT 1 FROM awareness_receipts WHERE kind='supplied' AND subject=? AND state='exposed' AND memory_epoch=?", (f"{batch['id']}|{session_id}", epoch)).fetchone` (L622); `db.execute` (L622); `dict` (L627); `db.execute` (L627); `OrderedDict` (L631); `db.execute` (L635); `len` (L647); `len` (L648); `_readiness_row` (L651); `grouped.setdefault(group_of(event), []).append` (L652); `grouped.setdefault` (L652); `group_of` (L652); `grouped.items` (L653); `digest` (L657); `db.execute("SELECT 1 FROM awareness_receipts WHERE kind='supplied' AND subject=? AND state='exposed' AND memory_epoch=?", (subject, epoch)).fetchone` (L659); `db.execute` (L659); `db.execute('SELECT r.summary,r.citations,r.batch_id FROM awareness_results r JOIN awareness_batch_events m ON m.batch_id=r.batch_id WHERE m.event_id=? AND r.memory_epoch=? ORDER BY r.created_at DESC, r.batch_id LIMIT 1', (event['event_id'], epoch)).fetchone` (L667); `db.execute` (L667); `_invalid_batch_evidence` (L673); `json.loads` (L674); `pending_groups.append` (L680); `len` (L683); `len` (L685); `json.dumps` (L685); `group.items` (L687); `len` (L689); `json.dumps` (L689); `selected.append` (L692); `now` (L694); `digest` (L695); `digest` (L699); `db.execute` (L702); `digest` (L704); `json.dumps` (L706); `len` (L709); `len` (L709)
- **expose** (L714): `required_text` (L717); `required_text` (L718); `store.connect` (L719); `db.execute` (L720); `db.execute("SELECT * FROM awareness_receipts WHERE kind='supplied' AND json_extract(detail,'$.packet_id')=? AND json_extract(detail,'$.session')=? AND memory_epoch=?", (packet_id, session_id, _epoch(db))).fetchall` (L721); `db.execute` (L721); `_epoch` (L724); `ValueError` (L726); `all` (L727); `now` (L728); `db.execute` (L730); `str` (L732)
- **mark_readiness** (L737): `required_text` (L740); `ValueError` (L742); `ValueError` (L744); `isinstance` (L745); `ValueError` (L746); `store.connect` (L747); `db.execute` (L748); `db.execute('SELECT 1 FROM records WHERE id=?', (record_id,)).fetchone` (L749); `db.execute` (L749); `ValueError` (L750); `db.execute` (L751); `json.dumps` (L752); `now` (L753)
- **_readiness_row** (L757): `db.execute('SELECT rec.deleted, COALESCE(vis.hidden,0) AS hidden FROM records rec LEFT JOIN record_visibility vis ON vis.record_id=rec.id WHERE rec.id=?', (record_id,)).fetchone` (L759); `db.execute` (L759); `db.execute('SELECT state FROM processing_readiness WHERE record_id=? AND stage=?', (record_id, stage)).fetchone` (L766); `db.execute` (L766); `db.execute("SELECT 1 FROM sqlite_master WHERE type='table' AND name=?", (table,)).fetchone` (L772); `db.execute` (L772); `db.execute(f'SELECT 1 FROM {table} WHERE record_id=? LIMIT 1', (record_id,)).fetchone` (L777); `db.execute` (L777)
- **readiness** (L785): `store.connect` (L788); `dict.fromkeys` (L790); `str` (L790); `_readiness_row` (L791)
- **material_enrichment** (L795): `store.connect` (L800); `db.execute` (L801); `db.execute('SELECT 1 FROM memory_changes WHERE event_id=?', (cause_event_id,)).fetchone` (L802); `db.execute` (L802); `ValueError` (L804); `db.execute('SELECT 1 FROM records WHERE id=? AND deleted=0', (record_id,)).fetchone` (L806); `db.execute` (L806); `ValueError` (L808); `changes.append` (L809); `list` (L811); `ValueError` (L816)
- **_in_quiet** (L820): `isinstance` (L821); `minutes` (L828); `minutes` (L828)
- **_in_quiet.minutes** (L824): `text.split` (L825); `int` (L826); `int` (L826)
- **_queue_delivery_db** (L837): `isinstance` (L839); `ValueError` (L840); `_epoch` (L841); `ValueError` (L842); `delivery.get` (L844); `delivery.get` (L844); `digest` (L846); `db.execute('SELECT * FROM awareness_deliveries WHERE idempotency_key=?', (key,)).fetchone` (L848); `db.execute` (L848); `_in_quiet` (L852); `delivery.get` (L852); `_moment` (L852); `delivery.get` (L853); `now` (L856); `db.execute` (L857); `int` (L861)
- **queue_delivery** (L865): `required_text` (L867); `isinstance` (L868); `ValueError` (L869); `store.connect` (L870); `db.execute` (L871); `_consumer_row` (L872); `db.execute('SELECT * FROM awareness_batches WHERE id=?', (batch_id,)).fetchone` (L873); `db.execute` (L873); `ValueError` (L876); `ValueError` (L878); `_queue_delivery_db` (L879)
- **next_delivery** (L882): `ValueError` (L889); `store.connect` (L890); `db.execute` (L891); `db.execute("SELECT * FROM awareness_deliveries WHERE state='queued' ORDER BY created_at,id LIMIT 1").fetchone` (L893); `db.execute` (L893); `_consumer_row` (L896); `ValueError` (L898); `db.execute("SELECT * FROM awareness_deliveries WHERE state='queued' AND consumer_id=? AND profile=? ORDER BY created_at,id LIMIT 1", (consumer_id, profile)).fetchone` (L899); `db.execute` (L899); `db.execute` (L905); `now` (L906); `dict` (L907); `claimed.update` (L908)
- **confirm_delivery** (L912): `required_text` (L913); `store.connect` (L914); `db.execute` (L915); `db.execute('SELECT * FROM awareness_deliveries WHERE id=?', (delivery_id,)).fetchone` (L916); `db.execute` (L916); `ValueError` (L919); `ValueError` (L923); `db.execute` (L924); `str` (L925); `now` (L925)
- **reconcile_deliveries** (L929): `type` (L933); `ValueError` (L934); `(_moment() - timedelta(seconds=stale_seconds)).isoformat` (L935); `_moment` (L935); `timedelta` (L935); `store.connect` (L936); `db.execute` (L937); `db.execute` (L938); `db.execute` (L942); `now` (L945); `db.execute` (L947); `_consumer_row` (L949); `delivery.get` (L952); `delivery.get` (L953); `db.execute` (L955); `now` (L957); `_in_quiet` (L958); `delivery.get` (L958); `_moment` (L958); `delivery.get` (L959); `db.execute` (L960); `now` (L961); `len` (L963)
- **revalidate_delivery** (L966): `store.connect` (L968); `db.execute` (L969); `db.execute('SELECT * FROM awareness_deliveries WHERE id=?', (delivery_id,)).fetchone` (L970); `db.execute` (L970); `ValueError` (L973); `db.execute('SELECT memory_epoch FROM awareness_batches WHERE id=?', (row['batch_id'],)).fetchone` (L976); `db.execute` (L976); `_epoch` (L978); `_invalid_batch_evidence` (L979); `db.execute` (L980); `now` (L983); `_consumer_row` (L985); `delivery.get` (L988); `delivery.get` (L989); `db.execute` (L991); `now` (L993); `_in_quiet` (L995); `delivery.get` (L995); `_moment` (L995); `delivery.get` (L996); `db.execute` (L998); `now` (L999)
- **compact** (L1004): `type` (L1007); `ValueError` (L1008); `store.connect` (L1009); `db.execute` (L1010); `db.execute('SELECT MIN(cursor_seq) FROM awareness_consumers').fetchone` (L1011); `db.execute` (L1011); `dict` (L1012); `db.execute` (L1012); `ValueError` (L1016); `db.execute` (L1017); `db.execute` (L1019); `db.executemany` (L1022); `db.executemany` (L1023); `now` (L1025); `db.executemany` (L1028); `now` (L1030); `db.executemany` (L1031); `db.execute` (L1032); `db.execute('SELECT id FROM awareness_consumers WHERE cursor_seq<? OR done_through<?', (through, through)).fetchall` (L1035); `db.execute` (L1035); `sorted` (L1037); `db.execute` (L1039); `now` (L1040); `db.execute` (L1041); `str` (L1042); `now` (L1042); `json.dumps` (L1043); `len` (L1043); `len` (L1044)
- **replay** (L1047): `type` (L1051); `ValueError` (L1052); `store.connect` (L1053); `_consumer_row` (L1054); `ValueError` (L1057); `max` (L1058); `db.execute("SELECT COUNT(*) FROM awareness_deliveries WHERE consumer_id=? AND state IN ('attempted','confirmed','uncertain')", (consumer_id,)).fetchone` (L1059); `db.execute` (L1059); `max` (L1063); `bool` (L1064); `db.execute` (L1067); `db.execute` (L1068); `max` (L1069); `now` (L1069); `db.execute` (L1073); `now` (L1076); `db.execute` (L1077); `now` (L1078); `json.dumps` (L1079)
- **diagnose** (L1084): `changes.config` (L1087); `initialize` (L1093); `store.connect` (L1094); `dict` (L1095); `db.execute` (L1095); `add` (L1096); `json.loads(row['delivery'] or '{}').get` (L1100); `json.loads` (L1100); `json.loads(row['delivery'] or '{}').get` (L1101); `json.loads` (L1101); `add` (L1102); `', '.join` (L1104); `sorted` (L1104); `db.execute("SELECT COUNT(*) FROM awareness_batches WHERE state='quarantined'").fetchone` (L1105); `db.execute` (L1105); `sum` (L1107); `add` (L1108); `len` (L1109); `db.execute("SELECT COUNT(*) FROM awareness_batches WHERE state='leased' AND lease_until<?", (_moment().isoformat(),)).fetchone` (L1112); `db.execute` (L1112); `_moment().isoformat` (L1113); `_moment` (L1113); `add` (L1114); `db.execute("SELECT stage,COUNT(*) FROM processing_readiness WHERE state='failed' GROUP BY stage ORDER BY stage").fetchall` (L1117); `db.execute` (L1117); `add` (L1119); `', '.join` (L1121)
- **diagnose.add** (L1090): `checks.append` (L1091); `bool` (L1091)
- **status** (L1125): `store.connect` (L1127); `db.execute` (L1129); `consumers.append` (L1130); `bool` (L1131); `bool` (L1134); `db.execute("SELECT COUNT(*) FROM awareness_batches WHERE consumer_id=? AND state IN ('pending','leased','retry_wait')", (row['id'],)).fetchone` (L1135); `db.execute` (L1135); `db.execute` (L1139); `db.execute` (L1141); `counts.get` (L1143); `counts.get` (L1144); `counts.get` (L1145); `sum` (L1146); `counts.items` (L1146); `counts.get` (L1147); `db.execute` (L1148); `_moment` (L1150); `db.execute("SELECT MIN(created_at) FROM awareness_batches WHERE state IN ('pending','retry_wait')").fetchone` (L1151); `db.execute` (L1151); `db.execute('SELECT COUNT(*) FROM memory_changes').fetchone` (L1153); `db.execute` (L1153); `db.execute('SELECT COUNT(*) FROM awareness_batches').fetchone` (L1154); `db.execute` (L1154); `db.execute('SELECT COALESCE(SUM(attempts),0) FROM awareness_batches').fetchone` (L1155); `db.execute` (L1155); `batches.get` (L1156); `batches.get` (L1158); `db.execute("SELECT COUNT(*) FROM awareness_batches WHERE state='leased' AND lease_until<?", (moment.isoformat(),)).fetchone` (L1159); `db.execute` (L1159); `moment.isoformat` (L1161); `max` (L1162); `int` (L1163); `(moment - datetime.fromisoformat(oldest)).total_seconds` (L1163); `datetime.fromisoformat` (L1163); `db.execute("SELECT COUNT(DISTINCT json_extract(detail,'$.packet_id')) FROM awareness_receipts WHERE kind='supplied'").fetchone` (L1164); `db.execute` (L1164); `changes.status` (L1167)

## personal_memory/awareness_worker.py

Source-neutral awareness execution through an injected Hermes agent run.

- **ServiceRetrieval.__init__** (L21): No direct calls.
- **ServiceRetrieval.search** (L24): `callable` (L25); `self._search` (L26); `self._search.call` (L27)
- **ServiceRetrieval.close** (L29): `callable` (L30); `getattr` (L31); `callable` (L32); `closer` (L33)
- **made_progress** (L36): `isinstance` (L44); `analysis.get` (L44); `isinstance` (L46); `(delivery or {}).get` (L46)
- **_batch_context** (L50): `set` (L54); `store.connect` (L56); `list` (L58); `db.execute` (L58); `events.append` (L68); `event.get` (L68); `seen_evidence.add` (L75); `min` (L78); `len` (L79); `evidence.append` (L80); `len` (L81); `len` (L81); `item['text'][:160].strip` (L88); `len` (L89); `query.split` (L89); `retrieval.search` (L92); `list` (L93); `found.get` (L94); `len` (L95); `seen.add` (L97); `related.append` (L98); `found.get` (L100)
- **process_once** (L114): `awareness.pending` (L116); `awareness.sweep` (L119); `awareness.claim` (L120); `uuid.uuid4` (L121); `_batch_context` (L125); `callable` (L125); `retrieval` (L125); `awareness.complete` (L127); `analyze` (L128); `isinstance` (L129); `set` (L129); `ValueError` (L130); `result.get` (L131); `isinstance` (L132); `summary.strip` (L132); `ValueError` (L133); `result.get` (L134); `result.get` (L135); `isinstance` (L136); `isinstance` (L136); `ValueError` (L137); `awareness.complete` (L141); `type` (L144); `str` (L144); `awareness.defer` (L146); `min` (L147); `min` (L147)
- **hermes_analyze** (L153): `set_hermes_home_override` (L161); `json.dumps` (L172); `str` (L173); `uuid.uuid4` (L175); `run_job` (L178); `reset_hermes_home_override` (L182); `RuntimeError` (L184); `response.strip` (L185); `response.startswith` (L186); `response.endswith` (L186); `response.split('\n', 1)[1].rsplit('```', 1)[0].strip` (L187); `response.split('\n', 1)[1].rsplit` (L187); `response.split` (L187); `json.loads` (L188)
- **_delivery_content** (L191): `store.connect` (L193); `db.execute("SELECT result.summary,result.citations FROM awareness_deliveries delivery JOIN awareness_results result ON result.batch_id=delivery.batch_id WHERE delivery.id=? AND delivery.state='attempted' AND result.memory_epoch=(SELECT value FROM memory_epoch WHERE id=1)", (intent['id'],)).fetchone` (L194); `db.execute` (L194); `ValueError` (L201); `json.loads` (L202); `', '.join` (L203); `str` (L203); `row['summary'].strip` (L204)
- **hermes_deliver** (L208): `dispatch_memory_notification` (L211)
- **deliver_once** (L215): `awareness.next_delivery` (L221); `awareness.revalidate_delivery` (L224); `_delivery_content` (L228); `dispatch` (L229); `isinstance` (L230); `receipt.strip` (L230); `RuntimeError` (L231); `awareness.confirm_delivery` (L232); `type` (L239); `str` (L239)

## personal_memory/backend.py

Retrieval extension boundary; canonical storage stays owned by Store.

- **RetrievalBackend.search** (L7): No direct calls.
- **load_backend** (L10): `Hybrid` (L17); `entrypoint.split` (L19); `getattr(importlib.import_module(module), factory)` (L20); `getattr` (L20); `importlib.import_module` (L20); `callable` (L21); `getattr` (L21); `ValueError` (L22)

## personal_memory/blobs.py

Resumable attachments inside the canonical SQLite backup/deletion boundary.

- **_get** (L12): `db.execute("SELECT b.* FROM memory_blobs b JOIN records r ON r.id=b.record_id WHERE b.id=? AND b.state!='forgotten' AND r.deleted=0", (blob_id,)).fetchone` (L13); `db.execute` (L13); `ValueError` (L14); `dict` (L15)
- **begin** (L18): `required_text` (L19); `required_text` (L19); `type` (L20); `ValueError` (L20); `isinstance` (L21); `len` (L21); `any` (L21); `ValueError` (L21); `digest` (L22); `store.connect` (L23); `db.execute` (L24); `db.execute('SELECT 1 FROM records WHERE id=? AND deleted=0', (record_id,)).fetchone` (L25); `db.execute` (L25); `ValueError` (L25); `db.execute` (L26); `_get` (L27); `db.execute` (L28)
- **put** (L32): `type` (L33); `isinstance` (L33); `len` (L33); `ValueError` (L33); `base64.b64decode` (L34); `store.connect` (L35); `db.execute` (L36); `_get` (L36); `ValueError` (L37); `math.ceil` (L38); `len` (L39); `min` (L39); `ValueError` (L39); `db.execute('SELECT data FROM memory_blob_chunks WHERE blob_id=? AND chunk_index=?', (blob_id, index)).fetchone` (L40); `db.execute` (L40); `ValueError` (L41); `db.execute` (L42)
- **complete** (L46): `store.connect` (L47); `db.execute` (L48); `_get` (L48); `hashlib.sha256` (L49); `db.execute` (L50); `ValueError` (L51); `hasher.update` (L52); `len` (L52); `hasher.hexdigest` (L53); `ValueError` (L53); `db.execute` (L54)
- **read** (L58): `store.connect` (L59); `_get` (L60); `ValueError` (L61); `type` (L62); `max` (L62); `math.ceil` (L62); `ValueError` (L62); `db.execute('SELECT data FROM memory_blob_chunks WHERE blob_id=? AND chunk_index=?', (blob_id, index)).fetchone` (L63); `db.execute` (L63); `blob.update` (L64); `base64.b64encode(row[0] if row else b'').decode` (L64); `base64.b64encode` (L64)
- **listing** (L68): `store.connect` (L69); `db.execute("SELECT b.id,b.filename,b.mime,b.size,b.sha256,b.state FROM memory_blobs b JOIN records r ON r.id=b.record_id WHERE b.record_id=? AND b.state='complete' AND r.deleted=0 ORDER BY b.id", (record_id,)).fetchall` (L70); `db.execute` (L70); `dict` (L71)
- **upload** (L74): `Path` (L76); `path.stat` (L76); `hashlib.sha256` (L76); `path.open` (L77); `src.read` (L78); `hasher.update` (L78); `client.call` (L79); `hasher.hexdigest` (L79); `set` (L81); `path.open` (L82); `src.read` (L84); `client.call` (L85); `base64.b64encode(data).decode` (L85); `base64.b64encode` (L85); `path.stat` (L87); `path.stat` (L87); `ValueError` (L87); `client.call` (L88)

## personal_memory/catalog.py

Source-backed account identities and paginated evidence access.

- **Catalog._participants** (L27): `metadata.get` (L28); `isinstance` (L29); `len` (L29); `ValueError` (L30); `required_text` (L32); `item.get` (L32); `required_text(item.get('address'), 'address', 500).strip` (L33); `required_text` (L33); `item.get` (L33); `required_text` (L34); `item.get` (L34); `required_text` (L35); `item.get` (L35); `address.rpartition` (L38); `ValueError` (L40); `domain.lower` (L41); `re.sub` (L43); `re.fullmatch` (L44); `ValueError` (L45); `digest` (L46); `db.execute` (L47); `now` (L47); `db.execute` (L48); `db.execute` (L49); `db.execute` (L50)
- **Catalog.identity** (L52): `ValueError` (L54); `timestamp` (L55); `timestamp` (L56); `ValueError` (L58); `self.connect` (L59); `db.execute('SELECT 1 FROM accounts WHERE entity_id=?', (account_id,)).fetchone` (L60); `db.execute` (L60); `ValueError` (L61); `db.execute("SELECT 1 FROM entities WHERE id=? AND kind='person'", (person_id,)).fetchone` (L62); `db.execute` (L62); `ValueError` (L63); `lifecycle.live_and_visible` (L66); `ValueError` (L67); `db.execute("SELECT 1 FROM identity_edges i\n                    JOIN records evidence ON evidence.id=i.record_id AND evidence.deleted=0\n                    WHERE i.account_id=? AND i.status='confirmed' AND i.person_id!=?\n                    AND NOT EXISTS(SELECT 1 FROM record_visibility v WHERE v.record_id=i.record_id AND v.hidden=1)\n                    AND (i.valid_to IS NULL OR ? IS NULL OR i.valid_to>?)\n                    AND (? IS NULL OR i.valid_from IS NULL OR i.valid_from<?)", (account_id, person_id, start, start, end, end)).fetchone` (L71); `db.execute` (L71); `ValueError` (L79); `digest` (L80); `db.execute` (L81); `now` (L82); `self.audit` (L83); `dict` (L84); `db.execute('SELECT * FROM identity_edges WHERE id=?', (edge_id,)).fetchone` (L84); `db.execute` (L84)
- **Catalog.identity_revoke** (L86): `self.connect` (L87); `db.execute('SELECT 1 FROM identity_edges WHERE id=?', (identity_id,)).fetchone` (L88); `db.execute` (L88); `ValueError` (L89); `db.execute` (L90); `self.audit` (L91)
- **Catalog.related_ids** (L94): `self.connect` (L95); `db.execute` (L96)
- **Catalog.connections** (L106): `self.connect` (L107); `dict` (L108); `db.execute` (L108)
- **Catalog.browse** (L114): `type` (L115); `ValueError` (L116); `clauses.append` (L118); `timestamp` (L120); `timestamp` (L121); `clauses.append` (L123); `args.append` (L123); `self.connect` (L125); `db.execute` (L127); `db.executemany` (L128); `self.related_ids` (L128); `clauses.append` (L129); `dict` (L130); `db.execute` (L130); `' AND '.join` (L130); `len` (L131); `len` (L132)

## personal_memory/changes.py

Durable memory-change journal: the shared evidence feed every awareness

- **record_id** (L52): `digest` (L54)
- **ensure** (L57): `db.executescript` (L59); `db.execute` (L60); `json.dumps` (L60); `now` (L60)
- **initialize** (L63): `store.connect` (L64); `ensure` (L65)
- **configure** (L68): `isinstance` (L71); `ValueError` (L72); `isinstance` (L74); `isinstance` (L74); `journal.get` (L74); `ValueError` (L75); `store.connect` (L76); `db.execute` (L77); `db.execute('SELECT * FROM changes_config WHERE id=1').fetchone` (L78); `db.execute` (L78); `initialize` (L80); `db.execute('SELECT * FROM changes_config WHERE id=1').fetchone` (L81); `db.execute` (L81); `int` (L82); `db.execute('SELECT COALESCE(MAX(sequence),0) FROM memory_changes').fetchone` (L85); `db.execute` (L85); `db.execute` (L86); `json.dumps` (L87); `now` (L87); `bool` (L88)
- **config** (L91): `store.connect` (L92); `db.execute('SELECT * FROM changes_config WHERE id=1').fetchone` (L93); `db.execute` (L93); `bool` (L96); `json.loads` (L96)
- **_enabled** (L100): `db.execute('SELECT enabled FROM changes_config WHERE id=1').fetchone` (L101); `db.execute` (L101); `bool` (L102)
- **classify** (L105): `isinstance` (L109); `coordinates.get` (L109); `datetime.now` (L115); `datetime.fromisoformat` (L115); `datetime.fromisoformat` (L116); `occurred_at.replace` (L116)
- **append** (L136): `ValueError` (L148); `ValueError` (L150); `_enabled` (L151); `isinstance` (L153); `classify` (L155); `ValueError` (L160); `required_text` (L161); `required_text` (L162); `required_text` (L163); `required_text` (L164); `db.execute('SELECT COALESCE(MAX(transition_version),0) FROM memory_changes WHERE source=? AND source_item_id=?', (source, source_item_id)).fetchone` (L165); `db.execute` (L165); `now` (L168); `digest` (L169); `db.execute('SELECT COALESCE(MAX(sequence),0) FROM memory_changes').fetchone` (L170); `db.execute` (L170); `db.execute` (L171); `db.execute('SELECT value FROM memory_epoch WHERE id=1').fetchone` (L176); `db.execute` (L176); `str` (L179); `json.dumps` (L180); `sorted` (L180); `set` (L180); `str` (L181); `len` (L181); `len` (L181); `db.executemany` (L182); `db.execute` (L185); `json.dumps` (L186)
- **_conversation_key** (L190): `operation.get` (L191); `operation.get` (L192); `coordinates.get` (L193); `metadata.get` (L193); `metadata.get` (L193)
- **describe_transition** (L196): `operation.get` (L204); `coordinates.get` (L205); `sorted` (L209); `set` (L209); `operation.get` (L212)
- **journal_operation_transition** (L225): `json.dumps` (L227); `operation.get` (L227); `describe_transition` (L228); `transition.get` (L234); `db.execute("SELECT event_id FROM memory_changes WHERE source=? AND source_item_id=? AND kind='created' ORDER BY sequence DESC LIMIT 1", (connection['source'], operation['source_id'])).fetchone` (L235); `db.execute` (L235); `operation.get` (L239); `records[0].get` (L240); `records[0].get` (L241); `transition.get` (L242); `append` (L243); `operation.get` (L248); `_conversation_key` (L250); `(operation.get('metadata') or {}).get` (L252); `operation.get` (L252)
- **journal_state_change** (L257): `append` (L259); `list` (L263)
- **journal_stream_lifecycle** (L267): `append` (L269)
- **journal_gap** (L276): `append` (L278); `str` (L282)
- **_window_bound** (L285): `datetime.fromisoformat(str(value).replace('Z', '+00:00')).isoformat` (L290); `datetime.fromisoformat` (L290); `str(value).replace` (L290); `str` (L290); `ValueError` (L292)
- **_filter_digest** (L295): `digest` (L296); `sorted` (L296)
- **_encode_cursor** (L299): `json.dumps` (L300); `digest` (L300); `base64.urlsafe_b64encode(raw.encode()).decode` (L301); `base64.urlsafe_b64encode` (L301); `raw.encode` (L301)
- **_decode_cursor** (L304): `json.loads` (L306); `base64.urlsafe_b64decode(value.encode()).decode` (L306); `base64.urlsafe_b64decode` (L306); `value.encode` (L306); `ValueError` (L309); `isinstance` (L310); `digest` (L310); `ValueError` (L313)
- **read** (L317): `required_text` (L321); `type` (L322); `ValueError` (L323); `isinstance` (L324); `set` (L324); `set` (L324); `ValueError` (L325); `_window_bound` (L326); `_window_bound` (L327); `ValueError` (L329); `config` (L330); `store.connect` (L334); `db.execute('SELECT value FROM memory_epoch WHERE id=1').fetchone` (L335); `db.execute` (L335); `db.execute('SELECT COALESCE(MAX(sequence),0) FROM memory_changes WHERE memory_epoch=?', (current_epoch,)).fetchone` (L336); `db.execute` (L336); `_filter_digest` (L346); `_decode_cursor` (L347); `db.execute('SELECT COALESCE(MIN(sequence),0) FROM memory_changes WHERE memory_epoch=?', (current_epoch,)).fetchone` (L348); `db.execute` (L348); `max` (L352); `clauses.append` (L356); `','.join` (L356); `len` (L356); `params.extend` (L357); `clauses.append` (L359); `params.append` (L359); `clauses.append` (L361); `params.append` (L361); `clauses.append` (L363); `params.append` (L363); `clauses.append` (L365); `params.append` (L365); `db.execute(f"SELECT * FROM memory_changes WHERE {' AND '.join(clauses)} ORDER BY sequence LIMIT ?", params + [limit + 1]).fetchall` (L366); `db.execute` (L366); `' AND '.join` (L366); `len` (L368); `_resolve` (L372); `len` (L373); `json.dumps(payload).encode` (L373); `json.dumps` (L373); `events.append` (L378); `_encode_cursor` (L381); `_filter_digest` (L382); `any` (L387)
- **store_generation_epoch** (L393): `store.connect` (L394); `db.execute('SELECT value FROM memory_epoch WHERE id=1').fetchone` (L395); `db.execute` (L395)
- **_resolve** (L398): `dict` (L402); `db.execute` (L403); `len` (L410); `json.loads` (L411); `len` (L412); `db.execute('SELECT 1 FROM memory_change_refs ref JOIN records rec ON rec.id=ref.record_id LEFT JOIN record_visibility vis ON vis.record_id=rec.id WHERE ref.event_id=? AND (rec.deleted!=0 OR COALESCE(vis.hidden,0)!=0) LIMIT 1', (event['event_id'],)).fetchone` (L413); `db.execute` (L413)
- **event_refs** (L428): `store.connect` (L429); `db.execute('SELECT ref.record_id,ref.role FROM memory_change_refs ref JOIN records rec ON rec.id=ref.record_id AND rec.deleted=0 LEFT JOIN record_visibility vis ON vis.record_id=rec.id WHERE ref.event_id=? AND COALESCE(vis.hidden,0)=0 ORDER BY ref.role,ref.record_id', (event_id,)).fetchall` (L430); `db.execute` (L430); `db.execute('SELECT record_count FROM memory_changes WHERE event_id=?', (event_id,)).fetchone` (L436); `db.execute` (L436); `ValueError` (L439)
- **pending_arrivals** (L446): `store.connect` (L449); `db.execute("SELECT m.source,m.source_item_id,m.novelty FROM memory_changes m\n            JOIN (SELECT source,source_item_id,MAX(sequence) AS top FROM memory_changes\n                  WHERE kind='created' GROUP BY source,source_item_id) latest\n              ON m.source=latest.source AND m.source_item_id=latest.source_item_id\n             AND m.sequence=latest.top ORDER BY m.source,m.source_item_id").fetchall` (L450); `db.execute` (L450)
- **consumer_positions** (L458): `store.connect` (L460); `db.execute("SELECT 1 FROM sqlite_master WHERE type='table' AND name='awareness_consumers'").fetchone` (L461); `db.execute` (L461); `db.execute` (L463)
- **status** (L466): `config` (L467); `store.connect` (L468); `db.execute` (L469); `db.execute` (L471); `db.execute('SELECT COALESCE(MAX(sequence),0) FROM memory_changes').fetchone` (L473); `db.execute` (L473)

Module/class/lambda calls: `<module> → timedelta` (L47)

## personal_memory/client.py

(Module entry point.)

- **NoRedirect.redirect_request** (L14): No direct calls.
- **ServiceError.__init__** (L19): `super().__init__` (L21); `super` (L21)
- **Client.__init__** (L25): `urlparse` (L26); `ValueError` (L28); `ValueError` (L30); `url.rstrip` (L31); `build_opener` (L32); `ProxyHandler` (L32); `NoRedirect` (L32)
- **Client.call** (L34): `getattr` (L35); `dict` (L36); `data.setdefault` (L36); `get_trace` (L39); `new_trace` (L40); `Request` (L41); `json.dumps(data or {}, ensure_ascii=False).encode` (L41); `json.dumps` (L41); `bind_trace` (L44); `time.monotonic` (L45); `self.opener.open` (L47); `response.read` (L48); `len` (L49); `RuntimeError` (L50); `log_call` (L53); `json.loads` (L54); `json.loads` (L60); `error.read` (L60); `payload.get` (L61); `payload.get` (L64); `isinstance` (L65); `payload.get` (L66); `log_call` (L70); `ServiceError` (L71); `payload.get` (L71); `log_call` (L73); `type` (L73)

Module/class/lambda calls: `<module> → logging.getLogger` (L10)

## personal_memory/common.py

(Module entry point.)

- **now** (L9): `datetime.now(timezone.utc).isoformat` (L10); `datetime.now` (L10)
- **digest** (L13): `hashlib.sha256(json.dumps(value, sort_keys=True, ensure_ascii=False, separators=(',', ':')).encode()).hexdigest` (L14); `hashlib.sha256` (L14); `json.dumps(value, sort_keys=True, ensure_ascii=False, separators=(',', ':')).encode` (L14); `json.dumps` (L14)
- **required_text** (L18): `isinstance` (L19); `value.strip` (L19); `len` (L19); `ValueError` (L20)
- **timestamp** (L24): `required_text` (L25); `datetime.fromisoformat` (L26); `value.replace('z', 'Z').replace` (L26); `value.replace` (L26); `ValueError` (L28); `parsed.astimezone(timezone.utc).isoformat` (L29); `parsed.astimezone` (L29)
- **atomic_write** (L32): `Path` (L39); `isinstance` (L40); `data.encode` (L41); `path.parent.mkdir` (L42); `tempfile.mkstemp` (L43); `os.fdopen` (L45); `stream.write` (L46); `stream.flush` (L47); `os.fsync` (L48); `stream.fileno` (L48); `os.chmod` (L49); `os.replace` (L50); `hasattr` (L51); `os.open` (L52); `os.fsync` (L53); `os.close` (L54); `os.path.exists` (L56); `os.unlink` (L57)
- **atomic_json** (L60): `atomic_write` (L61); `json.dumps(value, ensure_ascii=False, indent=2).encode` (L61); `json.dumps` (L61)

## personal_memory/configuration.py

Private bootstrap credentials plus Hermes's native flat-JSON behavior settings.

- **load_settings** (L7): `Path(home).expanduser` (L8); `Path` (L8); `json.loads` (L9); `(state / 'settings.json').read_text` (L9); `str` (L10); `Path(home).expanduser().resolve` (L10); `Path(home).expanduser` (L10); `Path` (L10); `public.exists` (L12); `json.loads` (L13); `public.read_text` (L13); `isinstance` (L14); `set` (L14); `ValueError` (L14); `cfg.update` (L15); `type` (L18); `ValueError` (L18); `session_allowed` (L21); `cfg.get` (L21); `normalize` (L23)

## personal_memory/curated.py

Versioned curated memory used by Hermes's native memory surfaces.

- **initialize** (L40): `store.connect` (L41); `db.executescript` (L42); `sorted` (L43); `db.execute` (L44); `now` (L44)
- **_target** (L47): `ValueError` (L49)
- **_ids** (L53): `isinstance` (L56); `len` (L56); `ValueError` (L57); `required_text` (L60); `result.append` (L62); `lifecycle.require_live_evidence` (L64)
- **_current** (L68): `db.execute('SELECT id,text,evidence_ids,position,created_version\n        FROM curated_entries WHERE target=? AND retired_version IS NULL\n        ORDER BY position,id', (target,)).fetchall` (L69); `db.execute` (L69); `dict` (L72); `json.loads` (L72)
- **_usage** (L75): `len` (L76); `'\n\n'.join` (L76); `min` (L78); `int` (L78)
- **read** (L81): `_target` (L82); `store.connect` (L83); `db.execute('SELECT version,updated_at FROM curated_heads WHERE target=?', (name,)).fetchone` (L86); `db.execute` (L86); `_current` (L87); `len` (L90); `_usage` (L90); `store.generation` (L93)
- **_match** (L96): `enumerate` (L97); `ValueError` (L99); `len` (L100); `ValueError` (L101)
- **_validate_operations** (L105): `isinstance` (L106); `len` (L106); `ValueError` (L107); `enumerate` (L109); `isinstance` (L110); `ValueError` (L111); `raw.get` (L112); `ValueError` (L114); `set` (L116); `ValueError` (L117); `len` (L119); `set` (L119); `ValueError` (L120); `normalized.append` (L121); `(raw.get('old_text') or '').strip` (L123); `raw.get` (L123); `(raw.get('content') or '').strip` (L124); `raw.get` (L124); `ValueError` (L126); `required_text` (L128); `normalized.append` (L129); `raw.get` (L130)
- **apply** (L134): `_target` (L136); `type` (L137); `ValueError` (L138); `type` (L139); `ValueError` (L140); `required_text` (L141); `_validate_operations` (L142); `digest` (L143); `store.connect` (L144); `db.execute` (L145); `db.execute('SELECT request_digest,result FROM curated_requests WHERE request_id=?', (request_id,)).fetchone` (L146); `db.execute` (L146); `ValueError` (L150); `json.loads` (L151); `db.execute('SELECT value FROM memory_epoch WHERE id=1').fetchone` (L152); `db.execute` (L152); `VersionConflict` (L153); `db.execute('SELECT version FROM curated_heads WHERE target=?', (target,)).fetchone` (L154); `db.execute` (L154); `VersionConflict` (L156); `_ids` (L157); `_current` (L158); `enumerate` (L159); `operation.get` (L164); `_ids` (L164); `operation.get` (L164); `any` (L166); `working.append` (L168); `_match` (L170); `working.pop` (L172); `_usage` (L175); `ValueError` (L177); `_current` (L179); `entry.get` (L180); `db.executemany` (L183); `sorted` (L184); `enumerate` (L185); `entry.get` (L186); `db.execute` (L187); `digest` (L189); `db.execute` (L190); `json.dumps` (L192); `now` (L193); `len` (L195); `db.execute` (L196); `now` (L197); `db.execute` (L198); `now` (L200); `store.audit` (L201); `len` (L205); `db.execute('SELECT max(id) FROM audit').fetchone` (L208); `db.execute` (L208); `db.execute` (L209); `json.dumps` (L210)
- **reset** (L214): `ValueError` (L216); `isinstance` (L217); `ValueError` (L218); `ValueError` (L222); `store.connect` (L223); `db.execute` (L224); `digest` (L225); `db.execute('SELECT request_digest,result FROM curated_requests WHERE request_id=?', (request_id,)).fetchone` (L226); `db.execute` (L226); `ValueError` (L228); `json.loads` (L229); `db.execute('SELECT value FROM memory_epoch WHERE id=1').fetchone` (L230); `db.execute` (L230); `VersionConflict` (L231); `db.execute('SELECT version FROM curated_heads WHERE target=?', (target,)).fetchone` (L233); `db.execute` (L233); `VersionConflict` (L235); `now` (L238); `db.execute` (L239); `db.execute` (L240); `db.execute` (L241); `store.audit` (L243); `_usage` (L245); `db.execute` (L247); `json.dumps` (L247)
- **invalidate** (L251): `db.execute` (L253); `db.execute('SELECT version FROM curated_heads WHERE target=?', (target,)).fetchone` (L257); `db.execute` (L257); `now` (L258); `digest` (L258); `db.execute` (L259); `db.execute` (L262); `db.execute` (L263); `digest` (L264); `db.execute('SELECT value FROM memory_epoch WHERE id=1').fetchone` (L265); `db.execute` (L265); `store.audit` (L266)

## personal_memory/dashboard_schema.py

Copied as the plugin root config_schema.py; pure host configuration declaration.


Module/class/lambda calls: `<module> → ProviderConfigSchema` (L4); `<module> → ProviderField` (L5); `<module> → ProviderField` (L7); `<module> → ProviderField` (L9); `<module> → ProviderField` (L11)

## personal_memory/deletions.py

Content-free deletion intents committed before canonical deletion.

- **DeletionLedger.__init__** (L9): `Path` (L10)
- **DeletionLedger.ids** (L11): `self.path.exists` (L12); `set` (L12); `closing` (L13); `sqlite3.connect` (L13); `self.path.resolve().as_uri` (L13); `self.path.resolve` (L13); `db.execute` (L14)
- **DeletionLedger.contains** (L15): `self.path.exists` (L16); `closing` (L17); `sqlite3.connect` (L17); `self.path.resolve().as_uri` (L17); `self.path.resolve` (L17); `db.execute('SELECT 1 FROM deletion_intents WHERE record_id=?', (record_id,)).fetchone` (L18); `db.execute` (L18)
- **DeletionLedger.contains_many** (L19): `list` (L20); `set` (L20); `self.path.exists` (L21); `set` (L21); `set` (L22); `closing` (L23); `sqlite3.connect` (L23); `self.path.resolve().as_uri` (L23); `self.path.resolve` (L23); `range` (L24); `len` (L24); `found.update` (L26); `db.execute` (L26); `','.join` (L26)
- **DeletionLedger.append** (L28): `required_text` (L29); `self.path.parent.mkdir` (L30); `closing` (L31); `sqlite3.connect` (L31); `db.execute` (L33); `db.execute` (L34); `db.executemany` (L35); `now` (L35); `self.path.chmod` (L36); `os.open` (L41); `os.fsync` (L42); `os.close` (L43)
- **DeletionLedger.merge** (L44): `self.append` (L44); `other.ids` (L44)

## personal_memory/extensions.py

Producer schemas remain optional; configured namespaces are validated server-side.

- **ExtensionRegistry.__init__** (L6): `schemas.items` (L18); `versions.items` (L20); `local_refs` (L21); `Draft202012Validator.check_schema` (L21); `Draft202012Validator` (L22); `FormatChecker` (L22)
- **ExtensionRegistry.__init__.local_refs** (L10): `isinstance` (L11); `isinstance` (L13); `value[key].startswith` (L13); `ValueError` (L14); `value.values` (L15); `local_refs` (L15); `isinstance` (L16); `local_refs` (L17)
- **ExtensionRegistry.validate** (L24): `enumerate` (L25); `record['extensions'].items` (L26); `ContractError` (L30); `next` (L31); `self.validators[namespace][version].iter_errors` (L31); `''.join` (L33); `str` (L33); `ContractError` (L34)

## personal_memory/gmail.py

Gmail source adapter. Transport is read-only; source commits belong to SourceSync.

- **decode** (L19): `base64.urlsafe_b64decode` (L20); `len` (L20)
- **GmailAdapter.__init__** (L24): `threading.Lock` (L27)
- **GmailAdapter.spec** (L29): `adapter_spec` (L30)
- **GmailAdapter.discover** (L34): `stream_spec` (L35)
- **GmailAdapter._access** (L37): `json.loads` (L38); `context['secrets']` (L38); `digest` (L39); `self.tokens.get` (L41); `time.monotonic` (L42); `token_request` (L45); `AdapterError` (L48); `time.monotonic` (L49); `int` (L49); `token.get` (L49)
- **GmailAdapter.request** (L52): `context.get('cancelled', lambda: False)` (L53); `context.get` (L53); `AdapterError` (L54); `self.transport` (L55); `urlencode` (L57); `range` (L58); `self._access` (L60); `urlopen` (L61); `Request` (L61); `response.read` (L62); `len` (L63); `AdapterError` (L63); `json.loads` (L64); `error.headers.get` (L68); `AdapterError` (L69); `path.split` (L69); `retry.isdigit` (L70); `min` (L70); `float` (L70); `AdapterError` (L74)
- **GmailAdapter.check** (L76): `self.request` (L77); `context['scope'].get` (L78); `profile['emailAddress'].casefold` (L79); `expected.casefold` (L79); `AdapterError` (L80)
- **GmailAdapter.normalize** (L84): `message.get` (L86); `root.get` (L86); `values.setdefault(h['name'].lower(), []).append` (L88); `values.setdefault` (L88); `h['name'].lower` (L88); `getaddresses` (L91); `values.get` (L91); `participants.append` (L93); `body` (L123); `'\n'.join` (L124); `', '.join` (L124); `datetime.fromtimestamp(int(message['internalDate']) / 1000, timezone.utc).isoformat` (L127); `datetime.fromtimestamp` (L127); `int` (L127); `parsedate_to_datetime` (L130); `values.get` (L130); `date.isoformat` (L131); `message.get` (L133); `message.get` (L133); `digest` (L136); `now` (L137); `enumerate` (L138); `range` (L138); `len` (L138); `records.append` (L139); `str` (L140); `normalized_item` (L146); `message.get` (L146)
- **GmailAdapter.normalize.body** (L95): `part.get` (L96); `part.get` (L96); `part.get` (L96); `content.get` (L97); `attachments.append` (L98); `part.get` (L98); `content.get` (L99); `content.get` (L100); `part.get` (L102); `next` (L105); `p.get` (L105); `body` (L106); `'\n'.join` (L107); `filter` (L107); `body` (L107); `content.get` (L109); `content.get` (L109); `AdapterError` (L110); `decode` (L111); `content.get` (L111); `part.get` (L114); `header['name'].lower` (L115); `Message` (L116); `m.get_content_charset` (L116); `raw.decode` (L117); `raw.decode` (L118); `PlainHTML` (L120); `parser.feed` (L120); `''.join` (L120)
- **GmailAdapter._message** (L148): `self.request` (L149); `quote` (L149); `getattr` (L151); `source_operation` (L152); `set` (L154); `message.get` (L154); `source_operation` (L155); `message.get` (L155); `hydrate` (L161); `message.get` (L161); `self.normalize` (L162); `message.get` (L163); `source_operation` (L166); `message.get` (L168); `message.get` (L169)
- **GmailAdapter._message.hydrate** (L156): `part.get` (L157); `part.get` (L158); `part.get` (L158); `content.get` (L158); `self.request` (L159); `quote` (L159); `quote` (L159); `part.get` (L160); `hydrate` (L160)
- **GmailAdapter.read_page** (L171): `self.accept_state` (L172); `cursor.get` (L175); `source_page` (L176); `read_state` (L176); `cursor.get` (L178); `context['scope'].get` (L181); `cursor.get` (L182); `self.request` (L183); `response.get` (L184); `response.get` (L185); `self._message` (L188); `len` (L189); `json.dumps(operation, ensure_ascii=False).encode` (L189); `json.dumps` (L189); `operations.append` (L191); `len` (L192); `cursor.get` (L193); `len` (L193); `cursor.get` (L195); `len` (L195); `digest` (L197); `o.get` (L197); `source_page` (L198); `digest` (L198); `read_state` (L199); `cursor.get` (L202); `cursor.get` (L204); `self.request` (L205); `response.get` (L206); `response.get` (L206); `cursor.get` (L209); `response.get` (L211); `int` (L212); `int` (L212); `history.get` (L214); `changes.get` (L216); `bool` (L218); `prior.get` (L218); `list` (L219); `changes.values` (L219); `cursor.get` (L219); `source_operation` (L222); `self._message` (L223); `len` (L225); `json.dumps(operation, ensure_ascii=False).encode` (L225); `json.dumps` (L225); `operations.append` (L227); `len` (L228); `len` (L228); `cursor.get` (L229); `len` (L229); `source_page` (L234); `digest` (L234); `read_state` (L235); `bool` (L236); `next_cursor.get` (L236); `next_cursor.get` (L236)
- **GmailAdapter.attachment** (L238): `quote` (L239); `descriptor.get` (L240); `self.request` (L241); `quote` (L241); `decode` (L242); `self.request` (L243); `find` (L251); `message.get` (L251); `AdapterError` (L252); `decode` (L253)
- **GmailAdapter.attachment.find** (L244): `part.get` (L245); `part.get('body', {}).get` (L246); `part.get` (L246); `part.get` (L247); `find` (L248)

## personal_memory/gmail_oauth.py

Desktop OAuth with a loopback callback, PKCE, and credentials kept out of logs.

- **token_request** (L18): `urlopen` (L20); `Request` (L20); `urlencode(fields).encode` (L20); `urlencode` (L20); `json.load` (L22); `ValueError` (L25)
- **authorize** (L28): `json.loads` (L29); `Path(client_file).read_text` (L29); `Path` (L29); `document.get` (L30); `isinstance` (L31); `client.get` (L31); `client.get` (L31); `ValueError` (L32); `client.get` (L33); `ValueError` (L34); `secrets.token_urlsafe` (L35); `secrets.token_urlsafe` (L35); `base64.urlsafe_b64encode(hashlib.sha256(verifier.encode()).digest()).decode().rstrip` (L36); `base64.urlsafe_b64encode(hashlib.sha256(verifier.encode()).digest()).decode` (L36); `base64.urlsafe_b64encode` (L36); `hashlib.sha256(verifier.encode()).digest` (L36); `hashlib.sha256` (L36); `verifier.encode` (L36); `HTTPServer` (L47); `urlencode` (L50); `on_ready` (L54); `webbrowser.open` (L55); `time.monotonic` (L56); `time.monotonic` (L57); `server.handle_request` (L57); `result.get` (L58); `result.get` (L58); `ValueError` (L59); `token_request` (L60); `token.get` (L62); `token.get('scope', '').split` (L62); `token.get` (L62); `ValueError` (L63)
- **authorize.Callback.log_message** (L39): No direct calls.
- **authorize.Callback.do_GET** (L40): `urlparse` (L41); `parse_qs` (L41); `secrets.compare_digest` (L42); `values.get` (L42); `self.send_response` (L43); `self.end_headers` (L43); `result.update` (L44); `values.get` (L44); `values.get` (L44); `self.send_response` (L45); `self.send_header` (L45); `self.send_header` (L45); `self.end_headers` (L45); `self.wfile.write` (L46)

## personal_memory/hindsight.py

Hindsight HTTP 0.9.2 adapter. Original local evidence owns truth and identity.

- **Hindsight.__init__** (L17): `config.get` (L18); `isinstance` (L18); `ValueError` (L19); `quote` (L22); `digest` (L25); `config.get` (L25); `config.get` (L26); `Client` (L27); `config.get` (L27); `config.get` (L27); `Client` (L28); `config.get` (L28); `config.get` (L28); `threading.RLock` (L34); `store.connect` (L35); `db.execute` (L36); `db.execute` (L38); `db.execute` (L42)
- **Hindsight._epoch** (L44): `read` (L50); `self.store.connect` (L51); `read` (L52)
- **Hindsight._epoch.read** (L45): `conn.execute("SELECT 1 FROM sqlite_master WHERE type='table' AND name='memory_epoch'").fetchone` (L47); `conn.execute` (L47); `conn.execute('SELECT value FROM memory_epoch WHERE id=1').fetchone` (L46); `conn.execute` (L46)
- **Hindsight._clear_pending** (L54): `read` (L58); `self.store.connect` (L59); `read` (L60)
- **Hindsight._clear_pending.read** (L55): `conn.execute('SELECT 1 FROM hindsight_bank_clear WHERE backend=?', (self.key,)).fetchone` (L56); `conn.execute` (L56)
- **Hindsight._perform_clear** (L62): `self.store.connect` (L66); `db.execute` (L67); `self._epoch` (L68); `now` (L68); `self.write_client.call` (L70); `type` (L72); `self.store.connect` (L74); `db.execute` (L75); `db.execute` (L76); `db.execute` (L77)
- **Hindsight.sync** (L81): `self._clear_pending` (L83); `self._perform_clear` (L86); `self._index_pass` (L87)
- **Hindsight._index_pass** (L89): `self.store.connect` (L90); `db.execute` (L93); `','.join` (L94); `time.time` (L95); `dict` (L96); `db.execute` (L96); `self._epoch` (L101); `self.write_client.call` (L105); `quote` (L105); `self.store.connect` (L106); `db.execute` (L107); `db.execute` (L108); `errors.append` (L110); `type` (L110); `self.store.connect` (L121); `db.execute` (L122); `','.join` (L125); `self._epoch` (L126); `self.store.connect` (L134); `db.executemany` (L135); `set` (L137); `self.store.connect` (L141); `db.executemany` (L142); `self._retain` (L150); `isinstance` (L155); `type` (L155); `isinstance` (L156); `len` (L157); `self._retain` (L160); `individual_errors.append` (L162); `type` (L162); `self._backoff` (L163); `errors.extend` (L164); `errors.append` (L166); `type` (L166); `self._backoff` (L167); `len` (L169); `len` (L169)
- **Hindsight._retain** (L171): `self.write_client.call` (L172); `result.get` (L173); `result.get` (L173); `result.get` (L174); `len` (L174); `len` (L174); `RuntimeError` (L175); `self.store.connect` (L177); `db.executemany` (L178); `db.executemany` (L180); `db.executemany` (L182); `now` (L183)
- **Hindsight._backoff** (L185): `self.store.connect` (L186); `db.executemany` (L187); `time.time` (L188); `type` (L188); `db.executemany` (L189); `json.dumps` (L191); `type` (L191); `now` (L191)
- **Hindsight.clear_bank** (L193): `self._perform_clear` (L203)
- **Hindsight.candidates** (L205): `now` (L207); `args.update` (L209); `self.read_client.call` (L210); `set` (L211); `response.get` (L212); `fact.get` (L213); `fact.get` (L221); `isinstance` (L222); `scores.get` (L222); `isinstance` (L223); `isinstance` (L223); `math.isfinite` (L224); `float` (L225); `rows.append` (L226); `found.add` (L226)
- **Hindsight.status** (L229): `self.store.connect` (L230); `self._clear_pending` (L231); `db.execute('SELECT count(*) FROM hindsight_done d JOIN records r ON r.id=d.record_id WHERE backend=? AND deleted=0 AND NOT EXISTS(SELECT 1 FROM record_visibility z WHERE z.record_id=r.id AND z.hidden=1)', (self.key,)).fetchone` (L232); `db.execute` (L232); `db.execute('SELECT count(*) FROM (SELECT backend,record_id FROM hindsight_done UNION SELECT backend,record_id FROM hindsight_pending) d JOIN records r ON r.id=d.record_id WHERE backend=? AND (r.deleted=1 OR EXISTS(SELECT 1 FROM record_visibility z WHERE z.record_id=r.id AND z.hidden=1))', (self.key,)).fetchone` (L233); `db.execute` (L233); `','.join` (L234); `db.execute("SELECT count(*) FROM records r WHERE deleted=0 AND NOT EXISTS(SELECT 1 FROM record_visibility z WHERE z.record_id=r.id AND z.hidden=1) AND r.source!='hermes-lineage' AND NOT EXISTS(SELECT 1 FROM hindsight_done d WHERE d.backend=? AND d.record_id=r.id)" + scope, [self.key] + ([] if '*' in self.sources else self.sources)).fetchone` (L235); `db.execute` (L235)

## personal_memory/hindsight_runtime.py

Managed Hindsight 0.9.2 lifecycle for the supported Hermes service path.

- **default_config** (L12): `hashlib.sha256(str(Path(data_dir).expanduser().resolve()).encode()).hexdigest` (L14); `hashlib.sha256` (L14); `str(Path(data_dir).expanduser().resolve()).encode` (L14); `str` (L14); `Path(data_dir).expanduser().resolve` (L14); `Path(data_dir).expanduser` (L14); `Path` (L14)
- **normalize** (L20): `settings.setdefault` (L22); `isinstance` (L23); `ValueError` (L23); `retrieval.get` (L24); `isinstance` (L25); `ValueError` (L25); `default_config` (L26); `set` (L30); `ValueError` (L31); `', '.join` (L31); `sorted` (L31); `supplied.items` (L32); `type` (L34); `ValueError` (L34); `isinstance` (L36); `merged.get` (L36); `merged[name].strip` (L36); `ValueError` (L36); `re.fullmatch` (L37); `ValueError` (L38); `len` (L39); `len` (L39); `ValueError` (L39); `isinstance` (L40); `merged.get` (L40); `any` (L40); `isinstance` (L40); `ValueError` (L41); `len` (L42); `any` (L42); `len` (L42); `ValueError` (L42); `isinstance` (L44); `len` (L44); `ValueError` (L44); `type` (L46); `ValueError` (L46); `isinstance` (L48); `merged.get` (L48); `merged['url'].startswith` (L48); `ValueError` (L49)
- **_automatic_provider** (L55): `any` (L57); `name.startswith` (L57); `Path(hermes_home or os.environ.get('HERMES_HOME', Path.home() / '.hermes')).expanduser` (L60); `Path` (L60); `os.environ.get` (L60); `Path.home` (L60); `(hermes_home / 'auth.json').is_file` (L61); `os.environ.get` (L62)
- **Runtime.__init__** (L68): No direct calls.
- **Runtime.start** (L71): `self.settings.get('retrieval', {}).get` (L72); `self.settings.get` (L72); `cfg.get` (L73); `RuntimeError` (L77); `cfg.get` (L78); `_automatic_provider` (L78); `self.settings.get` (L78); `cfg.get` (L80); `cfg.get` (L81); `cfg.get` (L82); `str` (L83); `any` (L86); `name.startswith` (L86); `daemon.update` (L87); `any` (L92); `name.startswith` (L92); `get_embed_manager` (L94); `self.manager.ensure_running` (L95); `self.manager.stop` (L96); `RuntimeError` (L97); `self.manager.get_url` (L98)
- **Runtime.close** (L102): `self.manager.stop` (L104)
- **Runtime.__enter__** (L106): `self.start` (L106)
- **Runtime.__exit__** (L107): `self.close` (L107)
- **_instance_roots** (L110): `Path(home or os.environ.get('HINDSIGHT_HOME', Path.home() / '.hindsight')).expanduser` (L112); `Path` (L112); `os.environ.get` (L112); `Path.home` (L112); `Path(os.environ.get('PG0_HOME', Path.home() / '.pg0')).expanduser` (L113); `Path` (L113); `os.environ.get` (L113); `Path.home` (L113)
- **stale_instances** (L117): `default_config` (L127); `_instance_roots` (L128); `set` (L129); `instances_dir.is_dir` (L130); `sorted` (L131); `instances_dir.iterdir` (L131); `name.startswith` (L133); `stale.append` (L134); `stems.add` (L134); `len` (L134); `profiles_dir.is_dir` (L135); `sorted` (L138); `profiles_dir.iterdir` (L138); `path.name.split` (L139); `stem.startswith` (L140); `stems.add` (L141); `stale.append` (L141); `str` (L142); `sorted` (L142)
- **prune_stale_instances** (L145): `stale_instances` (L152); `Path` (L156); `path.is_dir` (L158); `sum` (L158); `f.stat` (L158); `path.rglob` (L158); `f.is_file` (L158); `path.stat` (L158); `path.is_dir` (L159); `shutil.rmtree` (L159); `path.unlink` (L160); `removed.append` (L161); `errors.append` (L163); `type` (L163)

## personal_memory/host_bridge.py

Provider-side implementations of optional pinned Hermes host boundaries.

- **context_allowed** (L12): `kwargs.get` (L13); `isinstance` (L16); `Path(context.home).resolve` (L16); `Path` (L16); `Path(home).resolve` (L16); `Path` (L16); `str(kwargs.get('chat_type') or '').lower` (L17); `str` (L17); `kwargs.get` (L17); `kwargs.get` (L19); `str` (L21); `kwargs.get` (L21); `policy.get('owners', {}).get` (L22); `policy.get` (L22); `policy.get` (L25); `isinstance` (L26); `ValueError` (L26); `set` (L27); `isinstance` (L29); `set` (L29); `ValueError` (L30); `targets.add` (L31); `str(entry['platform']).lower` (L31); `str` (L31); `str` (L31); `str` (L31); `bool` (L32); `set` (L32); `sync_state` (L34); `Path` (L34); `db.execute` (L35); `json.dumps` (L36); `sorted` (L36); `int` (L36); `db.execute('SELECT targets,enabled FROM host_run_scopes WHERE job_id=? AND run_id=?', (context.job_id, context.run_id)).fetchone` (L37); `db.execute` (L37); `tuple` (L39); `ValueError` (L40); `db.execute` (L41)
- **check_delivery** (L47): `Path` (L48); `path.exists` (L49); `isinstance` (L49); `content.strip` (L49); `sqlite3.connect` (L50); `path.resolve().as_uri` (L50); `path.resolve` (L50); `db.execute("SELECT 1 FROM sqlite_master WHERE name='host_run_scopes'").fetchone` (L51); `db.execute` (L51); `db.execute('SELECT targets,enabled FROM host_run_scopes WHERE job_id=? AND run_id=?', (job_id, run_id)).fetchone` (L52); `db.execute` (L52); `sqlite3.connect` (L55); `path.resolve().as_uri` (L55); `path.resolve` (L55); `db.execute("SELECT 1 FROM sqlite_master WHERE name='host_delivery_payloads'").fetchone` (L56); `db.execute` (L56); `db.execute('SELECT record_id FROM host_delivery_payloads WHERE job_id=? AND run_id=? AND fingerprint=?', (job_id, run_id, digest(content.strip()))).fetchone` (L57); `db.execute` (L57); `digest` (L58); `content.strip` (L58); `load_settings` (L60); `Client` (L60); `cfg.get` (L60); `client.call` (L61); `sorted` (L64); `map` (L64); `json.loads` (L64); `sorted` (L64); `map` (L64); `load_settings(home).get` (L66); `load_settings` (L66); `r['platform'].lower` (L67); `policy.get` (L67); `set` (L68); `map` (L68)
- **local_process_caller** (L71): `current_transport` (L85)
- **authorize_native** (L88): `isinstance` (L90); `Path(context.home).resolve` (L91); `Path` (L91); `Path(home).resolve` (L91); `Path` (L91); `local_process_caller` (L92); `MemoryHostContext` (L96); `str` (L96); `Path(home).resolve` (L96); `Path` (L96); `isinstance` (L97); `context_allowed` (L98); `load_settings(home).get` (L98); `load_settings` (L98)
- **continuity** (L103): `load_settings(home).get` (L105); `load_settings` (L105); `context_allowed` (L106); `Path` (L108); `sync_state` (L109); `db.execute` (L110); `db.execute('SELECT record_id,targets FROM host_artifacts WHERE fingerprint=? AND kind=?', (digest([source_job_id or context.job_id, kind, content]), kind)).fetchone` (L111); `db.execute` (L111); `digest` (L112); `sorted` (L115); `map` (L115); `json.loads` (L115); `sorted` (L115); `load_settings` (L116); `Client` (L116); `cfg.get` (L116); `client.call` (L117); `client.call` (L121); `any` (L122); `page.get` (L123); `sync_state` (L126); `db.execute` (L127); `db.execute` (L128)
- **filter_native** (L132): `Path` (L138); `state.is_file` (L139); `sqlite3.connect` (L140); `state.resolve().as_uri` (L140); `state.resolve` (L140); `db.execute("SELECT 1 FROM sqlite_master WHERE name='native_history_archives'").fetchone` (L142); `db.execute` (L142); `db.execute` (L143); `dict` (L147); `sorted` (L148); `isinstance` (L148); `r.get` (L148); `sqlite3.connect` (L151); `Path(database).as_uri` (L151); `Path` (L151); `range` (L152); `len` (L152); `native.execute` (L154); `','.join` (L154); `sorted` (L157); `set` (L157); `sessions_by_id.values` (L157); `mappings.extend` (L161); `dict` (L161); `db.execute` (L161); `int` (L162); `r['source_id'].rsplit` (L162); `native_rows.items` (L164); `digest` (L167); `datetime.fromtimestamp(float(row[4]), timezone.utc).isoformat` (L167); `datetime.fromtimestamp` (L167); `float` (L167); `db.execute('SELECT source_id,record_id FROM native_history_fingerprints WHERE fingerprint=?', (fingerprint,)).fetchone` (L168); `db.execute` (L168); `mappings.append` (L170); `load_settings` (L173); `Client` (L174); `cfg.get` (L174); `range` (L176); `len` (L176); `states.update` (L178); `client.call` (L178); `list` (L178); `families.update` (L179); `client.call` (L179); `list` (L179); `set` (L181); `mapping.get` (L183); `int` (L183); `mapping['source_id'].rsplit` (L183); `by_native_id.setdefault(native_id, []).append` (L185); `by_native_id.setdefault` (L185); `affected.add` (L186); `row.get` (L189); `sessions_by_id.get` (L190); `all` (L193); `row.get` (L195); `output.append` (L199)
- **skill_revision** (L203): `isinstance` (L204); `len` (L204); `(Path(home) / 'skills').resolve` (L207); `Path` (L207); `root.exists` (L209); `root.rglob` (L210); `str` (L211); `path.parent.relative_to` (L211); `path.resolve` (L212); `resolved.is_relative_to` (L213); `resolved.stat` (L213); `matches.append` (L214); `str` (L214); `resolved.relative_to` (L214); `__import__('hashlib').sha256(resolved.read_bytes()).hexdigest` (L214); `__import__('hashlib').sha256` (L214); `__import__` (L214); `resolved.read_bytes` (L214); `len` (L215)
- **sync_on_read** (L218): `Path(database).resolve` (L225); `Path` (L225); `Path(home).resolve` (L226); `Path` (L226); `PermissionError` (L226); `sorted` (L227); `isinstance` (L227); `r.get` (L227); `stamp` (L236); `set` (L236); `sqlite3.connect` (L237); `database.as_uri` (L237); `range` (L238); `len` (L238); `sessions.update` (L240); `db.execute` (L240); `','.join` (L240); `Path` (L241); `sync_state` (L242); `db.execute` (L243); `db.execute` (L244); `str` (L244); `db.execute('SELECT 1 FROM native_read_sync WHERE database_path=? AND session_id=? AND stamp=?', (str(database), sid, version)).fetchone` (L245); `db.execute` (L245); `str` (L245); `load_settings` (L247); `Client` (L247); `HermesHistoryConnector` (L248); `sorted` (L248); `sync_history` (L249); `stamp` (L252); `sync_state` (L253); `db.executemany` (L254); `str` (L254)
- **sync_on_read.stamp** (L229): `Path` (L231); `str` (L231); `path.stat` (L233); `values.append` (L233); `values.append` (L234); `digest` (L235)
- **native_evidence** (L258): `isinstance` (L259); `row.get` (L259); `set` (L260); `set` (L260); `sqlite3.connect` (L261); `Path(database).resolve().as_uri` (L261); `Path(database).resolve` (L261); `Path` (L261); `sorted` (L262); `range` (L263); `len` (L263); `sessions.update` (L265); `native.execute` (L265); `','.join` (L265); `sync_state` (L266); `Path` (L266); `db.execute` (L267); `str` (L267); `Path(database).resolve` (L267); `Path` (L267); `digest` (L268); `str` (L268); `sessions.items` (L268); `sorted` (L269); `range` (L270); `len` (L270); `db.execute` (L272); `','.join` (L272); `covered.add` (L273); `refs.add` (L273); `sorted` (L274)

## personal_memory/host_store.py

Hermes MemoryStore-compatible proxy backed by canonical curated-memory APIs.

- **CanonicalMemoryStore.__init__** (L20): No direct calls.
- **CanonicalMemoryStore.target_enabled** (L31): No direct calls.
- **CanonicalMemoryStore.reset_consolidation_failures** (L34): No direct calls.
- **CanonicalMemoryStore._entries_for** (L37): No direct calls.
- **CanonicalMemoryStore._set_entries** (L40): `setattr` (L41)
- **CanonicalMemoryStore._char_limit** (L43): No direct calls.
- **CanonicalMemoryStore._char_count** (L46): `len` (L47); `'\n§\n'.join` (L47); `self._entries_for` (L47)
- **CanonicalMemoryStore._usage** (L49): `self._char_count` (L50); `self._char_limit` (L50)
- **CanonicalMemoryStore._usage_pct** (L52): `self._char_count` (L53); `self._char_limit` (L53); `min` (L54); `int` (L54)
- **CanonicalMemoryStore._render_block** (L56): `'\n§\n'.join` (L60); `self._char_limit` (L61); `min` (L62); `int` (L62); `len` (L62); `len` (L62)
- **CanonicalMemoryStore._sanitize** (L66): `list` (L70); `scan_for_threats` (L73); `result.append` (L74); `', '.join` (L76)
- **CanonicalMemoryStore.load_from_disk** (L79): `self.refresh_live` (L84); `self.provider.client.call` (L86); `self._set_entries` (L90); `self._render_block` (L92); `self._sanitize` (L93); `self.provider.lineage.add` (L95); `entry.get` (L97)
- **CanonicalMemoryStore.refresh_live** (L101): `self.provider.client.call` (L102); `payload['stores'].items` (L103); `self._set_entries` (L104)
- **CanonicalMemoryStore.format_for_system_prompt** (L108): `self._system_prompt_snapshot.get` (L109)
- **CanonicalMemoryStore.fork** (L111): `CanonicalMemoryStore(self.provider, self.memory_char_limit, self.user_char_limit, memory_enabled=self.memory_enabled, user_profile_enabled=self.user_profile_enabled).load_from_disk` (L113); `CanonicalMemoryStore` (L113)
- **CanonicalMemoryStore._scan** (L118): `operation.get` (L124); `operation.get` (L125); `operation.get` (L125); `_scan_memory_content` (L126)
- **CanonicalMemoryStore.apply_batch** (L130): `copy.deepcopy` (L131); `operation.get` (L133); `operation.pop` (L134); `self._scan` (L135); `self.provider.curated_write_evidence` (L138); `digest` (L139); `self.provider.client.call` (L141); `getattr` (L146); `self.refresh_live` (L148); `list` (L151); `self._entries_for` (L151); `self._usage` (L152); `type` (L155); `self._set_entries` (L156); `self.provider._invalidate` (L158); `len` (L160); `self._entries_for` (L160); `self._usage_pct` (L161)
- **CanonicalMemoryStore.add** (L164): `self.apply_batch` (L165)
- **CanonicalMemoryStore.replace** (L167): `self.apply_batch` (L168)
- **CanonicalMemoryStore.remove** (L171): `self.apply_batch` (L172)
- **CanonicalMemoryStore.clear** (L174): `self.apply_batch` (L175)

## personal_memory/importers.py

Offline exports to canonical records. No account scraping or implicit uploads.

- **PlainHTML.__init__** (L20): `super().__init__` (L21); `super` (L21)
- **PlainHTML.handle_starttag** (L22): `self.parts.append` (L24)
- **PlainHTML.handle_endtag** (L25): `max` (L26)
- **PlainHTML.handle_data** (L27): `self.parts.append` (L28)
- **emails** (L31): `Path` (L32); `path.suffix.lower` (L33); `BytesParser(policy=policy.default).parsebytes` (L34); `BytesParser` (L34); `path.read_bytes` (L34); `mailbox.mbox` (L36); `parsedate_to_datetime` (L40); `str` (L40); `message.get` (L40); `ValueError` (L42); `message.get_body` (L43); `body.get_content` (L44); `body.get_content_type` (L45); `PlainHTML` (L46); `parser.feed` (L46); `''.join` (L46); `getaddresses` (L49); `str` (L49); `message.get_all` (L49); `participants.append` (L51); `header.lower` (L51); `str` (L52); `message.get` (L52); `'\n'.join` (L53); `headers.items` (L53); `headers['Message-ID'].strip` (L54); `digest` (L54); `when.isoformat` (L55); `str` (L57); `p.get_filename` (L57); `message.iter_attachments` (L57); `path.suffix.lower` (L60); `box.close` (L60)
- **whatsapp** (L66): `ValueError` (L67); `ValueError` (L68); `ZoneInfo` (L69); `Counter` (L69); `Path(path).open` (L99); `Path` (L99); `line.rstrip` (L101); `raw.replace('\u200e', '').replace('\u200f', '').replace` (L102); `raw.replace('\u200e', '').replace` (L102); `raw.replace` (L102); `HEADER.match` (L103); `convert` (L105); `current[2].append` (L108); `current[3].append` (L108); `preamble.append` (L109); `convert` (L110); `ValueError` (L111)
- **whatsapp.convert** (L71): `int` (L73); `re.split` (L73); `dict` (L74); `zip` (L74); `clock.upper().replace('\u202f', ' ').strip` (L76); `clock.upper().replace` (L76); `clock.upper` (L76); `re.sub` (L77); `clock.count` (L78); `clock.count` (L78); `datetime.strptime(clock, fmt).time` (L79); `datetime.strptime` (L79); `datetime` (L80); `naive.replace` (L81); `local.astimezone(timezone.utc).astimezone(zone).replace` (L82); `local.astimezone(timezone.utc).astimezone` (L82); `local.astimezone` (L82); `ValueError` (L83); `'\n'.join` (L84); `payload.partition` (L84); `re.sub` (L87); `re.fullmatch` (L88); `'\n'.join` (L90); `digest` (L91); `local.isoformat` (L91); `str` (L92); `local.isoformat` (L92); `naive.replace(tzinfo=zone, fold=0).utcoffset` (L96); `naive.replace` (L96); `naive.replace(tzinfo=zone, fold=1).utcoffset` (L96); `naive.replace` (L96)
- **health_csv** (L114): `Path(path).open` (L115); `Path` (L115); `csv.DictReader` (L116); `{'timestamp', 'metric', 'value', 'unit'}.issubset` (L117); `ValueError` (L118); `timestamp` (L120); `Decimal` (L121); `ValueError` (L122); `value.is_finite` (L123); `row['metric'].strip` (L123); `row['unit'].strip` (L123); `ValueError` (L124); `row.get` (L125); `digest` (L125); `row.get` (L127); `str` (L128)
- **jsonl** (L131): `Path(path).open` (L132); `Path` (L132); `line.strip` (L134); `json.loads` (L134)
- **typed_health** (L138): `client.call` (L145); `float` (L146)

Module/class/lambda calls: `emails.<lambda@36> → BytesParser(policy=policy.default).parse` (L36); `emails.<lambda@36> → BytesParser` (L36); `<module> → re.compile` (L63)

## personal_memory/ingestion.py

Versioned, language-neutral ingestion contract and Python connector interface.

- **string** (L20): No direct calls.
- **closed** (L24): `list` (L25)
- **ContractError.__init__** (L51): `super().__init__` (L53); `super` (L53)
- **_json** (L56): `ContractError` (L57); `isinstance` (L58); `value.items` (L59); `isinstance` (L60); `ContractError` (L60); `_json` (L61); `isinstance` (L62); `enumerate` (L63); `_json` (L63); `isinstance` (L64); `math.isfinite` (L64); `ContractError` (L65); `isinstance` (L66); `ContractError` (L67)
- **_validate** (L70): `ContractError` (L72); `ContractError` (L73); `schema.get` (L74); `any` (L77); `types[t]` (L77); `isinstance` (L77); `ContractError` (L78); `str` (L78); `isinstance` (L79); `len` (L80); `schema.get` (L80); `len` (L80); `schema.get` (L80); `ContractError` (L80); `re.search` (L81); `ContractError` (L81); `schema.get` (L82); `re.fullmatch` (L83); `ContractError` (L84); `timestamp` (L85); `ContractError` (L86); `isinstance` (L87); `schema.get` (L88); `ContractError` (L89); `value.items` (L90); `schema.get('properties', {}).get` (L91); `schema.get` (L91); `next` (L93); `schema.get('patternProperties', {}).items` (L93); `schema.get` (L93); `re.fullmatch` (L93); `_validate` (L94); `schema.get` (L95); `ContractError` (L95); `isinstance` (L96); `len` (L97); `schema.get` (L97); `ContractError` (L97); `enumerate` (L98); `_validate` (L99); `schema.get` (L100); `len` (L100); `json.dumps` (L100); `len` (L100); `ContractError` (L100)
- **validate_record** (L103): `_json` (L104); `_validate` (L105); `ContractError` (L107); `len` (L108); `json.dumps(record, ensure_ascii=False).encode` (L108); `json.dumps` (L108); `ContractError` (L109); `copy.deepcopy` (L111); `timestamp` (L112); `timestamp` (L113)
- **validate_batch** (L117): `isinstance` (L118); `len` (L118); `ContractError` (L119); `enumerate` (L121); `records.append` (L122); `validate_record` (L122); `ContractError` (L124)
- **IngestionConnector.spec** (L143): No direct calls.
- **IngestionConnector.read** (L146): No direct calls.
- **IngestionConnector.validate_extra** (L148): No direct calls.
- **IngestionConnector.records** (L151): `self.read` (L152); `validate_record` (L153); `ContractError` (L156); `self.validate_extra` (L157)
- **submit** (L161): `connector.records` (L167); `client.call` (L168)
- **adapt_existing** (L171): `copy.deepcopy` (L177); `record.get` (L177); `metadata.pop` (L179); `participants.append` (L180); `p.get` (L181); `p.get` (L181); `validate_record` (L183); `record.get` (L184); `record.get` (L184); `record.get` (L185)

Module/class/lambda calls: `<module> → closed` (L28); `<module> → string` (L28); `<module> → string` (L28); `<module> → string` (L28); `<module> → string` (L28); `<module> → closed` (L29); `<module> → string` (L29); `<module> → string` (L29); `<module> → string` (L30); `<module> → string` (L31); `<module> → closed` (L32); `<module> → string` (L32); `<module> → closed` (L36); `<module> → string` (L37); `<module> → string` (L37); `<module> → string` (L38); `<module> → string` (L38); `<module> → string` (L41); `_validate.<lambda@75> → isinstance` (L75); `_validate.<lambda@75> → isinstance` (L75); `_validate.<lambda@76> → isinstance` (L76); `ConnectorSpec → dataclass` (L128)

## personal_memory/intelligence.py

Structured personal knowledge and task state, grounded in canonical evidence.

- **bounds** (L27): `timestamp` (L28); `timestamp` (L29); `ValueError` (L30)
- **valid** (L34): `payload.get` (L35); `payload.get` (L35)
- **scalar** (L38): `type` (L39); `type` (L39); `math.isfinite` (L39); `ValueError` (L40); `len` (L41); `document` (L41); `ValueError` (L41)
- **Intelligence.__init__** (L46): `Learning` (L46)
- **Intelligence._quote** (L48): `isinstance` (L50); `len` (L50); `ValueError` (L50); `isinstance` (L53); `set` (L53); `ValueError` (L53); `required_text` (L54); `db.execute('SELECT text FROM records WHERE id=? AND deleted=0', (e['record_id'],)).fetchone` (L55); `db.execute` (L55); `lifecycle.live_and_visible` (L56); `ValueError` (L56); `ids.append` (L57); `sorted` (L58); `set` (L58)
- **Intelligence._entity** (L60): `db.execute('SELECT 1 FROM entities WHERE id=?', (eid,)).fetchone` (L61); `db.execute` (L61); `ValueError` (L61)
- **Intelligence.snapshot** (L63): `strings` (L64); `self.store.connect` (L65); `db.execute` (L66); `digest` (L68); `db.execute('SELECT id FROM learning_objects WHERE id=?', (oid,)).fetchone` (L69); `db.execute` (L69); `self.learning._get` (L71); `sorted` (L72); `sorted` (L72); `ValueError` (L72); `db.execute('SELECT fingerprint FROM records WHERE id=? AND deleted=0', (rid,)).fetchone` (L77); `db.execute` (L77); `lifecycle.live_and_visible` (L78); `ValueError` (L78); `dict` (L80); `db.execute` (L80); `self.learning._put` (L81); `now` (L81)
- **Intelligence.belief** (L83): `ValueError` (L84); `ValueError` (L85); `isinstance` (L86); `any` (L86); `isinstance` (L86); `isinstance` (L86); `(context or {}).items` (L86); `ValueError` (L86); `required_text` (L87); `scalar` (L87); `bounds` (L87); `dict` (L88); `self.store.connect` (L89); `db.execute` (L90); `self._entity` (L90); `self._quote` (L90); `self._coverage` (L92); `now` (L92); `ValueError` (L93); `self.learning._put` (L95); `db.executemany` (L96)
- **Intelligence.resolve_belief** (L99): `strings` (L100); `self.store.connect` (L101); `db.execute` (L102); `self.learning._get` (L102); `ValueError` (L103); `self.learning._get` (L105); `any` (L106); `ValueError` (L106); `db.execute` (L107); `self.store.audit` (L108)
- **Intelligence.beliefs** (L111): `type` (L112); `ValueError` (L112); `timestamp` (L113); `now` (L113); `isinstance` (L114); `ValueError` (L114); `args.append` (L116); `self.store.connect` (L117); `db.execute('SELECT * FROM learning_objects WHERE ' + clause + ' ORDER BY created_at DESC,id LIMIT 1001', args).fetchall` (L118); `db.execute` (L118); `dict` (L119); `json.loads` (L119); `valid` (L120); `all` (L120); `context.get` (L120); `r['payload']['context'].items` (L120); `matched.sort` (L121); `groups.setdefault(r['payload']['predicate'], []).append` (L123); `groups.setdefault` (L123); `groups.items` (L124); `len` (L124); `document` (L124); `len` (L125); `len` (L125)
- **Intelligence.relation** (L128): `required_text` (L129); `bounds` (L129); `self.store.connect` (L130); `db.execute` (L131); `self._entity` (L131); `self._entity` (L131); `self._quote` (L132); `self.learning._put` (L133); `dict` (L133); `db.executemany` (L134)
- **Intelligence.graph** (L137): `type` (L138); `ValueError` (L138); `type` (L139); `ValueError` (L139); `timestamp` (L140); `now` (L140); `set` (L140); `self.store.connect` (L141); `self._entity` (L142); `range` (L143); `set` (L144); `sorted` (L145); `visited.add` (L146); `db.execute("SELECT * FROM learning_objects WHERE kind='relation' AND state='recorded' AND (json_extract(payload,'$.subject_id')=? OR json_extract(payload,'$.object_id')=?) ORDER BY id LIMIT ?", (eid, eid, max_edges + 1)).fetchall` (L147); `db.execute` (L147); `len` (L148); `json.loads` (L150); `valid` (L151); `len` (L152); `dict` (L153); `next_frontier.update` (L153); `list` (L156); `edges.values` (L156)
- **Intelligence.measurement** (L158): `type` (L159); `math.isfinite` (L159); `ValueError` (L159); `required_text` (L160); `required_text` (L160); `timestamp` (L160); `self.store.connect` (L169); `db.execute` (L170); `self._entity` (L170); `db.execute('SELECT * FROM metric_definitions WHERE metric=?', (metric,)).fetchone` (L171); `db.execute` (L171); `json.loads` (L172); `defaults.get` (L172); `ValueError` (L173); `math.isfinite` (L175); `ValueError` (L175); `self.learning._put` (L176); `dict` (L176); `self._quote` (L176)
- **Intelligence.aggregate** (L178): `bounds` (L179); `ValueError` (L180); `self.store.connect` (L181); `db.execute("SELECT count(*),min(json_extract(payload,'$.value')),max(json_extract(payload,'$.value')),avg(json_extract(payload,'$.value'))\n             FROM learning_objects WHERE kind='measurement' AND state='recorded' AND json_extract(payload,'$.subject_id')=?\n             AND json_extract(payload,'$.metric')=? AND json_extract(payload,'$.unit')=?\n             AND json_extract(payload,'$.measured_at')>=? AND json_extract(payload,'$.measured_at')<?", (subject_id, metric, unit, start, end)).fetchone` (L182); `db.execute` (L182); `dict` (L186)
- **Intelligence.task** (L189): `required_text` (L190); `timestamp` (L190); `strings` (L191); `strings` (L191); `self.store.connect` (L192); `db.execute` (L193); `self.learning._get` (L194); `self.learning._put` (L195); `dict` (L195); `self.store.deletions.contains` (L196); `self.store.deletions.contains` (L196); `db.execute` (L197); `now` (L197); `db.executemany` (L198); `dict` (L199); `db.execute('SELECT * FROM task_runtime WHERE object_id=?', (obj['id'],)).fetchone` (L199); `db.execute` (L199)
- **Intelligence.transition** (L201): `strings` (L203); `self.store.connect` (L204); `db.execute` (L205); `self.learning._get` (L205); `db.execute('SELECT * FROM task_runtime WHERE object_id=?', (task_id,)).fetchone` (L206); `db.execute` (L206); `type` (L207); `ValueError` (L207); `transitions.get` (L208); `set` (L208); `ValueError` (L208); `db.execute("SELECT 1 FROM task_dependencies d JOIN task_runtime t ON t.object_id=d.dependency_id JOIN learning_objects o ON o.id=t.object_id WHERE d.task_id=? AND (t.state!='completed' OR o.state!='recorded')", (task_id,)).fetchone` (L210); `db.execute` (L210); `ValueError` (L211); `lifecycle.live_and_visible` (L214); `ValueError` (L214); `db.executemany` (L215); `self.store.deletions.append` (L216); `db.execute` (L217); `now` (L217); `db.execute` (L218); `self.store.audit` (L219); `dict` (L220); `db.execute('SELECT * FROM task_runtime WHERE object_id=?', (task_id,)).fetchone` (L220); `db.execute` (L220)
- **Intelligence.tasks** (L222): `type` (L223); `ValueError` (L223); `args.append` (L225); `self.store.connect` (L226); `db.execute('SELECT o.id,o.payload,t.version,t.state,t.due_at FROM learning_objects o JOIN task_runtime t ON t.object_id=o.id WHERE ' + clause + ' ORDER BY o.id LIMIT ?', args + [limit + 1]).fetchall` (L227); `db.execute` (L227); `dict` (L228); `json.loads` (L228); `len` (L228)
- **Intelligence.due_events** (L230): `type` (L232); `ValueError` (L232); `self.store.connect` (L233); `db.execute` (L234); `db.execute("SELECT t.* FROM task_runtime t JOIN learning_objects o ON o.id=t.object_id WHERE o.state='recorded' AND t.state IN ('open','in_progress') AND t.due_at<=? AND NOT EXISTS(SELECT 1 FROM task_events e WHERE e.task_id=t.object_id AND e.version=t.version) ORDER BY t.due_at LIMIT ?", (now(), limit)).fetchall` (L235); `db.execute` (L235); `now` (L235); `digest` (L237); `db.execute` (L238); `now` (L238); `len` (L239)
- **Intelligence.feedback** (L241): `ValueError` (L242); `required_text` (L243); `self.store.connect` (L244); `db.execute` (L245); `db.execute('SELECT 1 FROM records WHERE id=? AND deleted=0', (record_id,)).fetchone` (L246); `db.execute` (L246); `ValueError` (L246); `db.execute('SELECT record_id,result FROM memory_feedback WHERE key=?', (key,)).fetchone` (L247); `db.execute` (L247); `tuple` (L248); `ValueError` (L248); `db.execute` (L249); `now` (L249)
- **Intelligence.quality** (L252): `self.store.connect` (L253); `dict` (L254); `db.execute` (L254); `dict` (L255); `db.execute` (L255); `db.execute("SELECT count(*) FROM task_runtime t JOIN learning_objects o ON o.id=t.object_id WHERE o.state='recorded' AND t.state NOT IN ('completed','cancelled')").fetchone` (L256); `db.execute` (L256)
- **Intelligence.claim_event** (L259): `self.due_events` (L261); `self.store.connect` (L262); `db.execute` (L263); `time.time` (L263); `db.execute` (L264); `db.execute("SELECT e.* FROM task_events e JOIN task_runtime t ON t.object_id=e.task_id JOIN learning_objects o ON o.id=e.task_id\n                WHERE o.state='recorded' AND t.state IN ('open','in_progress') AND t.version=e.version\n                AND (e.state='pending' OR (e.state='leased' AND e.lease_until<?)) ORDER BY e.created_at,e.id LIMIT 1", (clock,)).fetchone` (L265); `db.execute` (L265); `uuid.uuid4` (L269); `db.execute` (L270); `self.learning._get` (L271)
- **Intelligence.ack_event** (L275): `self.store.connect` (L276); `db.execute` (L277); `db.execute('SELECT * FROM task_events WHERE id=?', (event_id,)).fetchone` (L278); `db.execute` (L278); `ValueError` (L279); `self.learning._get` (L280); `db.execute('SELECT * FROM task_runtime WHERE object_id=?', (row['task_id'],)).fetchone` (L281); `db.execute` (L281); `ValueError` (L282); `ValueError` (L283); `db.execute` (L284); `self.store.audit` (L285)
- **Intelligence.domain** (L288): `required_text` (L289); `required_text` (L289); `strings` (L289); `self.store.connect` (L290); `db.execute` (L291); `db.execute` (L292); `document` (L292); `self.store.audit` (L293)
- **Intelligence._coverage** (L296): `db.execute('SELECT * FROM coverage_domains WHERE domain=?', (domain,)).fetchone` (L297); `db.execute` (L297); `json.loads` (L300); `db.execute('SELECT source,state,through_at FROM sources WHERE source=?', (source,)).fetchone` (L301); `db.execute` (L301); `statuses.append` (L302); `dict` (L302); `all` (L303)
- **Intelligence.coverage** (L307): `timestamp` (L308); `now` (L308); `self.store.connect` (L309); `self._coverage` (L309)
- **Intelligence.procedures** (L311): `type` (L312); `ValueError` (L312); `self.store.connect` (L313); `db.execute("SELECT p.* FROM learning_objects p JOIN learning_objects c ON c.id=json_extract(p.payload,'$.candidate_id')\n             WHERE p.kind='procedure' AND p.state='recorded' AND c.state='active' AND p.id>? ORDER BY p.id LIMIT ?", (after, limit + 1)).fetchall` (L314); `db.execute` (L314); `dict` (L316); `json.loads` (L316); `len` (L316)
- **Intelligence.retry_event** (L319): `self.store.connect` (L320); `db.execute` (L321); `db.execute('SELECT * FROM task_events WHERE id=?', (event_id,)).fetchone` (L322); `db.execute` (L322); `ValueError` (L323); `self.learning._get` (L324); `db.execute('SELECT version,state FROM task_runtime WHERE object_id=?', (row['task_id'],)).fetchone` (L325); `db.execute` (L325); `ValueError` (L326); `db.execute` (L327); `ValueError` (L328); `self.store.audit` (L329)
- **Intelligence.summaries** (L332): `required_text` (L333); `type` (L334); `ValueError` (L334); `self.store.query_terms` (L335); `self.store.connect` (L337); `db.execute("SELECT o.* FROM knowledge_fts f JOIN learning_objects o ON o.id=f.id WHERE knowledge_fts MATCH ? AND o.kind='consolidation' AND o.state='recorded' ORDER BY rank LIMIT ?", (terms, limit)).fetchall` (L338); `db.execute` (L338); `dict` (L339); `json.loads` (L339)
- **Intelligence.metric** (L342): `required_text` (L343); `required_text` (L343); `isinstance` (L344); `len` (L344); `ValueError` (L344); `conversions.items` (L345); `required_text` (L346); `isinstance` (L347); `len` (L347); `any` (L347); `type` (L347); `math.isfinite` (L347); `ValueError` (L347); `conversions.get` (L348); `ValueError` (L348); `document` (L349); `self.store.connect` (L350); `db.execute` (L351); `db.execute('SELECT canonical_unit,conversions FROM metric_definitions WHERE metric=?', (metric,)).fetchone` (L351); `db.execute` (L351); `tuple` (L352); `ValueError` (L352); `db.execute("SELECT 1 FROM learning_objects WHERE kind='measurement' AND json_extract(payload,'$.metric')=? LIMIT 1", (metric,)).fetchone` (L353); `db.execute` (L353); `ValueError` (L353); `db.execute` (L354); `self.store.audit` (L355)

Module/class/lambda calls: `Intelligence.beliefs.<lambda@121> → len` (L121)

## personal_memory/investigate.py

Hermes-authored parallel retrieval plans. Evidence leads, never answer verification.

- **Investigation.__init__** (L11): `ThreadPoolExecutor` (L13); `threading.BoundedSemaphore` (L14)
- **Investigation.close** (L16): `self.pool.shutdown` (L17)
- **Investigation.search** (L19): `required_text` (L20); `isinstance` (L21); `len` (L21); `ValueError` (L21); `type` (L23); `ValueError` (L23); `isinstance` (L25); `len` (L25); `any` (L26); `isinstance` (L26); `ValueError` (L27); `set` (L28); `isinstance` (L30); `set` (L30); `ValueError` (L30); `required_text` (L31); `branch.get` (L31); `required_text` (L31); `branch.get` (L31); `ValueError` (L32); `seen.add` (L33); `branch.get` (L33); `isinstance` (L34); `len` (L34); `ValueError` (L34); `list` (L35); `dict.fromkeys` (L35); `required_text` (L35); `branch.get` (L36); `isinstance` (L37); `set` (L37); `ValueError` (L37); `dict` (L38); `filters.items` (L39); `timestamp` (L39); `required_text` (L39); `filters.get` (L40); `filters.get` (L40); `ValueError` (L40); `dict` (L41); `json.dumps` (L43); `parsed.append` (L44); `dict` (L44); `self.capacity.acquire` (L49); `range` (L50); `self.capacity.release` (L50); `ValueError` (L51); `time.monotonic` (L53); `jobs.items` (L55); `self.pool.submit` (L56); `future.add_done_callback` (L58); `range` (L60); `len` (L60); `self.capacity.release` (L60); `futures.values` (L61); `f.cancel` (L61); `wait` (L63); `futures.values` (L63); `futures.items` (L65); `f.cancel` (L66); `f.result` (L67); `type` (L68); `self.store.connect` (L72); `b['filters'].get` (L73); `self.store.related_ids` (L73); `list` (L74); `results.get(b['key'], {}).get` (L74); `results.get` (L74); `any` (L75); `queues.values` (L75); `len` (L75); `len` (L78); `queue.pop` (L79); `lifecycle.live_and_visible` (L82); `db.execute('SELECT id,source,source_id,occurred_at,text FROM records WHERE id=?', (rid,)).fetchone` (L83); `db.execute` (L83); `f.get` (L87); `f.get` (L88); `f.get` (L89); `terms` (L93); `bool` (L94); `terms` (L94); `any` (L95); `bool` (L95); `terms` (L95); `len` (L95); `terms` (L95); `min` (L95); `len` (L95); `terms` (L95); `hit.get` (L96); `relevance.get` (L97); `isinstance` (L98); `relevance.get` (L98); `aliases.get` (L103); `item['duplicate_record_ids'].append` (L106); `max` (L108); `min` (L108); `hit.get` (L108); `len` (L108); `min` (L109); `len` (L109); `dict` (L111); `len` (L111); `hit.get` (L112); `episodes.append` (L113); `item['branch_ids'].append` (L114); `branch_ids[b['id']].append` (L115); `set` (L119); `any` (L121); `db.execute` (L122); `seeds.add` (L122); `set` (L123); `len` (L123); `sorted` (L124); `self.intelligence.graph` (L125); `all` (L130); `lifecycle.live_and_visible` (L130); `payload.get` (L132); `payload.get` (L132); `len` (L133); `json.dumps` (L133); `connections.append` (L137); `edge_ids.add` (L137); `results.get` (L139); `errors.get` (L139); `bool` (L140); `result.get('diagnostics', {}).get` (L140); `result.get` (L140); `result.get` (L140); `requirements.append` (L141); `len` (L143); `len` (L146); `len` (L146); `round` (L146); `time.monotonic` (L146); `sum` (L147); `errors.values` (L147)

Module/class/lambda calls: `Investigation.search.<lambda@58> → self.capacity.release` (L58)

## personal_memory/learning.py

Evidence-backed learning artifacts. Proposals never activate themselves.

- **strings** (L26): `isinstance` (L27); `len` (L27); `ValueError` (L28); `required_text` (L29); `len` (L30); `set` (L30); `len` (L30); `ValueError` (L30)
- **document** (L34): `json.dumps` (L35); `len` (L36); `raw.encode` (L36); `ValueError` (L36)
- **Learning.__init__** (L41): No direct calls.
- **Learning._get** (L43): `db.execute('SELECT * FROM learning_objects WHERE id=?', (object_id,)).fetchone` (L44); `db.execute` (L44); `ValueError` (L45); `ValueError` (L46); `dict` (L47); `json.loads` (L47)
- **Learning._put** (L49): `required_text` (L50); `document` (L50); `digest` (L52); `self.store.deletions.contains` (L53); `ValueError` (L53); `db.execute('SELECT * FROM learning_objects WHERE id=?', (oid,)).fetchone` (L54); `db.execute` (L54); `ValueError` (L57); `self._get` (L58); `set` (L59); `strings` (L59); `self._get` (L61); `refs.update` (L62); `db.execute` (L62); `ValueError` (L63); `lifecycle.require_live_evidence` (L65); `db.execute` (L66); `now` (L67); `db.executemany` (L68); `sorted` (L68); `db.executemany` (L69); `self.store.audit` (L70); `self._get` (L71)
- **Learning.outcome** (L73): `ValueError` (L74); `required_text` (L75); `{'goal': goal, 'action': action, 'result': result}.items` (L75); `payload.update` (L76); `strings` (L76); `strings` (L77); `self.store.connect` (L78); `db.execute` (L79); `self._put` (L80); `list` (L80); `set` (L80); `set` (L80)
- **Learning.propose** (L82): `ValueError` (L83); `type` (L84); `ValueError` (L84); `required_text` (L85); `{'family': family, 'lesson': lesson, 'scope': scope}.items` (L85); `payload.update` (L86); `strings` (L86); `strings` (L87); `strings` (L87); `strings` (L88); `self.store.connect` (L89); `db.execute` (L90); `self._get` (L91); `db.execute` (L93); `json.loads` (L94); `previous.get` (L95); `previous.get` (L95); `digest` (L95); `ValueError` (L96); `self._put` (L97)
- **Learning.evaluate** (L99): `isinstance` (L100); `len` (L100); `ValueError` (L100); `set` (L101); `isinstance` (L103); `set` (L103); `ValueError` (L103); `required_text` (L104); `ValueError` (L105); `seen.add` (L106); `type` (L107); `type` (L107); `ValueError` (L107); `ValueError` (L108); `ValueError` (L109); `self.store.connect` (L110); `db.execute` (L111); `self._get` (L111); `ValueError` (L112); `ValueError` (L113); `all` (L114); `digest` (L115); `strings` (L116); `self._put` (L118)
- **Learning.promote** (L120): `self.store.connect` (L121); `db.execute` (L122); `self._get` (L122); `self._get` (L122); `ValueError` (L123); `db.execute("SELECT metadata FROM audit WHERE action='learning_promote' AND object_id=? ORDER BY id DESC LIMIT 1", (candidate_id,)).fetchone` (L125); `db.execute` (L125); `json.loads` (L126); `ValueError` (L128); `result.get` (L130); `result.get` (L130); `ValueError` (L130); `digest` (L131); `ValueError` (L132); `self._get` (L134); `db.execute` (L134); `next` (L135); `ValueError` (L136); `ValueError` (L137); `db.execute` (L138); `db.execute` (L139); `db.execute` (L141); `db.execute` (L142); `self.store.audit` (L143); `self._get` (L144)
- **Learning.retract** (L146): `self.store.connect` (L147); `db.execute` (L148); `self._get` (L148); `self.store.deletions.append` (L149); `invalidate` (L150); `self.store.audit` (L151)
- **Learning.active** (L154): `self.store.connect` (L155); `self._get` (L156); `ValueError` (L157)
- **Learning.browse** (L160): `ValueError` (L161); `ValueError` (L162); `type` (L163); `ValueError` (L163); `required_text` (L164); `self.store.connect` (L165); `db.execute` (L166); `self._get` (L167); `len` (L167)
- **invalidate** (L171): `list` (L172); `seeds.extend` (L173); `db.execute` (L173); `db.execute('WITH RECURSIVE affected(id) AS (\n            SELECT ? UNION SELECT d.child_id FROM learning_dependencies d JOIN affected a ON a.id=d.parent_id)\n            SELECT id FROM affected', (oid,)).fetchall` (L175); `db.execute` (L175); `db.executemany` (L178); `db.executemany` (L179)

## personal_memory/lifecycle.py

Unified lifecycle retirement shared by canonical and derived memory.

- **_descendants** (L26): `db.execute` (L27)
- **live_and_visible** (L32): `bool` (L34); `db.execute('SELECT 1 FROM records WHERE id=? AND deleted=0 AND NOT EXISTS(SELECT 1 FROM record_visibility WHERE record_id=? AND hidden=1)', (record_id, record_id)).fetchone` (L34); `db.execute` (L34)
- **require_live_evidence** (L40): `live_and_visible` (L42); `ValueError` (L43)
- **retire** (L46): `_descendants` (L57); `_descendants` (L59); `awareness.invalidate_record` (L61); `db.execute` (L62); `invalidate_learning` (L63); `invalidate_curated` (L64); `db.execute` (L67); `db.execute` (L71); `semantic.enqueue` (L76); `sorted` (L76)
- **repair_retired_dependencies** (L80): `store.connect` (L87); `db.execute` (L88); `_repair` (L89)
- **apply_upgrade** (L92): `store.connect` (L101); `db.execute` (L102); `db.execute('SELECT 1 FROM memory_migrations WHERE name=? AND version=?', (name, version)).fetchone` (L105); `db.execute` (L105); `db.execute` (L107); `_repair` (L108); `db.execute` (L109); `now` (L109)
- **_repair** (L114): `db.execute` (L119); `db.execute` (L125); `db.execute` (L129); `db.execute` (L136); `db.execute` (L143); `set` (L147); `db.execute` (L149); `learning_seeds.add` (L152); `invalidate_learning` (L154); `sorted` (L154); `len` (L155); `set` (L156); `db.execute('SELECT 1 FROM curated_entries WHERE retired_version IS NULL AND EXISTS(SELECT 1 FROM json_each(curated_entries.evidence_ids) WHERE value=?)', (rid,)).fetchone` (L158); `db.execute` (L158); `offending.add` (L162); `sorted` (L163); `invalidate_curated` (L164); `store.audit` (L167); `dict` (L167)

## personal_memory/lineage.py

Persistent exposure ledger. IDs only; never a second copy of evidence text.

- **evidence_ids** (L9): `set` (L10); `walk` (L26)
- **evidence_ids.walk** (L11): `isinstance` (L12); `node.items` (L13); `isinstance` (L14); `found.add` (L15); `isinstance` (L16); `found.update` (L17); `isinstance` (L17); `isinstance` (L18); `found.update` (L19); `isinstance` (L19); `isinstance` (L19); `r.get` (L19); `walk` (L20); `all` (L21); `isinstance` (L21); `found.add` (L22); `isinstance` (L23); `walk` (L25)
- **ExposureLedger.__init__** (L31): `outbox.connect` (L33); `db.execute` (L34); `db.execute` (L35); `db.execute` (L36)
- **ExposureLedger.add** (L38): `evidence_ids` (L39); `self.outbox.connect` (L40); `db.executemany` (L41)
- **ExposureLedger.exposed** (L43): `self.outbox.connect` (L49); `db.execute('SELECT EXISTS(SELECT 1 FROM exposures WHERE session_id=?)', (session_id,)).fetchone` (L50); `db.execute` (L50)
- **ExposureLedger.block** (L53): `self.outbox.connect` (L54); `db.execute` (L55)
- **ExposureLedger.inherit** (L57): `self.outbox.connect` (L58); `db.execute` (L59); `db.execute` (L61)
- **ExposureLedger.bind** (L64): `self.outbox.connect` (L68); `db.execute` (L69); `json.dumps` (L69); `json.loads` (L70); `db.execute('SELECT parents FROM capture_dependencies WHERE capture_id=?', (capture_id,)).fetchone` (L70); `db.execute` (L70)
- **ExposureLedger.parents** (L72): `self.outbox.connect` (L73); `db.execute('SELECT reason FROM untracked_exposure WHERE session_id=?', (session_id,)).fetchone` (L74); `db.execute` (L74); `db.execute` (L75); `ValueError` (L77); `len` (L78); `ValueError` (L79)
- **ExposureLedger.compact_parents** (L82): `self.outbox.connect` (L90); `db.execute('SELECT reason FROM untracked_exposure WHERE session_id=?', (session_id,)).fetchone` (L91); `db.execute` (L91); `db.execute` (L92); `ValueError` (L93); `len` (L94); `range` (L96); `len` (L96); `adapt_existing` (L98); `digest` (L98); `digest` (L101); `now` (L101); `item['provenance'].update` (L102); `self.outbox.client.call` (L104); `ValueError` (L106); `next_level.append` (L107)

## personal_memory/native_catalog.py

Read model for source-backed native state; never scheduler/execution authority.

- **initialize** (L6): `store.connect` (L7); `db.execute` (L8)
- **observe** (L11): `record.get('extensions', {}).get('personal_memory.legacy', {}).get` (L12); `record.get('extensions', {}).get` (L12); `record.get` (L12); `metadata.get` (L13); `metadata.get` (L13); `required_text` (L15); `metadata.get` (L17); `store.connect` (L18); `metadata.get` (L19); `db.execute('SELECT 1 FROM record_dependencies WHERE child_id=? AND parent_id=?', (receipt['id'], record_id)).fetchone` (L21); `db.execute` (L21); `ValueError` (L22); `db.execute('SELECT r.rowid FROM native_state_heads h JOIN records r ON r.id=h.record_id WHERE h.kind=? AND h.object_key=?', (kind, key)).fetchone` (L23); `db.execute` (L23); `db.execute('SELECT rowid FROM records WHERE id=?', (record_id,)).fetchone` (L24); `db.execute` (L24); `db.execute` (L26)
- **read** (L29): `ValueError` (L30); `isinstance` (L31); `type` (L31); `ValueError` (L31); `store.connect` (L32); `db.execute("SELECT h.kind,h.object_key,h.record_id,(r.deleted OR EXISTS(SELECT 1 FROM record_visibility v WHERE v.record_id=r.id AND v.hidden=1)),r.text FROM native_state_heads h JOIN records r ON r.id=h.record_id WHERE h.kind||'/'||h.object_key>? AND (? IS NULL OR h.kind=?) ORDER BY h.kind,h.object_key LIMIT ?", (after, kind, kind, limit + 1)).fetchall` (L33); `db.execute` (L33); `json.loads` (L36); `output.append` (L38); `len` (L41)

## personal_memory/native_files.py

Profile-contained native file snapshots with chunk lineage and retraction.

- **_sync_files** (L10): `Path(home).resolve` (L11); `Path` (L11); `set` (L12); `ValueError` (L12); `isinstance` (L13); `all` (L13); `isinstance` (L13); `ValueError` (L13); `len` (L14); `ValueError` (L14); `files.update` (L17); `root.exists` (L19); `root.rglob` (L20); `str` (L20); `path.parent.relative_to` (L20); `sync_state` (L21); `db.execute` (L22); `db.execute` (L23); `db.execute` (L24); `db.execute` (L24); `db.execute` (L25); `set` (L28); `set` (L28); `Path` (L28); `len` (L29); `ValueError` (L29); `sorted` (L30); `set` (L30); `set` (L30); `selected` (L30); `retire` (L40); `files.get` (L42); `old.get` (L42); `path.exists` (L43); `sync_state` (L45); `db.execute` (L46); `db.execute` (L47); `path.resolve` (L50); `real.is_relative_to` (L51); `path.is_symlink` (L51); `ValueError` (L51); `path.stat` (L52); `ValueError` (L53); `path.read_bytes` (L54); `path.stat` (L54); `RuntimeError` (L55); `raw.decode` (L56); `hashlib.sha256(raw).hexdigest` (L56); `hashlib.sha256` (L56); `digest` (L57); `client.call` (L59); `report['files'].append` (L62); `digest` (L63); `range` (L64); `len` (L64); `digest` (L65); `str` (L65); `range` (L65); `len` (L65); `str` (L66); `path.relative_to` (L66); `adapt_existing` (L67); `json.dumps` (L68); `path.as_uri` (L69); `now` (L69); `item['provenance'].update` (L70); `list` (L70); `client.call` (L71); `enumerate` (L73); `adapt_existing` (L74); `str` (L74); `str` (L75); `path.relative_to` (L75); `path.as_uri` (L76); `str` (L76); `now` (L76); `part['provenance'].update` (L77); `client.call` (L78); `dict` (L79); `json.loads` (L80); `json.dumps` (L80); `client.call` (L83); `sync_state` (L84); `db.execute` (L85); `db.execute` (L86); `report['files'].append` (L87); `len` (L87); `retire` (L88)
- **_sync_files.selected** (L26): `Path` (L26)
- **_sync_files.retire** (L32): `sync_state` (L33); `list` (L33); `db.execute` (L33); `client.call` (L35); `client.call` (L36); `ValueError` (L37); `sync_state` (L38); `db.execute` (L38)
- **sync_files** (L93): `closing` (L96); `ProcessLease` (L96); `Path` (L96); `_sync_files` (L97)

## personal_memory/native_history.py

Read-only Hermes history adapter and resumable administrative reconciliation.

- **HermesHistoryConnector.__init__** (L22): `Path(database).expanduser().resolve` (L23); `Path(database).expanduser` (L23); `Path` (L23); `required_text` (L24); `isinstance` (L25); `ValueError` (L26); `sorted` (L27); `required_text` (L27); `isinstance` (L29); `ValueError` (L30); `self.parents_by_message.items` (L31); `isinstance` (L32); `key.isdecimal` (L32); `isinstance` (L32); `len` (L32); `ValueError` (L33); `required_text` (L34)
- **HermesHistoryConnector.read** (L38): `ValueError` (L40); `tempfile.TemporaryDirectory` (L44); `Path` (L45); `sqlite3.connect` (L46); `self.database.as_uri` (L46); `sqlite3.connect` (L47); `time.monotonic` (L49); `source.backup` (L53); `source.close` (L55); `destination.close` (L56); `snapshot.chmod` (L57); `sqlite3.connect` (L58); `snapshot.as_uri` (L58); `db.execute` (L61); `ValueError` (L64); `db.execute('SELECT id,session_id,role,content,timestamp,active,compacted,_compressed_summary FROM messages WHERE session_id=? AND id>? ORDER BY id LIMIT 200', (sid, cursor)).fetchall` (L68); `db.execute` (L68); `digest` (L76); `self.on_present` (L77); `self.parents_by_message.get` (L78); `str` (L78); `content.strip` (L83); `datetime.fromtimestamp(float(row['timestamp']), timezone.utc).isoformat` (L86); `datetime.fromtimestamp` (L86); `float` (L86); `str` (L87); `digest` (L90); `sorted` (L90); `set` (L90); `now` (L94); `sorted` (L98); `set` (L98); `db.close` (L106)
- **HermesHistoryConnector.read.progress** (L50): `time.monotonic` (L51); `TimeoutError` (L52)
- **sync_state** (L110): `Path` (L111); `path.parent.mkdir` (L112); `sqlite3.connect` (L113); `path.chmod` (L114); `db.execute` (L116); `db.execute` (L119); `db.execute` (L121); `db.execute` (L122); `db.execute` (L123); `db.close` (L126); `path.chmod` (L127)
- **sync_history** (L130): `uuid.uuid4` (L137); `closing` (L158); `ProcessLease` (L158); `Path(state_path).with_name` (L158); `Path` (L158); `sync_state` (L159); `db.execute('SELECT database_path FROM native_history_archives WHERE archive_id=?', (connector.archive_id,)).fetchone` (L160); `db.execute` (L160); `str` (L161); `ValueError` (L162); `db.execute` (L163); `str` (L163); `retire_pending` (L164); `bounded_batches` (L193); `connector.records` (L193); `digest` (L194); `sync_state` (L201); `db.execute('SELECT record_id,source_id FROM native_history_fingerprints WHERE fingerprint=?', (fingerprint,)).fetchone` (L202); `db.execute` (L202); `dict` (L203); `aliases.get` (L204); `sorted` (L209); `set` (L209); `len` (L210); `ValueError` (L210); `item['provenance'].update` (L211); `digest` (L213); `mark_present` (L214); `digest` (L217); `sync_state` (L219); `db.execute` (L220); `','.join` (L221); `digest` (L222); `sorted` (L224); `set` (L224); `aliases.values` (L224); `range` (L225); `len` (L225); `families.update` (L226); `client.call` (L226); `record_states` (L227); `list` (L227); `ids.values` (L227); `list` (L227); `previous.values` (L227); `set` (L229); `any` (L234); `ValueError` (L235); `any` (L237); `client.call` (L238); `previous.get` (L241); `unchanged.append` (L242); `pending.append` (L245); `accepted_ids.add` (L246); `client.call` (L247); `len` (L248); `len` (L248); `RuntimeError` (L248); `zip` (L249); `RuntimeError` (L250); `int` (L251); `sync_state` (L252); `db.execute` (L257); `db.execute` (L259); `db.execute` (L260); `len` (L262); `len` (L262); `retire_pending` (L263); `flush_seen` (L264); `sync_state` (L269); `db.execute('SELECT source_id FROM native_history_map WHERE archive_id=? AND session_id=? AND seen_run!=? LIMIT 100', (connector.archive_id, sid, run_id)).fetchall` (L270); `db.execute` (L270); `client.call` (L274); `sync_state` (L275); `db.execute` (L276); `report.update` (L278)
- **sync_history.retire_pending** (L141): `sync_state` (L143); `db.execute('SELECT record_id FROM native_history_retirements LIMIT 100').fetchall` (L144); `db.execute` (L144); `client.call` (L148); `client.call` (L149); `ValueError` (L150); `sync_state` (L151); `db.execute` (L152)
- **sync_history.flush_seen** (L166): `sync_state` (L168); `db.executemany` (L169); `seen.clear` (L170)
- **sync_history.mark_present** (L171): `seen.append` (L172); `len` (L173); `flush_seen` (L173)
- **sync_history.bounded_batches** (L176): `len` (L179); `json.dumps(item, ensure_ascii=False).encode` (L179); `json.dumps` (L179); `len` (L180); `batch.append` (L183)
- **sync_history.record_states** (L187): `sorted` (L188); `set` (L188); `range` (L189); `len` (L189); `states.update` (L190); `client.call` (L190)

Module/class/lambda calls: `HermesHistoryConnector → ConnectorSpec` (L20)

## personal_memory/operations.py

Read-only deployment diagnostics. Readiness does not mean personal-data completeness.

- **_major** (L21): `int` (L22); `str(version).split('+', 1)[0].split` (L22); `str(version).split` (L22); `str` (L22)
- **doctor** (L26): `Path(home).expanduser` (L27); `Path` (L27); `load_settings` (L29); `add` (L31); `path.stat` (L31); `public.exists` (L33); `add` (L33); `public.stat` (L33); `yaml.safe_load` (L38); `(plugin / 'plugin.yaml').read_text` (L38); `add` (L39); `str` (L39); `manifest.get` (L39); `add` (L40); `(plugin / 'personal_memory/investigate.py').is_file` (L40); `add` (L41); `type` (L41); `json.loads` (L44); `(home / 'personal-memory/host-runtime.json').read_text` (L44); `json.loads` (L45); `(Path(__file__).parent / 'host_contract.json').read_text` (L45); `Path` (L45); `Path(runtime['root']).resolve` (L46); `Path` (L46); `all` (L47); `hashlib.sha256((root / name).read_bytes()).hexdigest` (L47); `hashlib.sha256` (L47); `(root / name).read_bytes` (L47); `contract['files'].items` (L47); `all` (L47); `hashlib.sha256((root / name).read_bytes()).hexdigest` (L47); `hashlib.sha256` (L47); `(root / name).read_bytes` (L47); `contract['anchors'].items` (L47); `add` (L48); `add` (L49); `Path(cfg['data_dir']).expanduser().resolve` (L50); `Path(cfg['data_dir']).expanduser` (L50); `Path` (L50); `add` (L51); `data.is_relative_to` (L51); `home.resolve` (L51); `data.is_relative_to` (L51); `Path.home().resolve` (L51); `Path.home` (L51); `add` (L53); `importlib.util.find_spec` (L53); `TESTED_EMBEDDING_STACK.items` (L55); `importlib.metadata.version` (L56); `_major` (L58); `_major` (L58); `drift.append` (L58); `add` (L59); `', '.join` (L61); `Path` (L62); `inspect_database` (L64); `add` (L64); `add` (L65); `type` (L65); `diagnose` (L69); `Store` (L69); `add` (L69); `add` (L70); `type` (L70); `outbox.exists` (L72); `closing` (L75); `sqlite3.connect` (L75); `outbox.resolve().as_uri` (L75); `outbox.resolve` (L75); `db.execute("SELECT 1 FROM sqlite_master WHERE name='dead_letters'").fetchone` (L76); `db.execute` (L76); `db.execute('SELECT count(*) FROM dead_letters').fetchone` (L77); `db.execute` (L77); `db.execute("SELECT 1 FROM sqlite_master WHERE name='native_history_retirements'").fetchone` (L78); `db.execute` (L78); `db.execute('SELECT count(*) FROM native_history_retirements').fetchone` (L79); `db.execute` (L79); `add` (L80); `add` (L81); `Client` (L83); `client.call` (L85); `add` (L85); `client.call` (L86); `add` (L87); `status.get` (L87); `status.get` (L88); `add` (L89); `hindsight.get` (L89); `status.get('initialization_errors', {}).get` (L89); `status.get` (L89); `add` (L91); `status.get('investigation_contract', {}).get` (L91); `status.get` (L91); `status.get` (L91); `add` (L92); `type` (L92); `all` (L93)
- **doctor.add** (L30): `checks.append` (L30); `bool` (L30)

## personal_memory/outbox.py

Commit locally before network delivery; retries are idempotent at the server.

- **Outbox.__init__** (L15): `Path` (L16); `self.path.parent.mkdir` (L17); `threading.Event` (L19); `threading.Event` (L20); `threading.Lock` (L21); `self.connect` (L22); `db.execute` (L23); `db.execute` (L24); `db.execute` (L25); `db.execute` (L26); `db.execute` (L28); `db.execute` (L29); `db.execute` (L30); `db.execute` (L31); `db.execute` (L32); `db.execute` (L33); `db.execute` (L36); `db.execute` (L39); `db.execute` (L42); `self.path.chmod` (L45); `threading.Thread` (L46); `self.thread.start` (L47)
- **Outbox.connect** (L50): `sqlite3.connect` (L51); `db.execute` (L52); `db.close` (L57)
- **Outbox.enqueue** (L59): `adapt_existing` (L61); `now` (L62); `validate_batch` (L63); `digest` (L66); `i.get` (L66); `self._fingerprint` (L67); `digest` (L68); `self.connect` (L69); `db.execute('SELECT 1 FROM dead_letters WHERE id=?', (key,)).fetchone` (L70); `db.execute` (L70); `ValueError` (L71); `db.execute('SELECT fingerprint FROM ' + table + ' WHERE id=?', (key,)).fetchone` (L73); `db.execute` (L73); `ValueError` (L75); `now` (L76); `db.execute` (L78); `json.dumps` (L79); `db.execute` (L81); `json.dumps` (L82); `getattr` (L82); `now` (L82); `now` (L83); `db.execute` (L84); `json.dumps` (L85); `self.wake.set` (L86); `debug_enabled` (L87); `LOG.debug` (L87); `len` (L87)
- **Outbox._fingerprint** (L91): `digest` (L92); `item.items` (L92)
- **Outbox.health** (L96): `self.connect` (L97); `db.execute('SELECT count(*) FROM pending').fetchone` (L98); `db.execute` (L98); `db.execute('SELECT count(*) FROM dead_letters').fetchone` (L99); `db.execute` (L99); `self.thread.is_alive` (L100); `db.execute('SELECT min(created_at) FROM pending').fetchone` (L101); `db.execute` (L101)
- **Outbox.mark_receipt** (L103): `ValueError` (L104); `self.connect` (L105); `db.execute('SELECT record_ids,created_at FROM observation_receipts WHERE id=?', (key,)).fetchone` (L106); `db.execute` (L106); `now` (L107); `json.dumps` (L107); `json.loads` (L107); `db.execute` (L108)
- **Outbox.receipts** (L111): `self.connect` (L112); `db.execute('SELECT id,state,reason,record_ids,created_at,updated_at FROM observation_receipts ORDER BY updated_at DESC LIMIT ?', (limit,)).fetchall` (L113); `db.execute` (L113); `json.loads` (L114)
- **Outbox.replay_dead_letter** (L117): `self.connect` (L118); `db.execute('SELECT payload FROM dead_letters WHERE id=?', (entry_id,)).fetchone` (L119); `db.execute` (L119); `ValueError` (L120); `json.loads` (L121); `all` (L122); `self._fingerprint` (L122); `db.execute` (L123); `now` (L123); `db.execute` (L124); `db.execute` (L125); `now` (L126); `self.wake.set` (L127)
- **Outbox.pending** (L129): `self.connect` (L130); `db.execute('SELECT count(*) FROM pending').fetchone` (L131); `db.execute` (L131)
- **Outbox.captured_count** (L133): `self.connect` (L134); `db.execute('SELECT occurrences FROM message_captures WHERE session_id=? AND role=? AND content_digest=?', (session_id, role, content_digest)).fetchone` (L135); `db.execute` (L135)
- **Outbox.captured_counts** (L139): `self.connect` (L141); `db.execute('SELECT role,content_digest,occurrences FROM message_captures WHERE session_id=?', (session_id,)).fetchall` (L142); `db.execute` (L142)
- **Outbox.captured_host_ids** (L146): `self.connect` (L148); `db.execute` (L149)
- **Outbox.mark_message_capture** (L152): `self.connect` (L161); `db.execute` (L162); `db.execute` (L166)
- **Outbox.mark_message_host_id** (L168): `self.connect` (L170); `db.execute` (L171)
- **Outbox.observed_tools** (L173): `self.connect` (L174); `db.execute` (L175)
- **Outbox.mark_tool_observed** (L178): `self.connect` (L179); `db.execute` (L180)
- **Outbox._has_forgotten_parent** (L182): `sorted` (L183); `item.get('provenance', {}).get` (L183); `item.get` (L183); `range` (L184); `len` (L184); `self.client.call` (L185); `states.values` (L186)
- **Outbox._discard_deleted** (L190): `self.connect` (L193); `db.execute` (L194); `now` (L194); `db.execute` (L195); `self.mark_receipt` (L196)
- **Outbox._purge_forgotten_dead_letters** (L198): `self.stop.is_set` (L200); `self.connect` (L201); `db.execute('SELECT id,payload FROM dead_letters WHERE id>? ORDER BY id LIMIT 100', (cursor,)).fetchall` (L202); `db.execute` (L202); `json.loads` (L207); `self._has_forgotten_parent` (L208); `self._discard_deleted` (L209); `self._fingerprint` (L209)
- **Outbox.flush** (L215): `set` (L216); `self._purge_forgotten_dead_letters` (L218); `self.connect` (L219); `float` (L220); `time.time` (L220); `sorted` (L222); `','.join` (L222); `db.execute('SELECT id,payload,created_at,attempts,fingerprint FROM pending WHERE next_attempt<=? AND id IN (' + marks + ') ORDER BY created_at', [due, *ordered]).fetchall` (L223); `db.execute` (L223); `db.execute('SELECT id,payload,created_at,attempts,fingerprint FROM pending WHERE next_attempt<=? ORDER BY created_at LIMIT 20', (due,)).fetchall` (L228); `db.execute` (L228); `self.stop.is_set` (L230); `json.loads` (L232); `any` (L233); `adapt_existing` (L235); `self._has_forgotten_parent` (L237); `self._discard_deleted` (L238); `self.client.call` (L240); `self.connect` (L242); `getattr` (L243); `isinstance` (L243); `db.execute` (L244); `type` (L244); `now` (L244); `db.execute` (L245); `db.execute` (L246); `type` (L247); `now` (L247); `LOG.warning` (L248); `type` (L248); `db.execute` (L250); `type` (L251); `time.time` (L251); `min` (L251); `min` (L251); `debug_enabled` (L252); `LOG.debug` (L252); `type` (L252); `self.connect` (L254); `db.execute` (L255); `now` (L255); `db.execute` (L256); `db.execute` (L257); `now` (L257)
- **Outbox._run** (L260): `self.stop.is_set` (L261); `self.wake.wait` (L262); `self.wake.clear` (L263); `self.stop.is_set` (L264); `self.flush` (L265); `self.stop.wait` (L268)
- **Outbox.close** (L270): `self.stop.set` (L271); `self.wake.set` (L272); `self.thread.join` (L273)

## personal_memory/provider.py

Imported by Hermes only. The service and CLI do not depend on Hermes.

- **_episode_fingerprint** (L28): `digest` (L30); `row.get` (L30)
- **_claim_fingerprint** (L33): `digest` (L35); `row.get` (L35); `row.get` (L35); `row.get` (L35); `row.get` (L36)
- **_hook_detail** (L39): `isinstance` (L43); `json.loads` (L45); `isinstance` (L48); `data.get` (L51); `str(data['error'])[:60].replace` (L52); `str` (L52); `data.get` (L53); `isinstance` (L56); `data.get` (L56); `len` (L57)
- **PersonalMemoryProvider.__init__** (L64): `set` (L71); `set` (L72); `threading.RLock` (L73); `threading.Condition` (L77)
- **PersonalMemoryProvider.name** (L96): No direct calls.
- **PersonalMemoryProvider.is_available** (L99): `(Path(get_hermes_home()) / 'personal-memory' / 'settings.json').is_file` (L102); `Path` (L102); `get_hermes_home` (L102)
- **PersonalMemoryProvider.unavailable_reason** (L104): No direct calls.
- **PersonalMemoryProvider.initialize** (L107): `configure_logging` (L108); `Path` (L109); `load_settings` (L111); `kwargs.get` (L114); `context_allowed` (L116); `settings.get` (L116); `session_allowed` (L117); `settings.get` (L117); `kwargs.get` (L118); `kwargs.get` (L119); `kwargs.get` (L119); `atomic_json` (L121); `str` (L122); `Path(kwargs['host_memory_root']).resolve` (L122); `Path` (L122); `settings.get` (L125); `type` (L126); `ValueError` (L127); `settings.get` (L129); `isinstance` (L130); `ValueError` (L131); `bool` (L132); `awareness_cfg.get` (L132); `str` (L133); `awareness_cfg.get` (L133); `Client` (L134); `settings.get` (L134); `Client` (L135); `settings.get` (L135); `self.client.call` (L136); `settings.get` (L137); `kwargs.get` (L140); `str` (L141); `kwargs.get` (L141); `getattr` (L142); `self.actor.update` (L143); `Outbox` (L145); `database_path` (L145); `ExposureLedger` (L147); `getattr` (L148); `self.outbox.connect` (L149); `db.execute` (L150); `db.execute` (L151); `self.lineage.add` (L153)
- **PersonalMemoryProvider.system_prompt_block** (L155): No direct calls.
- **PersonalMemoryProvider.get_tool_schemas** (L158): `copy.deepcopy` (L159)
- **PersonalMemoryProvider.get_config_schema** (L161): No direct calls.
- **PersonalMemoryProvider.save_config** (L167): `values.get` (L170); `values.get` (L170); `isinstance` (L171); `port.isdecimal` (L171); `int` (L171); `isinstance` (L172); `wait.isdecimal` (L172); `int` (L172); `type` (L173); `ValueError` (L173); `type` (L174); `ValueError` (L174); `install` (L175); `Path` (L176); `json.loads` (L177); `path.read_text` (L177); `config.update` (L178); `atomic_json` (L179); `Path` (L180); `public_path.exists` (L181); `json.loads` (L181); `public_path.read_text` (L181); `public.update` (L182); `atomic_json` (L183)
- **PersonalMemoryProvider.backup_paths** (L185): `Path(get_hermes_home()).resolve` (L187); `Path` (L187); `get_hermes_home` (L187); `path.exists` (L189); `Path(json.loads(path.read_text())['data_dir']).expanduser().resolve` (L190); `Path(json.loads(path.read_text())['data_dir']).expanduser` (L190); `Path` (L190); `json.loads` (L190); `path.read_text` (L190); `data.is_relative_to` (L191); `str` (L191)
- **PersonalMemoryProvider.create_native_memory_store** (L193): `CanonicalMemoryStore` (L198)
- **PersonalMemoryProvider.curated_memory_status** (L200): `self.client.call` (L203)
- **PersonalMemoryProvider.dashboard_memory_status** (L205): `load_settings` (L207); `Client(cfg['url'], cfg['token'], timeout=3).call` (L208); `Client` (L208)
- **PersonalMemoryProvider.dashboard_memory_reset** (L210): `load_settings` (L212); `Client` (L213); `client.call` (L214); `state['stores'].items` (L215); `client.call` (L218); `digest` (L220); `time.time_ns` (L220); `client.call` (L221)
- **PersonalMemoryProvider.curated_write_evidence** (L224): `self.check_session_epoch` (L226); `self.outbox.flush` (L228); `self.lineage.compact_parents` (L229)
- **PersonalMemoryProvider._invalidate** (L231): `self.cache.clear` (L234)
- **PersonalMemoryProvider.handle_tool_call** (L237): `json.dumps` (L239); `json.dumps` (L241); `json.dumps` (L243); `dict` (L244); `args.items` (L244); `next` (L245); `set` (L246); `set` (L246); `json.dumps` (L247); `json.dumps` (L251); `self.current_input_record_ids.get` (L253); `self._cached_prefetch_result` (L264); `self.attribute_lineage` (L266); `json.dumps` (L267); `self.client.call` (L269); `self.attribute_lineage` (L273); `self.outbox.pending` (L275); `self.outbox.health` (L276); `self.outbox.receipts` (L278); `self.outbox.connect` (L279); `db.execute("SELECT 1 FROM sqlite_master WHERE name='host_event_receipts'").fetchone` (L280); `db.execute` (L280); `dict` (L281); `zip` (L281); `db.execute` (L282); `self._invalidate` (L284); `json.dumps` (L285); `json.dumps` (L287); `str` (L287); `self.outbox.pending` (L288); `traced` (L236)
- **PersonalMemoryProvider.prefetch** (L292): `self._prefetch` (L294); `result.startswith` (L295); `self.recall_ready.wait_for` (L298); `self._prefetch` (L299); `result.startswith` (L300); `json.loads` (L301); `result.split` (L301); `len` (L302); `len` (L302); `self.attribute_lineage` (L305); `payload.get` (L308); `self.mark_injected` (L311); `RecallStatus` (L312); `query.strip` (L313); `self._awareness_block` (L317); `traced` (L291)
- **PersonalMemoryProvider._awareness_block** (L319): `self.client.call` (L325); `LOG.info` (L328); `type` (L328); `packet.get` (L330); `self.awareness_turn.get` (L334); `self.awareness_turn.get` (L338); `json.dumps` (L341)
- **PersonalMemoryProvider._confirm_awareness_exposure** (L343): `self.awareness_pending.get` (L346); `isinstance` (L349); `payload.get` (L349); `json.dumps` (L350); `isinstance` (L352); `payload.get` (L352); `pending.get` (L353); `str` (L354); `str` (L354); `str` (L356); `pending.get` (L356); `self.client.call` (L358); `type` (L362)
- **PersonalMemoryProvider.recall_status** (L366): No direct calls.
- **PersonalMemoryProvider._prefetch** (L369): `query.strip` (L370); `self.cache.get` (L374); `time.monotonic` (L375); `self.health_client.call` (L377); `cached.get` (L382); `self._format_prefetch` (L383); `self.cache.pop` (L387); `self.inflight.add` (L389); `threading.Thread` (L414); `self.recall_threads.add` (L415); `thread.start` (L416)
- **PersonalMemoryProvider._prefetch.retrieve** (L391): `self.health_client.call` (L394); `self.current_input_record_ids.get` (L396); `self.client.call` (L397); `self._format_prefetch` (L400); `len` (L406); `self.cache.pop` (L407); `next` (L407); `iter` (L407); `time.monotonic` (L408); `tuple` (L410); `self.inflight.discard` (L411); `self.recall_threads.discard` (L412); `threading.current_thread` (L412); `self.recall_ready.notify_all` (L413)
- **PersonalMemoryProvider._format_prefetch** (L419): `result.get` (L421); `result.get` (L421); `result.get` (L422); `result.get` (L423); `result.get` (L424); `result.get` (L425); `self._retained_rows` (L426); `result.get` (L428); `len` (L429); `self._row_retained` (L430); `str` (L430); `source_row.get` (L430); `_episode_fingerprint` (L430); `dict` (L432); `bool` (L433); `row.get` (L433); `len` (L433); `row.get` (L433); `row.get` (L434); `compact['episodes'].append` (L435); `result.get` (L436); `len` (L437); `self._row_retained` (L438); `str` (L438); `row.get` (L438); `_claim_fingerprint` (L438); `compact['claims'].append` (L440); `row.get` (L440); `row.get` (L442); `bool` (L443); `json.dumps` (L445)
- **PersonalMemoryProvider._cached_prefetch_result** (L447): `any` (L456); `args.get` (L456); `args.get` (L458); `args.get` (L459); `args.get` (L459); `_DEPTH_RANK.get` (L460); `type` (L461); `args.get` (L462); `self.recall_ready.wait_for` (L466); `self.cache.get` (L467); `cached.get` (L468); `cached.get` (L468); `tuple` (L468); `time.monotonic` (L469); `self.health_client.call` (L471); `copy.deepcopy` (L473); `result.get` (L474); `result.get` (L475); `row.get` (L476); `isinstance` (L477); `result.get` (L477); `row.get` (L478); `result.setdefault` (L479)
- **PersonalMemoryProvider.attribute_lineage** (L482): `self.lineage.add` (L484)
- **PersonalMemoryProvider.mark_injected** (L486): `self._exposure_state` (L490); `payload.get` (L491); `str` (L492); `row.get` (L492); `_episode_fingerprint` (L492); `payload.get` (L493); `str` (L494); `row.get` (L494); `_claim_fingerprint` (L494)
- **PersonalMemoryProvider._exposure_state** (L496): `self.exposure.get` (L497)
- **PersonalMemoryProvider._retained_rows** (L503): `self.exposure.get` (L506); `dict` (L507)
- **PersonalMemoryProvider._row_retained** (L510): `rows.get` (L513); `bool` (L514)
- **PersonalMemoryProvider._bump_injection_epoch** (L516): `self._exposure_state` (L519); `state['rows'].clear` (L521)
- **PersonalMemoryProvider.queue_prefetch** (L523): `self._prefetch` (L524)
- **PersonalMemoryProvider.sync_turn** (L527): `digest` (L534); `digest` (L535); `self.current_input_record_ids.get` (L535); `self._replay_tool_results` (L537); `self.outbox.captured_counts` (L538); `self.outbox.captured_host_ids` (L539); `digest` (L542); `self._message_ordinal` (L543); `self._host_message_id` (L544); `self._last_message` (L544); `self._consume_started_input` (L545); `self.outbox.mark_message_host_id` (L550); `captured_ids.add` (L550); `captured_counts.get` (L554); `self._capture_event` (L555); `self.outbox.mark_message_capture` (L558); `captured_counts.get` (L559); `captured_ids.add` (L560); `self._invalidate` (L561); `traced` (L526)
- **PersonalMemoryProvider.on_pre_compress** (L563): `RuntimeError` (L565); `isinstance` (L566); `m.get` (L566); `m.get` (L567); `self._replay_tool_results` (L568); `digest` (L569); `self.outbox.captured_counts` (L570); `self.outbox.captured_host_ids` (L571); `enumerate` (L572); `digest` (L573); `row.get` (L573); `occurrences.get` (L573); `self._host_message_id` (L574); `captured_counts.get` (L576); `self._capture_event` (L577); `self.outbox.mark_message_capture` (L581); `captured_counts.get` (L582); `captured_ids.add` (L583); `bool` (L584); `RuntimeError` (L586); `self._bump_injection_epoch` (L589)
- **PersonalMemoryProvider._message_ordinal** (L593): `isinstance` (L597); `row.get` (L597); `row.get` (L597)
- **PersonalMemoryProvider._last_message** (L601): `isinstance` (L605); `row.get` (L605); `row.get` (L605)
- **PersonalMemoryProvider._host_message_id** (L609): `isinstance` (L616); `message.get` (L618); `str` (L620)
- **PersonalMemoryProvider._consume_started_input** (L623): `self.started_inputs.get` (L626); `self.started_inputs.pop` (L628)
- **PersonalMemoryProvider._capture_event** (L632): `metadata.get` (L637); `self.lineage.compact_parents` (L641); `self.lineage.bind` (L642); `digest` (L642); `str` (L644); `self.outbox.mark_receipt` (L645); `digest` (L645); `str` (L645); `json.dumps` (L647); `range` (L649); `len` (L649); `adapt_existing` (L651); `now` (L652); `source.removeprefix` (L652); `now` (L655); `item['provenance'].update` (L656); `capture_ids.append` (L658); `self.outbox.enqueue` (L658); `record_ids.append` (L659); `digest` (L659); `self._invalidate` (L660)
- **PersonalMemoryProvider._replay_tool_results** (L663): `self._capture_tool_results` (L671); `str` (L673)
- **PersonalMemoryProvider._capture_tool_results** (L675): `self.outbox.observed_tools` (L678); `set` (L678); `enumerate` (L679); `isinstance` (L680); `message.get` (L681); `isinstance` (L682); `call.get` (L683); `call.get` (L684); `isinstance` (L684); `function.get` (L684); `isinstance` (L685); `json.loads` (L687); `function.get` (L687); `isinstance` (L688); `call.get` (L688); `message.get` (L690); `message.get` (L691); `str` (L692); `calls.get` (L694); `message.get` (L694); `isinstance` (L695); `self.observe_tool_result` (L696); `call_arguments.get` (L696); `message.get` (L696); `self.outbox.mark_tool_observed` (L700); `observed.add` (L700)
- **PersonalMemoryProvider.observe_tool_result** (L703): `dict` (L707); `metadata.get` (L708); `metadata.get` (L709); `digest` (L709); `isinstance` (L711); `json.loads` (L711); `str(tool_name).startswith` (L715); `str` (L715); `isinstance` (L716); `self.lineage.add` (L717); `self.lineage.compact_parents` (L719); `str` (L722); `str` (L723); `self._attributed_replay` (L725); `self.lineage.block` (L726); `isinstance` (L729); `decoded.get` (L729); `isinstance` (L730); `isinstance` (L730); `proof.get` (L730); `self.lineage.add` (L731); `self.lineage.block` (L733); `str` (L736); `self._capture_event` (L737); `str` (L738); `str` (L740); `isinstance` (L741); `decoded.get` (L741); `self.on_host_event` (L742); `traced` (L702)
- **PersonalMemoryProvider._attributed_replay** (L746): `metadata.get` (L754); `self.lineage.exposed` (L754)
- **PersonalMemoryProvider.on_delegation** (L756): `self._capture_event` (L758); `digest` (L758)
- **PersonalMemoryProvider.filter_native_history** (L761): `dict` (L763); `sync_on_read` (L764); `filter_native` (L765)
- **PersonalMemoryProvider.native_read_evidence** (L767): `native_evidence` (L769)
- **PersonalMemoryProvider.check_native_delivery** (L771): `check_delivery` (L773)
- **PersonalMemoryProvider.check_session_epoch** (L775): `self.client.call` (L776); `RuntimeError` (L777)
- **PersonalMemoryProvider.reset_native_memory** (L779): `load_settings` (L781); `Client(cfg['url'], cfg['token'], timeout=30).call` (L782); `Client` (L782); `Path` (L784); `json.loads` (L785); `private.read_text` (L785); `atomic_json` (L785); `sync_state` (L787); `Path` (L787); `db.execute('SELECT 1 FROM sqlite_master WHERE name=?', (table,)).fetchone` (L790); `db.execute` (L790); `db.execute` (L791)
- **PersonalMemoryProvider.verify_exported_skill** (L794): `load_settings` (L797); `verify` (L798); `Client` (L798); `cfg.get` (L798)
- **PersonalMemoryProvider.requires_native_scope** (L800): No direct calls.
- **PersonalMemoryProvider.authorize_native_history** (L803): `authorize_native` (L805)
- **PersonalMemoryProvider.filter_native_continuity** (L807): `continuity` (L809)
- **PersonalMemoryProvider.authorize_tool_delivery** (L811): `getattr` (L812); `PermissionError` (L815); `self.check_session_epoch` (L816); `self.register_native_artifact` (L817); `self.check_native_delivery` (L818); `PermissionError` (L819)
- **PersonalMemoryProvider.filter_native_session_metadata** (L821): No direct calls.
- **PersonalMemoryProvider.retire_native_notepad** (L828): `load_settings` (L830); `Client` (L830); `client.call` (L833); `client.call` (L836); `page.get` (L837)
- **PersonalMemoryProvider.register_native_artifact** (L841): `getattr` (L842); `PermissionError` (L844); `content.strip` (L845); `self.lineage.compact_parents` (L849); `digest` (L850); `adapt_existing` (L851); `digest` (L852); `len` (L852); `now` (L853); `item['provenance'].update` (L854); `self.client.call` (L855); `range` (L856); `len` (L856); `copy.deepcopy` (L857); `str` (L857); `part['provenance'].update` (L858); `self.client.call` (L859); `self.outbox.connect` (L860); `db.execute` (L861); `db.execute` (L862); `json.dumps` (L862); `self.outbox.connect` (L864); `db.execute` (L865); `db.execute` (L866); `digest` (L866); `adapt_existing` (L868); `json.dumps` (L869); `json.loads` (L869); `now` (L871); `snapshot['provenance'].update` (L872); `self.client.call` (L873)
- **PersonalMemoryProvider.delegation_context** (L876): `self.client.call` (L878); `self.current_input_record_ids.get` (L879); `self.attribute_lineage` (L882); `result.get` (L884); `row.get` (L885); `packet['episodes'].append` (L886); `bool` (L887); `row.get` (L887); `len` (L887); `json.dumps` (L888)
- **PersonalMemoryProvider.on_host_event** (L891): `ValueError` (L894); `self._confirm_awareness_exposure` (L897); `copy.deepcopy` (L899); `isinstance` (L900); `ValueError` (L900); `digest` (L903); `(payload.get('terminal') or {}).get` (L906); `payload.get` (L906); `isinstance` (L907); `self._capture_tool_results` (L907); `self.lineage.block` (L908); `payload.get` (L911); `payload.get` (L912); `skill_revision` (L913); `args.get` (L913); `args.get` (L914); `self.sync_native_files` (L914); `self._capture_event` (L915); `digest` (L915); `type` (L919); `self.outbox.connect` (L920); `db.execute` (L921); `db.execute` (L922); `ack.get` (L923); `now` (L923); `traced` (L890)
- **PersonalMemoryProvider.sync_native_files** (L926): `load_settings` (L929); `self.lineage.compact_parents` (L930); `Client` (L931); `sync_files` (L932)
- **PersonalMemoryProvider.on_memory_write** (L934): `self.sync_native_files` (L935); `str` (L935); `self._capture_event` (L936); `digest` (L936); `str` (L938); `RuntimeError` (L939)
- **PersonalMemoryProvider.on_turn_start** (L942): `str` (L943); `self._capture_event` (L944); `digest` (L944); `self.outbox.mark_message_capture` (L947); `digest` (L947); `digest` (L949); `self.started_inputs.get` (L950); `self.outbox.flush` (L952); `self.lineage.add` (L953)
- **PersonalMemoryProvider.on_session_end** (L955): `self._capture_tool_results` (L956); `self.outbox.captured_counts` (L957); `self.outbox.captured_host_ids` (L958); `enumerate` (L959); `isinstance` (L960); `message.get` (L960); `message.get` (L962); `message.get` (L963); `digest` (L964); `occurrences.get` (L964); `self._host_message_id` (L965); `captured_counts.get` (L967); `self._capture_event` (L968); `digest` (L968); `self.outbox.mark_message_capture` (L972); `captured_counts.get` (L973); `captured_ids.add` (L974); `self.started_inputs.items` (L976)
- **PersonalMemoryProvider.on_session_switch** (L978): `kwargs.get` (L979); `bool` (L980); `kwargs.get` (L980); `self.exposure.get` (L985); `pstate['rows'].items` (L987); `self.lineage.inherit` (L989); `self.current_input_record_ids.pop` (L994); `self.started_inputs.items` (L996); `self._invalidate` (L997)
- **PersonalMemoryProvider.shutdown** (L999): `self._invalidate` (L1001); `self.outbox.close` (L1003); `list` (L1004); `time.monotonic` (L1005); `thread.join` (L1006); `max` (L1006); `time.monotonic` (L1006)
- **register** (L1009): `ctx.register_memory_provider` (L1010); `PersonalMemoryProvider` (L1010)

Module/class/lambda calls: `<module> → logging.getLogger` (L17)

## personal_memory/recovery.py

Authenticated encrypted backups and isolated, integrity-checked restores.

- **create_key** (L18): `Path` (L19); `path.parent.mkdir` (L19); `os.open` (L20); `os.fdopen` (L21); `f.write` (L21); `secrets.token_bytes` (L21); `f.flush` (L21); `os.fsync` (L21); `f.fileno` (L21); `str` (L22)
- **_key** (L25): `Path(path).read_bytes` (L26); `Path` (L26); `len` (L27); `ValueError` (L27); `Path(path).stat` (L28); `Path` (L28); `ValueError` (L28)
- **inspect_database** (L32): `closing` (L33); `sqlite3.connect` (L33); `Path(path).resolve().as_uri` (L33); `Path(path).resolve` (L33); `Path` (L33); `db.execute` (L34); `list` (L35); `db.execute` (L35); `ValueError` (L36); `db.execute('PRAGMA user_version').fetchone` (L37); `db.execute` (L37)
- **_hash** (L40): `hashlib.sha256` (L41); `Path(path).open` (L42); `Path` (L42); `f.read` (L43); `h.update` (L43); `h.hexdigest` (L44)
- **backup** (L47): `Path` (L49); `Path` (L49); `destination.exists` (L50); `ValueError` (L50); `json.loads` (L51); `(home / 'personal-memory/settings.json').read_text` (L51); `destination.parent.mkdir` (L52); `_key` (L53); `tempfile.TemporaryDirectory` (L54); `Path` (L55); `now` (L55); `registry.exists` (L57); `registry.read_bytes` (L58); `json.loads` (L58); `(tmp / 'skill-exports.json').write_bytes` (L59); `_hash` (L60); `Path` (L64); `Path` (L65); `source.exists` (L66); `ValueError` (L67); `closing` (L70); `sqlite3.connect` (L70); `source.resolve().as_uri` (L70); `source.resolve` (L70); `closing` (L71); `sqlite3.connect` (L71); `src.backup` (L72); `dst.commit` (L73); `target.chmod` (L74); `inspect_database` (L75); `_hash` (L75); `atomic_json` (L76); `tarfile.open` (L78); `tar.add` (L79); `secrets.token_bytes` (L80); `Cipher(algorithms.AES(key), modes.GCM(nonce)).encryptor` (L80); `Cipher` (L80); `algorithms.AES` (L80); `modes.GCM` (L80); `encryptor.authenticate_additional_data` (L81); `archive.open` (L83); `encrypted.open` (L83); `dst.write` (L84); `src.read` (L85); `dst.write` (L85); `encryptor.update` (L85); `dst.write` (L86); `encryptor.finalize` (L86); `dst.write` (L86); `dst.flush` (L86); `os.fsync` (L86); `dst.fileno` (L86); `encrypted.chmod` (L87); `os.link` (L89); `os.open` (L91); `os.fsync` (L92); `os.close` (L93); `str` (L94); `_hash` (L94)
- **restore** (L97): `Path` (L99); `Path` (L99); `destination.exists` (L100); `ValueError` (L100); `destination.parent.mkdir` (L101); `tempfile.TemporaryDirectory` (L102); `Path` (L103); `archive.open` (L104); `src.read` (L105); `len` (L105); `ValueError` (L105); `src.read` (L106); `archive.stat` (L106); `len` (L106); `ValueError` (L107); `src.seek` (L108); `src.read` (L108); `src.seek` (L108); `len` (L108); `Cipher(algorithms.AES(_key(key_file)), modes.GCM(nonce, tag)).decryptor` (L109); `Cipher` (L109); `algorithms.AES` (L109); `_key` (L109); `modes.GCM` (L109); `decryptor.authenticate_additional_data` (L110); `plain.open` (L111); `src.read` (L113); `min` (L113); `ValueError` (L114); `len` (L115); `dst.write` (L115); `decryptor.update` (L115); `dst.write` (L116); `decryptor.finalize` (L116); `restored.mkdir` (L117); `tarfile.open` (L118); `set` (L119); `member.isfile` (L121); `ValueError` (L122); `names.add` (L123); `tar.extractfile` (L124); `(restored / member.name).open` (L124); `shutil.copyfileobj` (L124); `(restored / member.name).chmod` (L125); `json.loads` (L126); `(restored / 'manifest.json').read_text` (L126); `manifest.get` (L127); `ValueError` (L127); `manifest['databases'].items` (L128); `ValueError` (L129); `_hash` (L130); `ValueError` (L130); `inspect_database` (L131); `manifest.get` (L132); `set` (L133); `ValueError` (L133); `ValueError` (L134); `extra.items` (L135); `_hash` (L136); `ValueError` (L136); `json.loads` (L137); `(restored / name).read_text` (L137); `Path(deletion_ledger).is_file` (L140); `Path` (L140); `ValueError` (L140); `inspect_database` (L141); `DeletionLedger(restored / 'memory.deletions.db').merge` (L142); `DeletionLedger` (L142); `DeletionLedger` (L142); `Store` (L146); `closing` (L147); `sqlite3.connect` (L147); `db.execute` (L148); `(restored / name).exists` (L150); `inspect_database` (L150); `_hash` (L150); `atomic_json` (L152); `restored.rename` (L153); `str` (L154)

## personal_memory/relevance.py

Candidate relevance heuristics; never proof of an answer or absence.

- **terms** (L7): `re.findall` (L8); `unicodedata.normalize('NFKC', text).casefold` (L8); `unicodedata.normalize` (L8); `t.isdigit` (L8)
- **RelevanceGate.__init__** (L11): `isinstance` (L13); `set` (L13); `ValueError` (L13); `config.get` (L14); `isinstance` (L15); `isinstance` (L15); `math.isfinite` (L15); `ValueError` (L15)
- **RelevanceGate.assess** (L16): `terms` (L17); `any` (L18); `bool` (L18); `terms` (L18); `isinstance` (L19); `math.isfinite` (L19)

Module/class/lambda calls: `<module> → frozenset` (L6); `<module> → 'a an the is are was were be been being do does did have has had i me my mine we our you your it its this that these those what which who whom whose where when why how can could would should please tell about of for to in on at from with and or as now number type private much'.split` (L6)

## personal_memory/reset.py

Resumable logical reset of canonical memory with a write epoch fence.

- **initialize** (L5): `store.connect` (L6); `db.executescript` (L7); `resume` (L12); `sorted` (L13); `store.deletions.ids` (L13); `rid.startswith` (L13); `store.connect` (L14); `db.execute('SELECT 1 FROM memory_reset_tokens WHERE token=?', (token,)).fetchone` (L14); `db.execute` (L14); `_reset` (L15)
- **pending** (L18): `store.connect` (L19); `bool` (L19); `db.execute("SELECT 1 FROM memory_resets WHERE state='pending' LIMIT 1").fetchone` (L19); `db.execute` (L19)
- **epoch** (L22): `store.connect` (L23); `db.execute('SELECT value FROM memory_epoch WHERE id=1').fetchone` (L23); `db.execute` (L23)
- **resume** (L26): `store.connect` (L28); `db.execute` (L28); `store.connect` (L31); `db.execute('SELECT source,source_id FROM memory_reset_sources WHERE epoch=? AND done=0 LIMIT 100', (value,)).fetchall` (L31); `db.execute` (L31); `store.forget_source` (L34); `store.connect` (L35); `db.execute` (L35); `store.connect` (L36); `db.execute` (L37); `db.execute` (L38); `db.execute` (L39); `db.execute` (L40); `db.execute` (L41); `db.execute` (L42); `db.execute('SELECT version FROM curated_heads WHERE target=?', (target,)).fetchone` (L44); `db.execute` (L44); `db.execute` (L46); `db.execute` (L47); `now` (L47); `db.execute` (L48); `db.execute` (L49)
- **reset** (L52): `ValueError` (L53); `__import__('uuid').uuid4` (L54); `__import__` (L54); `store.deletions.append` (L56); `_reset` (L57); `_clear_external` (L61)
- **_clear_external** (L65): `getattr` (L66); `callable` (L67); `clear` (L68); `type` (L69)
- **_reset** (L73): `resume` (L75); `store.connect` (L76); `db.execute` (L77); `db.execute` (L78); `db.execute` (L79); `db.execute` (L80); `db.execute` (L81); `db.execute` (L82); `db.execute` (L83); `db.execute('SELECT value FROM memory_epoch WHERE id=1').fetchone` (L84); `db.execute` (L84); `db.execute` (L85); `db.execute` (L86); `now` (L88); `db.execute` (L89); `db.execute` (L90); `now` (L91); `db.execute` (L92); `db.execute` (L93); `now` (L95); `db.execute` (L96); `now` (L96); `db.execute` (L97); `db.execute` (L98); `resume` (L99)

## personal_memory/retrieval.py

Progressive hybrid retrieval, RRF fusion, and canonical evidence hydration.

- **_epoch** (L16): `isinstance` (L18); `value.strip` (L20); `text.endswith` (L21); `datetime.fromisoformat` (L24); `parsed.replace` (L28); `parsed.timestamp` (L29)
- **_rerank_disabled_by_env** (L37): `os.environ.get('PERSONAL_MEMORY_DISABLE_RERANK', '').strip().lower` (L41); `os.environ.get('PERSONAL_MEMORY_DISABLE_RERANK', '').strip` (L41); `os.environ.get` (L41)
- **_semantic_disabled_by_env** (L44): `os.environ.get('PERSONAL_MEMORY_DISABLE_SEMANTIC', '').strip().lower` (L48); `os.environ.get('PERSONAL_MEMORY_DISABLE_SEMANTIC', '').strip` (L48); `os.environ.get` (L48)
- **Hybrid.__init__** (L52): `RelevanceGate` (L54); `self.config.get` (L54); `self.config.get` (L58); `float` (L60); `temporal.get` (L60); `float` (L64); `temporal.get` (L64); `math.isfinite` (L67); `math.isfinite` (L69); `threading.Event` (L74); `ThreadPoolExecutor` (L75); `self.config.get` (L82); `isinstance` (L83); `bool` (L85); `self._semantic_cfg.get` (L85); `_semantic_disabled_by_env` (L85); `threading.Lock` (L87); `self.config.get` (L90); `hindsight_cfg.get` (L91); `Hindsight` (L94); `type` (L96); `self.config.get` (L102); `isinstance` (L103); `bool` (L106); `rerank.get` (L106); `_rerank_disabled_by_env` (L106); `rerank.get` (L108); `max` (L111); `min` (L111); `int` (L111); `rerank.get` (L111); `self.config.get` (L119); `isinstance` (L120); `bool` (L122); `graph.get` (L122); `self._bounded_int` (L123); `graph.get` (L123); `self._bounded_float` (L124); `graph.get` (L124); `self._bounded_float` (L125); `graph.get` (L125); `self._bounded_int` (L126); `graph.get` (L126); `self._bounded_int` (L127); `graph.get` (L127); `self._bounded_int` (L128); `graph.get` (L128); `bool` (L136); `ProcessLease` (L141); `Path` (L141); `self._start_index_thread` (L144); `self.close` (L151); `LOG.warning` (L153)
- **Hybrid._start_index_thread** (L156): `threading.Thread` (L169); `thread.start` (L170); `self.threads.append` (L170)
- **Hybrid._start_index_thread.work** (L158): `self.stop.wait` (L160); `engine.sync` (L165); `type` (L167)
- **Hybrid._ensure_semantic** (L172): `SemanticIndex` (L188); `type` (L192); `self._start_index_thread` (L198)
- **Hybrid.close** (L201): `self.stop.set` (L202); `self.pool.shutdown` (L203); `time.monotonic` (L204); `self._warmup.join` (L210); `self._warmup.is_alive` (L211); `LOG.warning` (L212); `thread.join` (L214); `max` (L214); `time.monotonic` (L214); `any` (L220); `thread.is_alive` (L220); `RuntimeError` (L221); `self.index_lease.close` (L222); `self._warmup.is_alive` (L224)
- **Hybrid.start_warmup** (L227): `thread.is_alive` (L235); `threading.Thread` (L237); `thread.start` (L239)
- **Hybrid.warmup** (L242): `self.stop.is_set` (L257); `self._ensure_semantic` (L258); `self.stop.is_set` (L259); `self.semantic.embedder.query` (L261); `self.stop.is_set` (L264); `self.hindsight.candidates` (L266); `self.stop.is_set` (L269); `self._ensure_reranker` (L271)
- **Hybrid.clear_external** (L277): `callable` (L281); `getattr` (L281); `self.hindsight.clear_bank` (L282)
- **Hybrid.status** (L285): `self.store.status` (L286); `self.semantic.status` (L288); `result.update` (L293); `self.hindsight.status` (L295); `result['capabilities'].append` (L299); `result['capabilities'].append` (L301); `result['capabilities'].append` (L303); `result['capabilities'].append` (L307); `round` (L309)
- **Hybrid._bounded_int** (L314): `max` (L317); `min` (L317); `int` (L317)
- **Hybrid._bounded_float** (L322): `float` (L325); `math.isfinite` (L328)
- **Hybrid._propagate_graph** (L330): `self.store.connect` (L345); `set` (L365); `set` (L366); `range` (L367); `set` (L370); `entities.update` (L372); `entities_of` (L372); `set` (L373); `records_of` (L375); `visited.add` (L379); `nxt.add` (L379); `list` (L381); `entities_of` (L382); `list` (L383); `records_of` (L384); `sum` (L386); `max` (L386); `seeds.values` (L386); `max` (L387); `seeds.items` (L387); `dict` (L388); `range` (L390); `defaultdict` (L391); `mass.items` (L392); `record_entities.get` (L395); `len` (L398); `defaultdict` (L401); `entity_mass.items` (L402); `entity_records.get` (L405); `len` (L408); `personalization.get` (L411); `record_mass.get` (L411); `mass.get` (L418); `record_entities.get` (L422); `entity_records.get` (L423); `propagated.append` (L429); `propagated.sort` (L430)
- **Hybrid._propagate_graph.entities_of** (L346): `record_entities.get` (L347); `db.execute` (L349)
- **Hybrid._propagate_graph.records_of** (L355): `entity_records.get` (L356); `db.execute` (L358)
- **Hybrid._apply_recency** (L433): `time.time` (L443); `list` (L445); `self.store.connect` (L447); `range` (L448); `len` (L448); `','.join` (L450); `len` (L450); `db.execute` (L451); `sorted` (L461)
- **Hybrid._apply_recency.adjusted** (L454): `_epoch` (L455); `times.get` (L455); `max` (L458)
- **Hybrid._ensure_reranker** (L463): `CrossEncoder` (L473); `type` (L477)
- **Hybrid._apply_rerank** (L480): `len` (L490); `self._ensure_reranker` (L492); `list` (L495); `self.store.connect` (L497); `range` (L498); `len` (L498); `','.join` (L500); `len` (L500); `db.execute` (L501); `texts.get` (L503); `reranker.predict` (L505); `type` (L507); `float` (L509); `min` (L510); `max` (L510); `zip` (L514); `set` (L516); `sorted` (L517)
- **Hybrid.search** (L521): `required_text` (L524); `type` (L525); `ValueError` (L526); `ValueError` (L528); `isinstance` (L529); `len` (L529); `ValueError` (L530); `list` (L531); `dict.fromkeys` (L531); `required_text` (L531); `isinstance` (L533); `len` (L533); `any` (L534); `isinstance` (L534); `ValueError` (L535); `set` (L536); `timestamp` (L537); `timestamp` (L538); `ValueError` (L540); `time.monotonic` (L541); `self.store.related_ids` (L542); `clauses.append` (L544); `params.append` (L544); `clauses.append` (L545); `params.append` (L545); `clauses.append` (L546); `params.append` (L546); `clauses.append` (L547); `self.store.connect` (L549); `db.execute` (L550); `' AND '.join` (L550); `defaultdict` (L554); `defaultdict` (L554); `dict` (L555); `self._ensure_semantic` (L570); `self.store.search` (L573); `add` (L575); `add` (L576); `claim_rows.update` (L577); `futures.append` (L579); `self.pool.submit` (L579); `futures.append` (L581); `self.pool.submit` (L581); `time.monotonic` (L583); `add` (L585); `future.result` (L585); `max` (L585); `time.monotonic` (L585); `debug_enabled` (L586); `round` (L586); `time.monotonic` (L586); `future.cancel` (L588); `type` (L589); `debug_enabled` (L590); `sorted` (L598); `self.store.connect` (L600); `db.execute` (L602); `db.execute` (L603); `self.store.related_ids` (L609); `sorted` (L613); `neighbours.append` (L615); `self._propagate_graph` (L617); `neighbours.append` (L619); `add` (L620); `sorted` (L622); `self.store.related_ids` (L624); `self._apply_rerank` (L626); `self._apply_recency` (L627); `self.store.connect` (L631); `db.execute('SELECT 1 FROM record_visibility WHERE record_id=? AND hidden=1', (rid,)).fetchone` (L633); `db.execute` (L633); `db.execute("SELECT id,source,source_id,NULLIF(occurred_at,'') AS occurred_at,kind,text FROM records WHERE id=? AND deleted=0", (rid,)).fetchone` (L634); `db.execute` (L634); `dict` (L636); `self.relevance.assess` (L637); `similarities.get` (L637); `spans.get` (L642); `min` (L642); `len` (L642); `item.update` (L643); `min` (L643); `len` (L643); `min` (L644); `sorted` (L644); `round` (L644); `episodes.append` (L645); `len` (L646); `db.execute` (L652); `dict` (L653); `claim_rows.values` (L655); `db.execute("SELECT c.* FROM claims c JOIN records r ON r.id=c.record_id WHERE c.id=? AND r.deleted=0 AND c.status!='retracted'", (claim['id'],)).fetchone` (L657); `db.execute` (L657); `claims.append` (L659); `dict` (L659); `self.status` (L660); `debug_enabled` (L661); `LOG.debug` (L662); `len` (L663); `round` (L664); `time.monotonic` (L664); `state['semantic'].get` (L666); `self.store.generation` (L668); `len` (L668); `round` (L669); `time.monotonic` (L669)
- **Hybrid.search.add** (L558): `set` (L559); `enumerate` (L560); `seen.add` (L563); `max` (L565); `similarities.get` (L565); `reasons[rid].add` (L566); `spans.setdefault` (L567); `counts.get` (L568); `len` (L568)

Module/class/lambda calls: `Hybrid._apply_recency.<lambda@461> → adjusted` (L461)

## personal_memory/runner.py

Private subprocess entrypoint for operator-installed adapters.

- **main** (L6): `resource.setrlimit` (L9); `sys.argv[1].split` (L10); `getattr` (L11); `importlib.import_module` (L11); `sys.stdin.buffer.read` (L12); `len` (L13); `ValueError` (L13); `json.loads` (L14); `handler` (L15); `json.dumps` (L16); `len` (L17); `data.encode` (L17); `ValueError` (L17); `sys.stdout.write` (L18)

Module/class/lambda calls: `<module> → main` (L20)

## personal_memory/semantic.py

Rebuildable, chunked multilingual embeddings with a durable indexing journal.

- **_next_seq** (L45): `db.execute` (L48); `db.execute` (L49); `db.execute('SELECT value FROM semantic_seq WHERE id=1').fetchone` (L50); `db.execute` (L50)
- **_signal** (L53): `_next_seq` (L54); `db.execute` (L56); `db.execute` (L57); `now` (L60)
- **enqueue** (L64): `_signal` (L72)
- **ensure_schema** (L75): `db.executescript` (L83); `db.execute` (L84); `db.execute` (L90); `db.execute` (L91); `db.execute` (L95); `db.execute('SELECT COALESCE(MAX(id),0) FROM semantic_work').fetchone` (L98); `db.execute` (L98); `db.execute` (L99); `db.execute` (L100); `db.execute` (L101); `db.execute` (L102); `db.execute` (L103); `db.execute` (L104); `now` (L105); `db.commit` (L107)
- **FastEmbedder.__init__** (L111): `TextEmbedding` (L113); `config.get` (L113); `config.get` (L114); `config.get` (L114); `config.get` (L115); `Path` (L116); `hashlib.sha256` (L117); `sorted` (L118); `model_root.rglob` (L118); `file.open` (L119); `stream.read` (L120); `weights.update` (L120); `file.exists` (L123); `weights.update` (L123); `file.read_bytes` (L123); `digest` (L124); `config.get` (L124); `weights.hexdigest` (L124); `importlib.metadata.version` (L124); `config.get` (L125); `threading.Lock` (L126)
- **FastEmbedder.chunks** (L129): `max` (L133); `min` (L133); `self.tokenizer.no_truncation` (L134); `self.tokenizer.encode` (L136); `self.tokenizer.enable_truncation` (L138); `range` (L139); `len` (L139); `max` (L139); `min` (L139); `min` (L140); `len` (L140); `len` (L143)
- **FastEmbedder.documents** (L146): `list` (L148); `self.model.passage_embed` (L148)
- **FastEmbedder.query** (L150): `next` (L152); `self.model.query_embed` (L152)
- **EndpointEmbedder.__init__** (L157): `Client` (L159); `config.get` (L159); `config.get` (L159); `int` (L161); `config.get` (L161); `ValueError` (L162); `config.get` (L163); `config.get` (L164); `digest` (L165); `config.get` (L165)
- **EndpointEmbedder.chunks** (L166): `range` (L167); `len` (L167); `min` (L168); `len` (L168); `len` (L170)
- **EndpointEmbedder._embed** (L171): `self.client.call` (L172); `sorted` (L173); `list` (L174); `range` (L174); `len` (L174); `ValueError` (L174)
- **EndpointEmbedder.documents** (L176): `range` (L178); `len` (L178); `out.extend` (L178); `self._embed` (L178)
- **EndpointEmbedder.query** (L180): `self._embed` (L180)
- **SemanticIndex.__init__** (L184): `config.get` (L188); `EndpointEmbedder` (L188); `FastEmbedder` (L188); `threading.RLock` (L190); `threading.Lock` (L190); `config.get` (L194); `store.connect` (L200); `db.executescript` (L201); `ensure_schema` (L212); `db.execute` (L216); `self._activate` (L217); `store.connect` (L218); `db.execute` (L219); `self._add` (L220); `dict` (L220)
- **SemanticIndex._activate** (L222): `db.execute('SELECT seq FROM semantic_progress WHERE model=?', (self.key,)).fetchone` (L232); `db.execute` (L232); `db.execute('SELECT MIN(seq) FROM semantic_history').fetchone` (L233); `db.execute` (L233); `self._reconcile` (L235); `db.execute('SELECT h.record_id,h.kind FROM semantic_history h\n                    WHERE h.seq>? AND h.seq=(SELECT MAX(seq) FROM semantic_history m\n                                               WHERE m.record_id=h.record_id)\n                    ORDER BY h.seq', (row[0],)).fetchall` (L237); `db.execute` (L237); `_signal` (L241); `db.execute('SELECT value FROM semantic_seq').fetchone` (L242); `db.execute` (L242); `db.execute` (L243); `now` (L245); `db.execute('SELECT COALESCE(MIN(seq),0) FROM semantic_progress').fetchone` (L249); `db.execute` (L249); `db.execute` (L250); `max` (L250)
- **SemanticIndex._reconcile** (L252): `db.execute('SELECT r.id FROM records r WHERE r.deleted=0\n                AND NOT EXISTS(SELECT 1 FROM record_visibility z WHERE z.record_id=r.id AND z.hidden=1)\n                AND NOT EXISTS(SELECT 1 FROM vector_done d WHERE d.model=? AND d.record_id=r.id)', (self.key,)).fetchall` (L255); `db.execute` (L255); `_signal` (L259); `db.execute('SELECT DISTINCT v.record_id FROM\n                (SELECT record_id FROM vector_chunks UNION SELECT record_id FROM vector_done) v\n                WHERE NOT EXISTS(SELECT 1 FROM records r WHERE r.id=v.record_id AND r.deleted=0\n                AND NOT EXISTS(SELECT 1 FROM record_visibility z WHERE z.record_id=r.id AND z.hidden=1))').fetchall` (L260); `db.execute` (L260); `_signal` (L264)
- **SemanticIndex._add** (L266): `np.frombuffer(row['vector'], dtype=np.float32).copy` (L268); `np.frombuffer` (L268); `np.isfinite(vector).all` (L269); `np.isfinite` (L269); `np.linalg.norm` (L269); `ValueError` (L270); `np.linalg.norm` (L271); `len` (L272); `ValueError` (L272); `len` (L273); `self.hnsw.Index` (L278); `len` (L278); `self.index.init_index` (L279); `self.index.set_num_threads` (L280); `self.index.get_current_count` (L281); `self.index.get_max_elements` (L281); `self.index.resize_index` (L282); `self.index.get_max_elements` (L282); `self.index.add_items` (L283); `vector.reshape` (L283)
- **SemanticIndex._retire_record** (L287): `db.execute` (L296); `db.execute` (L297); `db.execute` (L298); `self.rows.items` (L300); `self.index.mark_deleted` (L301); `self.rows.pop` (L302); `self.vectors.pop` (L302)
- **SemanticIndex.sync** (L304): `time.time` (L307); `self.store.connect` (L309); `db.execute` (L310); `db.execute("SELECT record_id,seq FROM semantic_work WHERE kind='retire' AND available_at<=? ORDER BY seq LIMIT ?", (due, batch)).fetchall` (L311); `db.execute` (L311); `self._retire_record` (L315); `db.execute` (L317); `len` (L319); `self.store.connect` (L322); `dict` (L323); `db.execute` (L323); `self._index_record` (L328); `self.store.connect` (L329); `db.execute` (L330); `self.store.connect` (L332); `db.execute` (L333); `min` (L334); `db.execute` (L337); `time.time` (L338); `now` (L338); `db.execute` (L340); `type` (L341); `time.time` (L341); `db.execute` (L342); `json.dumps` (L344); `type` (L344); `now` (L344)
- **SemanticIndex._live** (L349): `bool` (L350); `db.execute('SELECT 1 FROM records r WHERE r.id=? AND r.deleted=0\n            AND NOT EXISTS(SELECT 1 FROM record_visibility z WHERE z.record_id=r.id AND z.hidden=1)', (record_id,)).fetchone` (L350); `db.execute` (L350)
- **SemanticIndex._publish** (L354): `self._add` (L363)
- **SemanticIndex._index_record** (L365): `self.store.connect` (L375); `self._live` (L376); `db.execute('SELECT 1 FROM vector_done WHERE model=? AND record_id=?', (self.key, rid)).fetchone` (L377); `db.execute` (L377); `dict` (L381); `db.execute` (L381); `self.store.connect` (L387); `db.execute` (L388); `self._retire_record` (L389); `db.execute` (L390); `dict` (L396); `db.execute` (L396); `self._embed_record` (L400); `db.execute('SELECT text FROM records WHERE id=?', (rid,)).fetchone` (L400); `db.execute` (L400); `self._publish` (L402); `self.store.connect` (L405); `db.execute` (L406); `self._live` (L407); `self._retire_record` (L408); `db.execute` (L410); `db.execute` (L411); `now` (L412); `db.execute` (L415)
- **SemanticIndex._embed_record** (L418): `list` (L425); `self.embedder.chunks` (L425); `self.embedder.documents` (L426); `len` (L427); `len` (L427); `ValueError` (L428); `self.np.asarray` (L432); `self.np.isfinite(vector).all` (L433); `self.np.isfinite` (L433); `self.np.linalg.norm` (L433); `ValueError` (L434); `len` (L435); `ValueError` (L436); `len` (L437); `validated.append` (L438); `self.store.connect` (L440); `db.execute` (L441); `zip` (L442); `db.execute` (L443); `vector.tobytes` (L445); `additions.append` (L446); `vector.tobytes` (L447)
- **SemanticIndex.candidates** (L451): `self.np.asarray` (L452); `self.embedder.query` (L452); `len` (L453); `self.np.isfinite(vector).all` (L453); `self.np.isfinite` (L453); `self.np.linalg.norm` (L453); `ValueError` (L454); `self.np.linalg.norm` (L455); `self.rows.items` (L457); `len` (L458); `len` (L458); `min` (L460); `self.index.set_ef` (L462); `max` (L462); `self.index.knn_query` (L463); `vector.reshape` (L463); `zip` (L465); `labels[0].tolist` (L465); `(1 - distances[0]).tolist` (L465); `sorted` (L467); `float` (L467); `set` (L468); `results.append` (L473); `seen.add` (L474)
- **SemanticIndex.status** (L477): `self.store.connect` (L478); `db.execute("SELECT count(*) FROM semantic_work w WHERE w.kind='index' AND NOT EXISTS(\n                SELECT 1 FROM vector_done d WHERE d.model=? AND d.record_id=w.record_id)", (self.key,)).fetchone` (L481); `db.execute` (L481); `db.execute("SELECT count(*) FROM semantic_work WHERE kind='retire'").fetchone` (L485); `db.execute` (L485); `db.execute('SELECT count(*) FROM vector_failures f JOIN records r ON r.id=f.record_id WHERE f.model=? AND r.deleted=0 AND NOT EXISTS(SELECT 1 FROM record_visibility z WHERE z.record_id=r.id AND z.hidden=1)', (self.key,)).fetchone` (L486); `db.execute` (L486)

## personal_memory/server.py

(Module entry point.)

- **create_server** (L16): `ValueError` (L18); `isinstance` (L19); `len` (L19); `ValueError` (L20); `MemoryService` (L23); `Server` (L88)
- **create_server.Handler.log_message** (L28): No direct calls.
- **create_server.Handler.setup** (L31): `super().setup` (L32); `super` (L32); `self.connection.settimeout` (L33)
- **create_server.Handler.respond** (L35): `getattr` (L38); `isinstance` (L39); `json.dumps(data, ensure_ascii=False).encode` (L41); `json.dumps` (L41); `self.send_response` (L42); `self.send_header` (L43); `self.send_header` (L44); `str` (L44); `len` (L44); `self.send_header` (L45); `self.send_header` (L47); `self.end_headers` (L48); `self.wfile.write` (L49)
- **create_server.Handler.do_POST** (L51): `sanitize_trace` (L52); `self.headers.get` (L52); `new_trace` (L52); `self.headers.get` (L53); `service.authenticate` (L54); `self.respond` (L56); `self.headers.get` (L57); `self.respond` (L58); `self.respond` (L60); `self.headers.get` (L62); `ValueError` (L63); `int` (L64); `self.headers.get` (L64); `self.respond` (L66); `strict_json` (L67); `self.rfile.read` (L67); `isinstance` (L68); `ValueError` (L69); `service.dispatch` (L70); `self.respond` (L71); `self.respond` (L73); `self.respond` (L75); `self.respond` (L77); `str` (L77); `self.respond` (L79); `str` (L79); `LOG.error` (L81); `self.respond` (L82)
- **create_server.Server.server_close** (L85): `service.close` (L86); `super().server_close` (L87); `super` (L87)

Module/class/lambda calls: `<module> → logging.getLogger` (L12)

## personal_memory/service.py

Shared application boundary for production ASGI and the local test transport.

- **_lease** (L31): No direct calls.
- **MemoryService.__init__** (L40): `self._construct` (L47); `self._release_failed_startup` (L51)
- **MemoryService._release_failed_startup** (L54): `self.close` (L58); `LOG.warning` (L60)
- **MemoryService._construct** (L62): `isinstance` (L63); `len` (L63); `ValueError` (L63); `list` (L64); `set` (L65); `principal.get` (L67); `isinstance` (L68); `len` (L68); `ValueError` (L68); `seen.add` (L69); `principal.get` (L70); `isinstance` (L71); `any` (L71); `isinstance` (L71); `ValueError` (L71); `principal.get` (L72); `ValueError` (L72); `principal.get` (L73); `ValueError` (L73); `isinstance` (L74); `principal.get` (L74); `any` (L75); `isinstance` (L75); `isinstance` (L76); `principal.get` (L76); `ValueError` (L77); `ExtensionRegistry` (L79); `validate_intelligence_config` (L85); `validate_recall_config` (L86); `(intelligence_config or {}).get` (L86); `Store` (L87); `database_path` (L87); `load_backend` (L90); `self._closables.append` (L91); `Learning` (L93); `AdaptiveRecall` (L94); `(intelligence_config or {}).get` (L94); `Intelligence` (L96); `Investigation` (L98); `self._closables.append` (L99); `Workflows` (L100); `self._closables.append` (L101); `self.learning_routes.update` (L105); `time.monotonic` (L114); `threading.Lock` (L114); `self.routes.update` (L137); `{'/v1/blob/begin': blobs.begin, '/v1/blob/put': blobs.put, '/v1/blob/complete': blobs.complete, '/v1/blob/read': blobs.read, '/v1/blob/list': blobs.listing}.items` (L137); `reset.initialize` (L142); `self.workflows.start` (L143); `native_catalog.initialize` (L150); `self.routes.update` (L154); `self.routes.update` (L155); `self.routes.update` (L156); `SourceRuntime` (L161); `self._closables.append` (L162); `self.routes.update` (L163); `self.store.connect` (L182); `db.execute("SELECT 1 FROM source_connections WHERE state='active' LIMIT 1").fetchone` (L183); `db.execute` (L183); `self.sources.start` (L184)
- **MemoryService.intelligence_schema** (L186): `partial` (L196); `{'list': blobs.listing, 'read': blobs.read, 'begin': blobs.begin, 'put': blobs.put, 'complete': blobs.complete}.items` (L198); `partial` (L199); `partial` (L201); `partial` (L202); `methods.items` (L204); `inspect.signature(method).parameters.items` (L206); `inspect.signature` (L206); `required.append` (L208)
- **MemoryService.authenticate** (L213): `hmac.compare_digest` (L216); `authorization.encode` (L216); `('Bearer ' + principal['token']).encode` (L216); `AccessDenied` (L217)
- **MemoryService.authorize** (L220): `self.store.connect` (L229); `self.learning._get` (L230); `args.get` (L230); `principal.get` (L231); `AccessDenied` (L232); `args.get` (L234); `AccessDenied` (L235); `args.get` (L240); `self.store.connect` (L242); `db.execute('SELECT record_id FROM memory_blobs WHERE id=?', (args.get('blob_id'),)).fetchone` (L243); `db.execute` (L243); `args.get` (L243); `AccessDenied` (L244); `self.store.evidence` (L246); `evidence.get('ingestion_record', {}).get('provenance', {}).get` (L248); `evidence.get('ingestion_record', {}).get` (L248); `evidence.get` (L248); `AccessDenied` (L249); `args.get` (L252); `args.get` (L252); `AccessDenied` (L253); `validate_batch` (L255); `args.get` (L255); `AccessDenied` (L259); `self.store.evidence` (L261); `AccessDenied` (L262); `AccessDenied` (L263); `AccessDenied` (L265)
- **MemoryService.ingest** (L267): `set` (L268); `ContractError` (L268); `type` (L270); `epoch` (L270); `ValueError` (L271); `validate_batch` (L272); `self.extension_registry.validate` (L273); `epoch` (L275); `ValueError` (L275); `self.store.ingest_contract` (L276); `args.get` (L276); `zip` (L278); `observe` (L279); `item.get('extensions', {}).get('personal_memory.legacy', {}).get` (L280); `item.get('extensions', {}).get` (L280); `item.get` (L280); `metadata.get` (L281); `metadata.get` (L283); `self.learning.outcome` (L284); `len` (L285)
- **MemoryService.dispatch** (L289): `get_trace` (L293); `self._dispatch` (L294); `bind_trace` (L295); `sanitize_trace` (L295); `new_trace` (L295); `time.monotonic` (L296); `self._dispatch` (L298); `log_call` (L300); `principal.get` (L300); `type` (L301); `LOG.isEnabledFor` (L303); `self._summary` (L303); `log_call` (L304); `principal.get` (L304)
- **MemoryService._summary** (L308): `isinstance` (L310); `isinstance` (L313); `result.get` (L313); `len` (L313); `isinstance` (L314); `result.get` (L314); `len` (L314); `result.get` (L316); `isinstance` (L317); `diag.get` (L318); `isinstance` (L319); `sum` (L319); `cand.values` (L319); `isinstance` (L319); `diag.get` (L320); `len` (L320); `isinstance` (L321); `diag.get` (L321)
- **MemoryService._dispatch** (L324): `path.endswith` (L326); `set` (L328); `isinstance` (L328); `ContractError` (L329); `isinstance` (L332); `operations.get` (L332); `ContractError` (L334); `self.dispatch` (L335); `KeyError` (L336); `self.authorize` (L337); `pending` (L339); `ValueError` (L340); `epoch` (L343); `AccessDenied` (L343); `ValueError` (L348); `path.endswith` (L350); `fn` (L351); `digest` (L351); `ValueError` (L354); `changes.read` (L356); `digest` (L356); `changes.configure` (L359); `awareness.replay` (L360); `awareness.configure_consumer` (L361); `ValueError` (L363); `digest` (L365); `self.learning_routes[path]` (L366); `self.routes[path]` (L367)
- **MemoryService.status** (L372): `hasattr` (L374); `self.retrieval.status` (L374); `self.store.status` (L374); `list` (L376); `dict.fromkeys` (L376); `result.get` (L376); `list` (L380); `dict.fromkeys` (L380); `list` (L384); `dict.fromkeys` (L384); `changes.status` (L386)
- **MemoryService.health** (L396): `self.store.generation` (L398); `dict` (L399); `round` (L400); `time.monotonic` (L400)
- **MemoryService.ready** (L402): `hasattr` (L403); `self.retrieval.status` (L403); `self.store.status` (L403); `list` (L404); `status.get` (L404); `pending` (L406); `problems.append` (L406); `status.get` (L408); `engine.get` (L409); `problems.append` (L409); `engine.get` (L410); `problems.append` (L410); `engine.get` (L411); `problems.append` (L411); `engine.get` (L412); `problems.append` (L412); `engine.get` (L415); `problems.append` (L415); `self.workflows.status` (L417)
- **MemoryService.close** (L419): `getattr` (L427); `self._closables.pop` (L428); `getattr` (L429); `close_fn` (L431); `errors.append` (L433); `retained.append` (L434); `self._closables.extend` (L435); `reversed` (L435)

Module/class/lambda calls: `<module> → logging.getLogger` (L13); `<module> → READ_PATHS.update` (L17); `<module> → READ_PATHS.update` (L18); `<module> → READ_PATHS.discard` (L19); `<module> → READ_PATHS.add` (L20); `<module> → READ_PATHS.add` (L21); `<module> → READ_PATHS.update` (L22); `<module> → READ_PATHS.add` (L23); `<module> → READ_PATHS.update` (L24); `<module> → AGENT_WRITES.update` (L26); `<module> → AGENT_WRITES.update` (L27); `MemoryService._construct.<lambda@116> → self.store.checkpoint` (L116); `MemoryService._construct.<lambda@118> → self.retrieval.search` (L118); `MemoryService._construct.<lambda@119> → self.adaptive.search` (L119); `MemoryService._construct.<lambda@120> → self.investigation.search` (L120); `MemoryService._construct.<lambda@121> → self.learning.browse` (L121); `MemoryService._construct.<lambda@122> → self.store.evidence` (L122); `MemoryService._construct.<lambda@123> → self.store.lineage_status` (L123); `MemoryService._construct.<lambda@124> → self.store.source_status` (L124); `MemoryService._construct.<lambda@125> → self.store.forget_source` (L125); `MemoryService._construct.<lambda@126> → self.store.supersede` (L126); `MemoryService._construct.<lambda@127> → self.store.entity` (L127); `MemoryService._construct.<lambda@127> → self.store.entities` (L127); `MemoryService._construct.<lambda@128> → self.store.claim` (L128); `MemoryService._construct.<lambda@128> → self.store.timeline` (L128); `MemoryService._construct.<lambda@129> → self.store.browse` (L129); `MemoryService._construct.<lambda@129> → self.store.connections` (L129); `MemoryService._construct.<lambda@130> → self.store.identity` (L130); `MemoryService._construct.<lambda@130> → self.store.identity_revoke` (L130); `MemoryService._construct.<lambda@131> → self.store.forget` (L131); `MemoryService._construct.<lambda@131> → self.store.coverage` (L131); `MemoryService._construct.<lambda@132> → self.status` (L132); `MemoryService._construct.<lambda@133> → self.store.generation` (L133); `MemoryService._construct.<lambda@134> → self.health` (L134); `MemoryService._construct.<lambda@134> → self.ready` (L134); `MemoryService._construct.<lambda@137> → fn` (L137); `MemoryService._construct.<lambda@144> → reset.reset` (L144); `MemoryService._construct.<lambda@145> → reset.epoch` (L145); `MemoryService._construct.<lambda@146> → curated.read` (L146); `MemoryService._construct.<lambda@151> → native_catalog.read` (L151); `MemoryService._construct.<lambda@156> → self.intelligence_schema` (L156); `MemoryService._construct.<lambda@156> → self.intelligence.summaries` (L156); `MemoryService._construct.<lambda@156> → self.intelligence.procedures` (L156); `MemoryService._construct.<lambda@156> → self.intelligence.coverage` (L156); `MemoryService._construct.<lambda@156> → self.intelligence.beliefs` (L156); `MemoryService._construct.<lambda@157> → self.intelligence.graph` (L157); `MemoryService._construct.<lambda@157> → self.intelligence.aggregate` (L157); `MemoryService._construct.<lambda@158> → self.intelligence.tasks` (L158); `MemoryService._construct.<lambda@158> → self.workflows.get` (L158); `MemoryService._construct.<lambda@159> → self.intelligence.quality` (L159); `MemoryService._construct.<lambda@159> → self.workflows.status` (L159); `MemoryService._construct.<lambda@163> → self.sources.status` (L163); `MemoryService._construct.<lambda@164> → self.sources.connect_gmail` (L164); `MemoryService._construct.<lambda@165> → self.sources.control` (L165); `MemoryService._construct.<lambda@169> → awareness.status` (L169); `MemoryService._construct.<lambda@170> → awareness.claim` (L170); `MemoryService._construct.<lambda@171> → awareness.complete` (L171); `MemoryService._construct.<lambda@171> → _lease` (L172); `MemoryService._construct.<lambda@171> → a.get` (L172); `MemoryService._construct.<lambda@171> → a.get` (L173); `MemoryService._construct.<lambda@171> → a.get` (L173); `MemoryService._construct.<lambda@174> → awareness.defer` (L174); `MemoryService._construct.<lambda@174> → _lease` (L175); `MemoryService._construct.<lambda@174> → a.get` (L175); `MemoryService._construct.<lambda@174> → a.get` (L175); `MemoryService._construct.<lambda@176> → awareness.sweep` (L176); `MemoryService._construct.<lambda@176> → awareness.prepare` (L177); `MemoryService._construct.<lambda@178> → awareness.expose` (L178)

## personal_memory/setup.py

(Module entry point.)

- **install** (L12): `RuntimeError` (L16); `Path(home).expanduser().resolve` (L17); `Path(home).expanduser` (L17); `Path` (L17); `home.mkdir` (L18); `config_path.exists` (L20); `yaml.safe_load` (L20); `config_path.read_text` (L20); `isinstance` (L22); `ValueError` (L23); `type` (L24); `ValueError` (L25); `isinstance` (L26); `config.get` (L26); `ValueError` (L27); `config.get('agent', {}).get` (L28); `config.get` (L28); `ValueError` (L30); `state.mkdir` (L32); `settings_path.exists` (L34); `json.loads` (L34); `settings_path.read_text` (L34); `secrets.token_urlsafe` (L35); `str` (L36); `secrets.token_urlsafe` (L38); `settings.setdefault('principals', []).append` (L39); `settings.setdefault` (L39); `rollback_path.exists` (L41); `json.loads` (L42); `rollback_path.read_text` (L42); `config_path.exists` (L44); `config.get` (L44); `config_path.exists` (L45); `config_path.read_text` (L45); `destination.exists` (L47); `(destination / '.personal-memory-managed').is_file` (L47); `ValueError` (L48); `destination.parent.mkdir` (L49); `Path` (L50); `tempfile.mkdtemp` (L50); `shutil.copytree` (L52); `Path` (L52); `shutil.ignore_patterns` (L53); `(stage / '__init__.py').write_text` (L54); `(stage / 'plugin.yaml').write_text` (L55); `shutil.copyfile` (L56); `Path` (L56); `(stage / '.personal-memory-managed').touch` (L57); `destination.with_name` (L58); `backup.exists` (L59); `shutil.rmtree` (L60); `destination.exists` (L61); `destination.rename` (L62); `stage.rename` (L63); `backup.exists` (L64); `shutil.rmtree` (L65); `stage.exists` (L67); `shutil.rmtree` (L68); `atomic_json` (L69); `public_path.exists` (L71); `atomic_json` (L73); `settings.get` (L73); `settings.get` (L74); `default_config` (L75); `json.loads` (L78); `public_path.read_text` (L78); `dict` (L80); `merged.update` (L80); `normalize` (L80); `public['retrieval']['hindsight'].pop` (L82); `atomic_json` (L83); `dict` (L84); `config.get` (L84); `atomic_json` (L94); `config_path.with_suffix` (L96); `os.open` (L97); `os.fdopen` (L98); `yaml.safe_dump` (L99); `stream.flush` (L100); `os.fsync` (L100); `stream.fileno` (L100); `os.replace` (L101); `launcher.write_text` (L103); `repr` (L103); `str` (L103); `repr` (L105); `str` (L105); `str` (L106); `str` (L106)
- **rollback** (L112): `Path(home).expanduser().resolve` (L114); `Path(home).expanduser` (L114); `Path` (L114); `json.loads` (L115); `(home / 'personal-memory' / 'rollback.json').read_text` (L115); `yaml.safe_load` (L117); `path.read_text` (L117); `config.get` (L118); `ValueError` (L119); `config.pop` (L121); `path.with_suffix` (L124); `temp.write_text` (L125); `yaml.safe_dump` (L125); `temp.chmod` (L125); `os.replace` (L126)

## personal_memory/skill_export.py

Explicit administrator export of an active evaluated procedural lesson.

- **_export_skill** (L9): `re.fullmatch` (L10); `ValueError` (L10); `client.call` (L11); `payload.get` (L13); `ValueError` (L13); `yaml.safe_dump` (L15); `json.dumps` (L19); `json.dumps` (L19); `Path(home).resolve` (L20); `Path` (L20); `registry.exists` (L22); `json.loads` (L22); `registry.read_text` (L22); `str` (L23); `target.relative_to` (L23); `entries.get` (L23); `target.resolve().is_relative_to` (L24); `target.resolve` (L24); `target.is_symlink` (L24); `ValueError` (L24); `target.exists` (L25); `hashlib.sha256(target.read_bytes()).hexdigest` (L25); `hashlib.sha256` (L25); `target.read_bytes` (L25); `previous.get` (L25); `ValueError` (L26); `ValueError` (L27); `target.parent.mkdir` (L28); `digest` (L31); `hashlib.sha256(content.encode()).hexdigest` (L32); `hashlib.sha256` (L32); `content.encode` (L32); `atomic_json` (L33); `atomic_write` (L36); `content.encode` (L36); `str` (L37)
- **verify** (L40): `Path(home).resolve` (L41); `Path` (L41); `Path(path).resolve` (L41); `Path` (L41); `path.is_relative_to` (L42); `registry.exists` (L44); `json.loads(registry.read_text()).get` (L45); `json.loads` (L45); `registry.read_text` (L45); `str` (L45); `path.relative_to` (L45); `hashlib.sha256(content.encode()).hexdigest` (L46); `hashlib.sha256` (L46); `content.encode` (L46); `client.call` (L47); `digest` (L48)
- **export_skill** (L52): `closing` (L55); `ProcessLease` (L55); `Path` (L55); `_export_skill` (L56)

## personal_memory/source_adapters.py

Bundled source adapters that wrap the existing deterministic importers.

- **EmailExportAdapter.__init__** (L25): `required_text` (L28); `max` (L29); `min` (L29); `int` (L29); `adapter_spec` (L30)
- **EmailExportAdapter.spec** (L33): `dict` (L34)
- **EmailExportAdapter._observed_at** (L36): `os.path.getmtime` (L37); `datetime.fromtimestamp(modified, tz=timezone.utc).isoformat` (L38); `datetime.fromtimestamp` (L38)
- **EmailExportAdapter.check** (L40): `os.path.exists` (L41); `AdapterError` (L42); `str` (L44)
- **EmailExportAdapter.discover** (L46): `stream_spec` (L47)
- **EmailExportAdapter._records** (L49): `self._observed_at` (L52); `parse_emails` (L53); `adapt_existing` (L54); `os.path.basename` (L55); `str` (L55)
- **EmailExportAdapter.read_page** (L58): `(state['cursor'] or {}).get` (L59); `(state['cursor'] or {}).get` (L60); `source_page` (L61); `read_state` (L61); `enumerate` (L65); `self._records` (L65); `len` (L68); `window.append` (L70); `source_operation` (L73); `len` (L75); `source_page` (L77); `read_state` (L79)
- **EmailExportAdapter.normalize** (L85): `adapt_existing` (L86); `os.path.basename` (L89); `str` (L89); `self._observed_at` (L90); `normalized_item` (L91)

## personal_memory/source_runtime.py

Service-owned ingestion loop: fair historical/live passes and persistent retry times.

- **SourceRuntime.__init__** (L19): `SecretStore` (L21); `Path` (L21); `GmailAdapter` (L22); `SourceSync` (L23); `installed.spec` (L25); `ValueError` (L26); `self.sync.register` (L27); `SyncWorker` (L28); `threading.Event` (L29); `threading.Lock` (L30); `threading.Lock` (L30)
- **SourceRuntime.start** (L33): `self.config.get` (L34); `self.thread.is_alive` (L36); `self.stop.clear` (L37); `threading.Thread` (L38); `self.thread.start` (L39)
- **SourceRuntime.close** (L41): `self.stop.set` (L42); `self.thread.join` (L43)
- **SourceRuntime.connect_gmail** (L45): `isinstance` (L46); `set` (L46); `ValueError` (L47); `isinstance` (L49); `credentials.get` (L49); `len` (L49); `ValueError` (L50); `type` (L51); `ValueError` (L52); `date.fromisoformat(after).isoformat` (L55); `date.fromisoformat` (L55); `json.dumps` (L56); `connection_context` (L57); `self.adapter.check` (L59); `digest` (L60); `profile['account_id'].casefold` (L60); `self.secrets.put` (L61); `self.store.connect` (L64); `db.execute('SELECT scope FROM source_connections WHERE adapter_id=? AND source=?', ('google.gmail', source)).fetchone` (L65); `db.execute` (L65); `json.loads(old[0]).get` (L66); `json.loads` (L66); `json.loads` (L67); `self.sync.configure` (L68); `self.start` (L70)
- **SourceRuntime.status** (L74): `self.store.connect` (L75); `db.execute` (L76); `ValueError` (L78); `self.sync.status` (L82); `status.pop` (L82); `self.store.connect` (L83); `db.execute("SELECT count(*) FROM source_heads WHERE connection_id=? AND record_id!=''", (cid,)).fetchone` (L84); `db.execute` (L84); `db.execute` (L85); `dict` (L86); `db.execute` (L86); `values.append` (L88); `bool` (L89); `self.thread.is_alive` (L89)
- **SourceRuntime.control** (L91): `ValueError` (L92); `self.store.connect` (L94); `self.sync._connection` (L95); `db.execute` (L96); `db.execute` (L97); `now` (L97); `getattr(self.sync, action)` (L99); `getattr` (L99); `self._schedule` (L103); `self.discovered.pop` (L104); `self.start` (L105)
- **SourceRuntime._schedule** (L108): `self.store.connect` (L109); `db.execute('SELECT failures FROM source_schedule WHERE connection_id=? AND role=?', (cid, role)).fetchone` (L112); `db.execute` (L112); `max` (L113); `min` (L113); `min` (L113); `random.uniform` (L113); `db.execute` (L114); `time.time` (L118)
- **SourceRuntime._due** (L120): `self.store.connect` (L121); `db.execute('SELECT next_at FROM source_schedule WHERE connection_id=? AND role=?', (cid, role)).fetchone` (L122); `db.execute` (L122); `time.time` (L123)
- **SourceRuntime._schedule_row** (L125): `self.store.connect` (L126); `db.execute('SELECT next_at,config_key,error FROM source_schedule WHERE connection_id=? AND role=?', (cid, role)).fetchone` (L127); `db.execute` (L127)
- **SourceRuntime._declarations** (L129): `connection.get` (L140); `connection.get` (L140); `connection.get` (L140); `digest` (L141); `list` (L141); `self._schedule_row` (L142); `time.time` (L145); `self.discovered.get` (L147); `self.discovered.pop` (L158); `adapter.discover` (L160); `self.sync.context` (L160); `self.sync._set_state` (L162); `self._schedule` (L163); `self._schedule` (L166); `type` (L166); `min` (L172); `max` (L172); `int` (L172); `connection['scope'].get` (L172); `self._schedule` (L173)
- **SourceRuntime._recover_cursor** (L176): `self.sync.verify` (L178); `self.store.connect` (L179); `db.execute` (L180); `self.sync._connection` (L181); `json.loads` (L181); `json.dumps` (L182); `db.execute` (L183); `digest` (L183); `db.execute` (L184); `db.execute` (L185); `db.execute` (L186); `now` (L186)
- **SourceRuntime._in_backoff** (L188): `self.store.connect` (L191); `db.execute('SELECT error FROM source_schedule WHERE connection_id=? AND role=?', (cid, role)).fetchone` (L192); `db.execute` (L192)
- **SourceRuntime.tick** (L195): `self.tick_lock.acquire` (L196); `self.store.connect` (L198); `db.execute` (L199); `self.sync.connection` (L201); `self.sync.registry.get` (L202); `self.sync.take_signals` (L208); `self._declarations` (L209); `stream.get` (L213); `connection['scope'].get` (L216); `passes.sort` (L217); `self.stop.is_set` (L225); `json.dumps` (L228); `self._due` (L229); `self._in_backoff` (L233); `self.sync.stream_state` (L235); `(state['cursor'] or {}).get` (L236); `self.sync.stream_state` (L238); `(historical['cursor'] or {}).get` (L239); `(state['cursor'] or {}).get` (L240); `self.sync.restart_stream` (L241); `(state['cursor'] or {}).get` (L243); `(state['cursor'] or {}).get` (L243); `self.sync.verify` (L244); `self.worker.run_once` (L245); `self._recover_cursor` (L250); `self._schedule` (L251); `self._schedule` (L255); `max` (L255); `result.get` (L255); `result.get` (L255); `bool` (L260); `result.get` (L260); `self._schedule` (L262); `connection['scope'].get` (L263); `self.sync.stream_state` (L265); `cursor.get` (L266); `self._schedule` (L267); `self._schedule` (L269); `self.sync._set_state` (L272); `self._schedule` (L273); `self._schedule` (L276); `type` (L276); `all` (L277); `covered.get` (L277); `self.sync.ack_signals` (L282); `self.stop.is_set` (L283); `self._attachment` (L283); `self.tick_lock.release` (L284)
- **SourceRuntime._attachment** (L286): `self.sync.claim_job` (L287); `self.store.connect` (L294); `db.execute('SELECT 1 FROM records WHERE id=? AND deleted=0 AND NOT EXISTS(SELECT 1 FROM record_visibility WHERE record_id=? AND hidden=1)', (rid, rid)).fetchone` (L295); `db.execute` (L295); `self.sync.complete_job` (L297); `self.sync.connection` (L298); `adapter.attachment` (L300); `self.sync.context` (L300); `len` (L301); `descriptor.get` (L301); `len` (L301); `ValueError` (L302); `hashlib.sha256(raw).hexdigest` (L303); `hashlib.sha256` (L303); `self.store.connect` (L306); `self.sync._job_guard` (L307); `db.execute('SELECT 1 FROM record_visibility WHERE record_id=? AND hidden=1', (rid,)).fetchone` (L308); `db.execute` (L308); `ValueError` (L309); `blobs.begin` (L310); `len` (L311); `enumerate` (L313); `range` (L313); `len` (L313); `self.stop.is_set` (L314); `AdapterError` (L314); `blobs.put` (L316); `base64.b64encode(raw[start:start + blobs.CHUNK_SIZE]).decode` (L316); `base64.b64encode` (L316); `blobs.complete` (L317); `self.sync.complete_job` (L318); `self.sync.fail_job` (L320); `type` (L320)
- **SourceRuntime._run** (L323): `self.stop.is_set` (L324); `self.tick` (L325); `LOG.error` (L327); `type` (L327); `self.stop.wait` (L328)

Module/class/lambda calls: `<module> → logging.getLogger` (L15)

## personal_memory/source_sdk.py

Versioned source-adapter protocol, independent of record schema 1.0.

- **AdapterError.__init__** (L26): `ValueError` (L28); `required_text` (L30); `type` (L31); `super().__init__` (L32); `super` (L32)
- **_text** (L35): `isinstance` (L36); `value.strip` (L36); `len` (L36); `ContractError` (L37)
- **_flag** (L41): `isinstance` (L42); `ContractError` (L43)
- **adapter_spec** (L47): `isinstance` (L52); `ContractError` (L53); `set` (L55); `set` (L55); `ContractError` (L57); `sorted` (L57); `_text` (L58); `len` (L59); `set` (L59); `len` (L59); `ContractError` (L60); `_text` (L61); `_text` (L62); `_text` (L63); `bool` (L64); `declared.get` (L64)
- **adapter_supports** (L68): `ValueError` (L70); `bool` (L71); `spec['capabilities'].get` (L71)
- **connection_context** (L74): `callable` (L82); `_text` (L85); `_text` (L86); `copy.deepcopy` (L87); `_text` (L88); `_text` (L89)
- **stream_spec** (L92): `ContractError` (L95); `set` (L96); `set` (L96); `ContractError` (L98); `sorted` (L98); `isinstance` (L101); `ContractError` (L102); `parts.append` (L103); `_text` (L103); `str` (L104); `partition.get` (L104); `len` (L105); `len` (L105); `ContractError` (L106); `_text` (L107); `_text` (L111); `_text` (L113); `_text` (L115)
- **read_state** (L118): `ContractError` (L120); `type` (L121); `ContractError` (L122); `len` (L125); `json.dumps` (L125); `ContractError` (L126); `ContractError` (L128); `copy.deepcopy` (L129); `str` (L130); `_text` (L132)
- **source_operation** (L135): `ContractError` (L139); `validate_record` (L142); `_text` (L143); `ContractError` (L144); `normalized.append` (L145); `ContractError` (L147); `ContractError` (L149); `_text` (L150); `_text` (L152); `str` (L152); `copy.deepcopy` (L153); `_text` (L154); `copy.deepcopy` (L155); `copy.deepcopy` (L156); `list` (L156)
- **normalized_item** (L159): `validate_record` (L160); `_text` (L161); `str` (L162); `copy.deepcopy` (L163); `list` (L163); `copy.deepcopy` (L164); `list` (L164)
- **source_page** (L167): `type` (L175); `ContractError` (L176); `set` (L177); `ContractError` (L180); `observed_ids.add` (L181); `isinstance` (L184); `set` (L184); `ContractError` (L185); `ContractError` (L187); `observations.append` (L188); `_text` (L188); `str` (L188); `_text` (L189); `str` (L189); `str` (L190); `entry.get` (L190); `_text` (L191); `copy.deepcopy` (L192); `list` (L192); `bool` (L193); `bool` (L194)
- **validate_page** (L197): `isinstance` (L199); `ContractError` (L200); `set` (L201); `ContractError` (L203); `sorted` (L203); `ContractError` (L206); `_text` (L207); `_json` (L209); `len` (L210); `json.dumps(page, ensure_ascii=False).encode` (L210); `json.dumps` (L210); `ContractError` (L211); `type` (L212); `ContractError` (L213); `type` (L214); `ContractError` (L215); `isinstance` (L216); `len` (L216); `ContractError` (L217); `set` (L218); `enumerate` (L219); `isinstance` (L220); `ContractError` (L221); `set` (L222); `ContractError` (L225); `sorted` (L225); `ContractError` (L227); `source_operation` (L228); `ContractError` (L230); `seen.add` (L231); `isinstance` (L232); `isinstance` (L232); `ContractError` (L233); `len` (L234); `ContractError` (L235); `isinstance` (L237); `ContractError` (L238); `set` (L240); `set` (L240); `read_state` (L240); `ContractError` (L242); `sorted` (L242); `ContractError` (L244); `read_state` (L245); `source_page` (L246); `page.get` (L247); `page.get` (L248)
- **page_digest** (L257): `digest` (L260); `page.get` (L260)
- **SourceAdapter.spec** (L268): No direct calls.
- **SourceAdapter.check** (L271): No direct calls.
- **SourceAdapter.discover** (L274): No direct calls.
- **SourceAdapter.read_page** (L277): No direct calls.
- **SourceAdapter.normalize** (L280): No direct calls.
- **SourceAdapter.accept_state** (L282): `AdapterError` (L287); `self.migrate_state` (L289)
- **SourceAdapter.fetch_attachment** (L292): `AdapterError` (L293)
- **SourceAdapter.attachment** (L295): `self.fetch_attachment` (L297)
- **SourceAdapter.verify_event** (L299): `AdapterError` (L300)
- **SourceAdapter.maintain_subscription** (L302): `AdapterError` (L303)
- **SourceAdapter.migrate_state** (L305): `AdapterError` (L306)
- **_ConnectorAdapter.__init__** (L312): `adapter_spec` (L315)
- **_ConnectorAdapter.spec** (L321): `copy.deepcopy` (L322)
- **_ConnectorAdapter.check** (L324): No direct calls.
- **_ConnectorAdapter.discover** (L327): `stream_spec` (L328)
- **_ConnectorAdapter.read_page** (L330): `state['cursor'].get` (L331); `source_page` (L332); `read_state` (L332); `self._connector.records` (L335); `operations.append` (L336); `source_operation` (L336); `source_page` (L337); `read_state` (L338)
- **_ConnectorAdapter.normalize** (L341): `validate_record` (L342); `normalized_item` (L343)
- **wrap_ingestion_connector** (L346): `_ConnectorAdapter` (L349)

Module/class/lambda calls: `connection_context.<lambda@83> → (_ for _ in ()).throw` (L83); `connection_context.<lambda@83> → AdapterError` (L84)

## personal_memory/source_secrets.py

Host-side protected storage for adapter credentials.

- **SecretStore.__init__** (L16): `Path` (L18)
- **SecretStore._load** (L20): `self.path.exists` (L21); `json.loads` (L23); `self.path.read_text` (L23)
- **SecretStore._name** (L26): `required_text` (L27); `any` (L28); `name.startswith` (L28); `ValueError` (L29)
- **SecretStore.put** (L32): `self._name` (L33); `isinstance` (L34); `len` (L34); `ValueError` (L35); `self._load` (L36); `atomic_json` (L38); `self.path.chmod` (L40)
- **SecretStore.resolve** (L45): `isinstance` (L46); `reference.startswith` (L46); `KeyError` (L47); `self._load().get` (L48); `self._load` (L48); `len` (L48); `KeyError` (L50); `len` (L50)
- **SecretStore.resolver** (L53): No direct calls.
- **SecretStore.names** (L58): `sorted` (L59); `self._load` (L59)
- **SecretStore.delete** (L61): `self._name` (L62); `self._load` (L63); `data.pop` (L64); `atomic_json` (L65)
- **SecretStore.__repr__** (L67): `len` (L68); `self._load` (L68)

## personal_memory/source_sync.py

Durable source-sync state: connections, streams, leases, receipts, heads, coverage, jobs.

- **_iso_or_none** (L81): `timestamp` (L82)
- **SourceSync.__init__** (L89): `dict` (L91)
- **SourceSync.register** (L94): `adapter.spec` (L95)
- **SourceSync._adapter** (L99): `self.registry.get` (L100); `ValueError` (L102)
- **SourceSync._connection** (L105): `db.execute('SELECT * FROM source_connections WHERE id=?', (connection_id,)).fetchone` (L106); `db.execute` (L106); `ValueError` (L108)
- **SourceSync.configure** (L111): `self._adapter` (L113); `isinstance` (L114); `ValueError` (L115); `scope.get` (L116); `type` (L117); `ValueError` (L118); `ValueError` (L120); `ValueError` (L122); `json.dumps` (L123); `required_text` (L124); `digest` (L125); `self.store.connect` (L126); `db.execute` (L127); `db.execute('SELECT * FROM source_connections WHERE adapter_id=? AND source=?', (adapter_id, source)).fetchone` (L128); `db.execute` (L128); `db.execute` (L132); `digest` (L134); `now` (L134); `db.execute` (L135); `db.execute` (L137); `db.execute` (L138); `db.execute` (L140); `digest` (L144); `json.dumps` (L144); `now` (L145); `now` (L145); `self.store.audit` (L146)
- **SourceSync.connection** (L150): `self.store.connect` (L151); `dict` (L152); `self._connection` (L152); `json.loads` (L153)
- **SourceSync.verify** (L156): `self.store.connect` (L159); `self._connection` (L160); `self.context` (L161); `dict` (L161); `self._adapter(row['adapter_id']).check` (L163); `self._adapter` (L163); `json.loads(row['scope']).get` (L164); `json.loads` (L164); `result.get` (L165); `AdapterError` (L166); `self._set_state` (L169)
- **SourceSync.context** (L173): `isinstance` (L175); `json.loads` (L175); `connection.get` (L176); `connection_context` (L183); `hasattr` (L186)
- **SourceSync.context.resolve** (L177): `AdapterError` (L179); `self.secrets.resolve` (L180); `AdapterError` (L182)
- **SourceSync._set_state** (L189): `self.store.connect` (L190); `db.execute` (L191); `self._connection` (L192); `db.execute` (L193); `now` (L194)
- **SourceSync.pause** (L196): `self.store.connect` (L197); `db.execute` (L198); `self._connection` (L199); `db.execute` (L200); `now` (L201); `db.execute` (L202); `db.execute` (L203); `now` (L205); `now` (L205)
- **SourceSync.resume** (L208): `self._set_state` (L209)
- **SourceSync.disconnect** (L212): `self.store.connect` (L215); `db.execute` (L216); `self._connection` (L217); `db.execute` (L218); `now` (L219); `db.execute` (L220); `db.execute` (L222); `now` (L223); `self.store.audit` (L224)
- **SourceSync.register_trigger** (L227): `required_text` (L230); `self.store.connect` (L231); `self._connection` (L232); `self._adapter(row['adapter_id']).spec` (L233); `self._adapter` (L233); `{'events': 'events', 'subscriptions': 'subscriptions'}.get` (L234); `spec['capabilities'].get` (L235); `ValueError` (L236); `self.store.connect` (L237); `self.store.audit` (L238)
- **SourceSync.claim** (L243): `ValueError` (L247); `required_text` (L248); `self.store.connect` (L249); `db.execute` (L250); `self._connection` (L251); `ValueError` (L253); `db.execute` (L254); `now` (L257); `db.execute('SELECT * FROM source_streams WHERE connection_id=? AND stream=? AND partition=? AND role=?', (connection_id, stream, partition, role)).fetchone` (L258); `db.execute` (L258); `now` (L260); `ValueError` (L262); `(datetime.now(timezone.utc) + timedelta(seconds=ttl)).isoformat` (L266); `datetime.now` (L266); `timedelta` (L266); `db.execute` (L267); `now` (L269); `json.loads` (L274); `reset.epoch` (L277)
- **SourceSync.release** (L279): `self.store.connect` (L280); `db.execute` (L281); `db.execute` (L282); `now` (L285); `ValueError` (L288)
- **SourceSync.stream_state** (L291): `self.store.connect` (L292); `db.execute('SELECT * FROM source_streams WHERE connection_id=? AND stream=? AND partition=? AND role=?', (connection_id, stream, partition, role)).fetchone` (L293); `db.execute` (L293); `json.loads` (L297)
- **SourceSync.restart_stream** (L303): `self.store.connect` (L307); `db.execute` (L308); `self._connection` (L309); `ValueError` (L311); `db.execute` (L312); `now` (L314); `self.store.audit` (L315)
- **SourceSync.commit_page** (L321): `required_text` (L326); `validate_page` (L327); `list` (L328); `digest` (L329); `page_digest` (L329); `self.connection` (L331); `self._version_order` (L332); `bool` (L334); `self.store.connect` (L335); `db.execute` (L336); `self._connection` (L337); `db.execute('SELECT * FROM source_streams WHERE connection_id=? AND stream=? AND partition=? AND role=?', (lease['connection_id'], lease['stream'], lease['partition'], lease['role'])).fetchone` (L338); `db.execute` (L338); `ValueError` (L341); `now` (L345); `ValueError` (L346); `ValueError` (L348); `ValueError` (L350); `reset.epoch` (L352); `ValueError` (L353); `db.execute('SELECT page_digest,outcome FROM source_page_receipts WHERE op_id=?', (op_id,)).fetchone` (L354); `db.execute` (L354); `ValueError` (L357); `json.loads` (L358); `ValueError` (L362); `json.loads` (L363); `self._apply_operation` (L365); `changes.journal_stream_lifecycle` (L367); `page.get` (L368); `db.execute` (L369); `now` (L372); `changes.journal_gap` (L374); `entry.get` (L374); `required_text` (L376); `bad.get` (L376); `str` (L377); `bad.get` (L377); `db.execute` (L378); `now` (L382); `self._enqueue_job` (L383); `changes.journal_gap` (L386); `self._enqueue_item_obligations` (L387); `db.execute` (L389); `json.dumps` (L391); `now` (L392); `now` (L392); `isinstance` (L395); `next_cursor.get` (L395); `changes.journal_stream_lifecycle` (L396); `dict` (L397); `db.execute` (L398); `json.dumps` (L400); `now` (L400)
- **SourceSync._version_order** (L406): `self._adapter(connection['adapter_id']).discover` (L410); `self._adapter` (L410); `self.context` (L410); `declared.get` (L414); `ValueError` (L417)
- **SourceSync._newer** (L419): `_ROLE_RANK.get` (L424); `_ROLE_RANK.get` (L424); `int` (L427); `int` (L427); `timestamp` (L432); `timestamp` (L432); `_ROLE_RANK.get` (L437); `_ROLE_RANK.get` (L437)
- **SourceSync._apply_operation** (L439): `db.execute('SELECT * FROM source_heads WHERE source=? AND source_id=?', (source, source_id)).fetchone` (L443); `db.execute` (L443); `self._newer` (L452); `operation.get` (L452); `db.execute` (L453); `operation.get` (L455); `now` (L456); `db.execute` (L460); `operation.get` (L461); `now` (L461); `self.store.audit` (L462); `changes.journal_state_change` (L463); `self._newer` (L468); `operation.get` (L468); `ValueError` (L472); `db.execute` (L473); `now` (L475); `operation.get` (L475); `sorted` (L477); `self._current` (L477); `self._retire` (L480); `db.executemany` (L482); `semantic.enqueue` (L486); `sorted` (L486); `self.store.audit` (L487); `changes.journal_state_change` (L488); `ValueError` (L496); `self._is_tombstoned` (L501); `self.store._apply_ingest_item` (L513); `applied_rows.append` (L514); `record_ids.append` (L515); `results['records'].extend` (L519); `self._current` (L521); `self._newer` (L522); `operation.get` (L522); `db.execute('SELECT metadata FROM source_item_metadata WHERE source=? AND source_id=?', (source, source_id)).fetchone` (L525); `db.execute` (L525); `set` (L528); `self._retire` (L528); `db.executemany` (L529); `semantic.enqueue` (L531); `db.execute` (L532); `db.executemany` (L533); `db.execute` (L534); `operation.get` (L536); `now` (L536); `db.execute` (L537); `json.dumps` (L538); `operation.get` (L538); `set` (L539); `self.store.audit` (L540); `changes.journal_operation_transition` (L541); `set` (L544); `self._retire` (L544)
- **SourceSync._record_id** (L549): `digest` (L550)
- **SourceSync._is_tombstoned** (L552): `self.store.deletions.contains` (L554); `self.store.source_key` (L555); `self.store.deletions.contains` (L557); `self._record_id` (L557)
- **SourceSync._current** (L561): `db.execute` (L562); `set` (L563)
- **SourceSync._retire** (L565): `lifecycle.retire` (L567)
- **SourceSync._enqueue_item_obligations** (L569): `results.get` (L571); `isinstance` (L573); `entry.get` (L573); `any` (L574); `self._record_id` (L574); `isinstance` (L579); `entry.get` (L579); `entry.get` (L581); `self._record_id` (L585); `db.execute('SELECT 1 FROM record_visibility WHERE record_id=? AND hidden=1', (rid,)).fetchone` (L586); `db.execute` (L586); `descriptor.get` (L587); `descriptor.get` (L587); `ValueError` (L588); `self._enqueue_job` (L589); `digest` (L590); `entry.get` (L592); `digest` (L593); `projection.get` (L593); `projection.get` (L594); `projection.get` (L594); `entry.get` (L594); `self._enqueue_job` (L595); `dict` (L595)
- **SourceSync._enqueue_job** (L598): `db.execute` (L599); `digest` (L603); `json.dumps` (L604); `now` (L604); `now` (L604); `now` (L604)
- **SourceSync.signal** (L608): `self.store.connect` (L611); `db.execute` (L612); `self._connection` (L613); `required_text` (L615); `db.execute('SELECT 1 FROM source_inbox WHERE connection_id=? AND event_id=?', (connection_id, event_id)).fetchone` (L616); `db.execute` (L616); `db.execute` (L619); `json.dumps` (L621); `now` (L622)
- **SourceSync.pending_signals** (L625): `self.store.connect` (L626); `db.execute('SELECT COUNT(*) FROM source_inbox WHERE connection_id=? AND served=0', (connection_id,)).fetchone` (L627); `db.execute` (L627)
- **SourceSync.take_signals** (L630): `self.store.connect` (L632); `db.execute` (L633); `db.execute('SELECT COUNT(*) AS n, MAX(id) AS top FROM source_inbox WHERE connection_id=? AND served=0', (connection_id,)).fetchone` (L634); `db.execute` (L634)
- **SourceSync.ack_signals** (L638): `self.store.connect` (L640); `db.execute` (L641); `int` (L642)
- **SourceSync.claim_job** (L647): `required_text` (L650); `self.store.connect` (L651); `db.execute` (L652); `now` (L653); `','.join` (L658); `len` (L658); `arguments.extend` (L659); `db.execute("SELECT * FROM source_jobs WHERE connection_id IN (SELECT id FROM source_connections WHERE state='active') AND ((state IN ('pending','retry_wait') AND available_at<=?) OR (state='leased' AND lease_until<=?))" + scope_sql + kind_sql + ' ORDER BY created_at LIMIT 1', [moment, moment] + arguments).fetchone` (L660); `db.execute` (L660); `db.execute("SELECT 1 FROM source_jobs WHERE state='leased' AND lease_until>?" + scope_sql + kind_sql, [moment] + arguments).fetchone` (L665); `db.execute` (L665); `ValueError` (L668); `(datetime.now(timezone.utc) + timedelta(seconds=ttl)).isoformat` (L671); `datetime.now` (L671); `timedelta` (L671); `db.execute` (L672); `now` (L673); `dict` (L674); `job.update` (L675); `json.loads` (L676)
- **SourceSync._job_guard** (L679): `job.get` (L680); `job.get` (L680); `db.execute` (L681); `now` (L683); `now` (L683); `ValueError` (L685)
- **SourceSync.complete_job** (L687): `self.store.connect` (L688); `db.execute` (L689); `self._job_guard` (L690); `db.execute` (L691); `json.dumps` (L692)
- **SourceSync.fail_job** (L695): `(datetime.now(timezone.utc) + timedelta(seconds=0 if quarantine else max(0, retry_after or 0))).isoformat` (L697); `datetime.now` (L697); `timedelta` (L697); `max` (L697); `self.store.connect` (L698); `db.execute` (L699); `self._job_guard` (L700); `db.execute` (L701); `json.dumps` (L703); `str` (L703)
- **SourceSync.head** (L708): `self.store.connect` (L709); `db.execute('SELECT * FROM source_heads WHERE source=? AND source_id=?', (source, source_id)).fetchone` (L710); `db.execute` (L710); `dict` (L712)
- **SourceSync.coverage** (L714): `self.store.connect` (L715); `db.execute('SELECT start,end,state,note,generation FROM source_coverage WHERE connection_id=? AND stream=? AND partition=? ORDER BY id', (connection_id, stream, partition)).fetchall` (L716); `db.execute` (L716); `dict` (L719)
- **SourceSync.status** (L721): `self.store.connect` (L722); `dict` (L723); `self._connection` (L723); `dict` (L724); `db.execute` (L724); `json.loads` (L728); `json.loads` (L729); `max` (L731)
- **SourceSync.pending_jobs** (L734): `','.join` (L738); `len` (L738); `arguments.extend` (L739); `arguments.append` (L741); `self.store.connect` (L742); `db.execute(query, arguments).fetchall` (L743); `db.execute` (L743); `dict` (L744); `json.loads` (L746)
- **SyncWorker.__init__** (L755): `uuid.uuid4` (L757)
- **SyncWorker._context** (L759): `self.sync.context` (L760)
- **SyncWorker.run_once** (L762): `self.sync.claim` (L766); `self.sync.connection` (L768); `self.sync._adapter` (L769); `adapter.discover` (L773); `self._context` (L773); `next` (L774); `AdapterError` (L776); `selected.get` (L779); `AdapterError` (L782); `AdapterError` (L784); `read_state` (L785); `adapter.read_page` (L786); `self._context` (L786); `digest` (L787); `self.sync.commit_page` (L789); `bool` (L790); `bool` (L792); `page.get` (L792); `self.sync._set_state` (L798); `self.sync.release` (L808)

## personal_memory/storage.py

Fresh-install SQLite naming compatible with Hermes native backup detection.

- **database_path** (L5): `Path` (L6)

## personal_memory/store.py

Canonical records and derived claims. No LLM extraction runs implicitly.

- **Store.__init__** (L15): `Path` (L16); `path.parent.mkdir` (L17); `threading.RLock` (L19); `DeletionLedger` (L21); `path.with_name` (L21); `self.connect` (L22); `db.execute` (L23); `db.execute('PRAGMA user_version').fetchone` (L24); `db.execute` (L24); `RuntimeError` (L26); `db.executescript` (L27); `db.executescript` (L57); `db.executescript` (L58); `db.execute` (L71); `db.execute` (L72); `db.execute` (L73); `db.execute` (L74); `db.executescript` (L76); `db.executescript` (L78); `db.executescript` (L81); `db.execute` (L82); `db.execute` (L82); `db.executescript` (L83); `db.executescript` (L85); `db.executescript` (L87); `semantic.ensure_schema` (L91); `db.execute` (L94); `db.execute` (L95); `changes.ensure` (L97); `awareness.ensure` (L99); `db.execute` (L101); `now` (L101); `db.execute` (L102); `db.execute` (L103); `path.chmod` (L104); `self.deletions.ids` (L105); `rid.startswith` (L106); `rid.startswith` (L108); `rid.split` (L109); `rid.startswith` (L110); `self.connect` (L111); `db.execute` (L112); `rid.startswith` (L114); `self.connect` (L116); `invalidate` (L116); `self.connect` (L118); `db.execute('SELECT 1 FROM records WHERE id=? AND deleted=0', (rid,)).fetchone` (L118); `db.execute` (L118); `self._forget` (L119); `self.deletions.ids` (L120); `rid.startswith` (L120); `self.connect` (L126); `db.execute('SELECT id,source,source_id FROM records WHERE deleted=0 AND id>? ORDER BY id LIMIT 500', (cursor,)).fetchall` (L127); `db.execute` (L127); `self.source_key` (L130); `self._forget` (L131); `initialize_reset` (L134); `lifecycle.apply_upgrade` (L139)
- **Store.connect** (L142): `sqlite3.connect` (L143); `db.execute` (L145); `db.execute` (L146); `db.execute` (L147); `db.close` (L152)
- **Store.audit** (L154): `db.execute` (L155); `now` (L156); `json.dumps` (L156)
- **Store.ingest_contract** (L158): `validate_batch` (L160); `normalized.append` (L163); `self.ingest` (L168)
- **Store._receipt** (L170): `db.execute('SELECT 1 FROM records WHERE id=? AND deleted=0', (parent,)).fetchone` (L173); `db.execute` (L173); `ValueError` (L174); `db.execute` (L175); `db.execute` (L178); `digest` (L179); `json.dumps` (L179)
- **Store.ingest** (L181): `isinstance` (L182); `len` (L182); `ValueError` (L183); `self.connect` (L185); `db.execute` (L186); `isinstance` (L188); `set` (L188); `ValueError` (L189); `required_text` (L190); `required_text` (L191); `json.dumps` (L192); `len` (L193); `ValueError` (L193); `item.get('_contract', {}).get('provenance', {}).get` (L195); `item.get('_contract', {}).get` (L195); `item.get` (L195); `ValueError` (L196); `digest` (L197); `item.items` (L197); `db.execute('SELECT cursor,batch_hash FROM connector_checkpoints WHERE connector_id=? AND source=?', (checkpoint['connector_id'], checkpoint['source'])).fetchone` (L198); `db.execute` (L198); `json.loads` (L200); `ValueError` (L202); `ValueError` (L204); `db.execute` (L206); `self._apply_ingest_item` (L209); `results.append` (L210); `item.get` (L213); `contract.get` (L214); `changes.append` (L215); `str` (L216); `provenance.get` (L216); `item.get` (L221); `db.execute` (L223); `now` (L224)
- **Store._apply_ingest_item** (L227): `isinstance` (L233); `ValueError` (L234); `required_text` (L235); `item.get` (L235); `required_text` (L236); `item.get` (L236); `self.deletions.contains` (L237); `self.source_key` (L237); `ValueError` (L238); `required_text` (L239); `item.get` (L239); `required_text` (L240); `item.get` (L240); `item.get` (L241); `item.get` (L241); `timestamp` (L241); `item.get` (L241); `required_text` (L242); `item.get` (L242); `item.get` (L243); `isinstance` (L244); `ValueError` (L245); `digest` (L246); `db.execute('SELECT id,fingerprint,deleted FROM records WHERE source=? AND source_id=? AND revision=?', (source, source_id, revision)).fetchone` (L247); `db.execute` (L247); `ValueError` (L251); `ValueError` (L253); `self._receipt` (L254); `item.get` (L254); `digest` (L256); `self.deletions.contains` (L257); `ValueError` (L257); `db.execute` (L258); `now` (L259); `json.dumps` (L260); `db.execute` (L261); `self._participants` (L262); `self._receipt` (L263); `item.get` (L263); `db.execute` (L264); `now` (L264); `self.audit` (L265); `semantic.enqueue` (L267)
- **Store.checkpoint** (L270): `self.connect` (L271); `db.execute('SELECT cursor,updated_at FROM connector_checkpoints WHERE connector_id=? AND source=?', (connector_id, source)).fetchone` (L272); `db.execute` (L272); `json.loads` (L273)
- **Store.lineage_status** (L275): `isinstance` (L276); `len` (L276); `ValueError` (L277); `required_text` (L279); `self.connect` (L280); `db.execute('SELECT deleted FROM records WHERE id=?', (rid,)).fetchone` (L283); `db.execute` (L283)
- **Store.source_key** (L288): `digest` (L289)
- **Store.source_status** (L291): `required_text` (L292); `isinstance` (L293); `len` (L293); `ValueError` (L294); `required_text` (L295); `self.deletions.contains_many` (L296); `self.source_key` (L296); `self.source_key` (L297)
- **Store.supersede** (L299): `self.connect` (L301); `db.execute` (L302); `db.execute('SELECT 1 FROM records WHERE id=? AND deleted=0', (record_id,)).fetchone` (L303); `db.execute` (L303); `ValueError` (L303); `db.execute('SELECT 1 FROM records WHERE id=? AND deleted=0', (replacement_id,)).fetchone` (L304); `db.execute` (L304); `ValueError` (L304); `lifecycle.retire` (L305); `self.audit` (L306); `len` (L307)
- **Store.forget_source** (L309): `required_text` (L310); `required_text` (L311); `self.deletions.append` (L313); `self.source_key` (L313); `self.connect` (L314); `db.execute` (L315); `set` (L316); `affected.update` (L318); `self._forget` (L318); `sorted` (L319)
- **Store.evidence** (L322): `isinstance` (L323); `ValueError` (L323); `self.connect` (L324); `db.execute('SELECT * FROM records WHERE id=? AND deleted=0', (record_id,)).fetchone` (L325); `db.execute` (L325); `ValueError` (L327); `dict` (L328); `json.loads` (L329); `len` (L333); `type` (L334); `type` (L334); `len` (L335); `len` (L335); `ValueError` (L335); `result.update` (L337); `len` (L337); `db.execute('SELECT envelope FROM ingestion_receipts WHERE record_id=? ORDER BY observed_at DESC LIMIT 1', (record_id,)).fetchone` (L342); `db.execute` (L342); `json.loads` (L344)
- **Store.entity** (L349): `required_text` (L350); `required_text` (L351); `required_text` (L352); `isinstance` (L353); `ValueError` (L354); `self.connect` (L355); `uuid.uuid4` (L356); `db.execute('SELECT * FROM entities WHERE id=?', (eid,)).fetchone` (L358); `db.execute` (L358); `ValueError` (L360); `self.audit` (L362); `dict` (L362); `db.execute` (L363); `int` (L364); `db.execute` (L366); `int` (L366); `now` (L366); `self.audit` (L367); `db.execute('SELECT 1 FROM records WHERE id=? AND deleted=0', (record_id,)).fetchone` (L369); `db.execute` (L369); `ValueError` (L370); `db.execute` (L371); `dict` (L372); `db.execute('SELECT * FROM entities WHERE id=?', (eid,)).fetchone` (L372); `db.execute` (L372)
- **Store.claim** (L374): `required_text` (L376); `required_text` (L377); `ValueError` (L379); `ValueError` (L381); `ValueError` (L383); `required_text` (L384); `timestamp` (L385); `timestamp` (L386); `ValueError` (L388); `self.connect` (L389); `db.execute` (L393); `digest` (L395); `lifecycle.require_live_evidence` (L398); `db.execute('SELECT * FROM claims WHERE id=?', (cid,)).fetchone` (L399); `db.execute` (L399); `dict` (L401); `db.execute('SELECT text FROM records WHERE id=?', (record_id,)).fetchone` (L402); `db.execute` (L402); `ValueError` (L403); `db.execute('SELECT 1 FROM entities WHERE id=?', (subject_id,)).fetchone` (L404); `db.execute` (L404); `ValueError` (L405); `db.execute("SELECT * FROM claims WHERE id=? AND status='active'", (supersedes,)).fetchone` (L407); `db.execute` (L407); `ValueError` (L409); `ValueError` (L411); `db.execute` (L412); `now` (L413); `db.execute` (L414); `db.execute` (L415); `db.execute` (L417); `db.execute` (L419); `self.audit` (L420); `dict` (L421); `db.execute('SELECT * FROM claims WHERE id=?', (cid,)).fetchone` (L421); `db.execute` (L421)
- **Store.query_terms** (L424): `required_text` (L425); `re.findall` (L426); `ValueError` (L428); `' OR '.join` (L429); `term.replace` (L429)
- **Store.search** (L431): `type` (L433); `ValueError` (L434); `self.query_terms` (L435); `clauses.append` (L437); `clauses.append` (L440); `params.append` (L440); `self.related_ids` (L442); `clauses.append` (L444); `clauses.append` (L446); `params.append` (L446); `timestamp` (L446); `clauses.append` (L448); `params.append` (L448); `timestamp` (L448); `clauses.append` (L450); `' AND '.join` (L451); `self.connect` (L452); `db.execute` (L454); `db.executemany` (L455); `dict` (L456); `db.execute` (L456); `dict` (L464); `db.execute` (L464); `self.status` (L470)
- **Store.timeline** (L473): `type` (L474); `ValueError` (L475); `self.connect` (L476); `db.execute('SELECT * FROM entities WHERE id=?', (entity_id,)).fetchone` (L477); `db.execute` (L477); `ValueError` (L479); `dict` (L480); `db.execute` (L480); `dict` (L484); `db.execute` (L484); `dict` (L486)
- **Store.entities** (L489): `required_text` (L490); `query.replace('\\', '\\\\').replace('%', '\\%').replace` (L492); `query.replace('\\', '\\\\').replace` (L492); `query.replace` (L492); `self.connect` (L493); `dict` (L495); `db.execute` (L495)
- **Store.forget** (L501): `self._forget` (L502)
- **Store._forget** (L504): `self.connect` (L505); `db.execute` (L506); `db.execute('SELECT 1 FROM records WHERE id=?', (record_id,)).fetchone` (L507); `db.execute` (L507); `ValueError` (L508); `db.execute` (L509); `self.deletions.append` (L512); `awareness.invalidate_record` (L516); `db.execute` (L517); `invalidate` (L519); `invalidate_curated` (L521); `db.execute` (L522); `db.execute` (L523); `ids.extend` (L524); `db.execute` (L526); `db.execute` (L527); `db.execute` (L528); `db.execute` (L529); `db.execute` (L530); `db.execute` (L531); `db.execute` (L532); `db.execute` (L533); `db.execute` (L534); `db.execute` (L535); `self.audit` (L536); `semantic.enqueue` (L538)
- **Store.coverage** (L542): `required_text` (L543); `ValueError` (L545); `isinstance` (L546); `len` (L546); `ValueError` (L547); `timestamp` (L548); `ValueError` (L550); `self.connect` (L551); `db.execute` (L552); `now` (L553); `self.audit` (L554); `self.status` (L555)
- **Store.status** (L557): `self.connect` (L558); `db.execute('SELECT count(*) FROM curated_entries WHERE target=? AND retired_version IS NULL', (r['target'],)).fetchone` (L559); `db.execute` (L559); `db.execute` (L561); `db.execute('SELECT count(*) FROM records WHERE deleted=0').fetchone` (L563); `db.execute` (L563); `db.execute("SELECT count(*) FROM claims WHERE status='active'").fetchone` (L564); `db.execute` (L564); `db.execute('SELECT count(*) FROM entities').fetchone` (L565); `db.execute` (L565); `dict` (L566); `db.execute` (L566); `db.execute('SELECT coalesce(max(id),0) FROM audit').fetchone` (L568); `db.execute` (L568)
- **Store.generation** (L570): `self.connect` (L571); `db.execute('SELECT coalesce(max(id),0) FROM audit').fetchone` (L572); `db.execute` (L572)

## personal_memory/tools.py

(Module entry point.)

- **parser_safe** (L20): `isinstance` (L21); `node.items` (L23); `isinstance` (L27); `parser_safe` (L29); `isinstance` (L31); `parser_safe` (L32)
- **field** (L36): No direct calls.
- **schema** (L40): `list` (L43)

Module/class/lambda calls: `<module> → schema` (L47); `<module> → field` (L48); `<module> → field` (L48); `<module> → field` (L49); `<module> → field` (L49); `<module> → field` (L49); `<module> → field` (L49); `<module> → field` (L50); `<module> → field` (L50); `<module> → field` (L51); `<module> → field` (L51); `<module> → field` (L51); `<module> → schema` (L52); `<module> → field` (L52); `<module> → schema` (L53); `<module> → field` (L53); `<module> → schema` (L54); `<module> → field` (L55); `<module> → field` (L55); `<module> → field` (L55); `<module> → field` (L56); `<module> → field` (L56); `<module> → field` (L56); `<module> → schema` (L57); `<module> → field` (L58); `<module> → field` (L58); `<module> → field` (L58); `<module> → field` (L58); `<module> → field` (L59); `<module> → field` (L60); `<module> → field` (L60); `<module> → field` (L61); `<module> → field` (L61); `<module> → field` (L61); `<module> → parser_safe` (L63); `<module> → RECORD_SCHEMA.items` (L63); `<module> → schema` (L64); `<module> → field` (L65); `<module> → field` (L65); `<module> → schema` (L66); `<module> → schema` (L67); `<module> → field` (L68); `<module> → field` (L68); `<module> → field` (L68); `<module> → field` (L68); `<module> → field` (L68); `<module> → field` (L68); `<module> → schema` (L69); `<module> → field` (L69); `<module> → schema` (L70); `<module> → field` (L71); `<module> → field` (L71); `<module> → field` (L71); `<module> → field` (L71); `<module> → field` (L71); `<module> → field` (L71); `<module> → schema` (L72); `<module> → field` (L72); `<module> → schema` (L76); `<module> → field` (L77); `<module> → field` (L77); `<module> → field` (L77); `<module> → field` (L77); `<module> → field` (L78); `<module> → field` (L78); `<module> → field` (L79); `<module> → field` (L79); `<module> → field` (L79); `<module> → field` (L79); `<module> → field` (L79); `<module> → schema` (L80); `<module> → field` (L81); `<module> → field` (L81); `<module> → field` (L81); `<module> → field` (L81); `<module> → field` (L81); `<module> → field` (L82); `<module> → field` (L82); `<module> → field` (L82); `<module> → field` (L82); `<module> → schema` (L83); `<module> → field` (L84); `<module> → field` (L84); `<module> → field` (L84); `<module> → field` (L84); `<module> → field` (L85); `<module> → field` (L85); `<module> → field` (L85); `<module> → field` (L85); `<module> → field` (L85); `<module> → field` (L85); `<module> → field` (L86); `<module> → field` (L86); `<module> → field` (L86); `<module> → field` (L86); `<module> → schema` (L88); `<module> → field` (L89); `<module> → field` (L89); `<module> → field` (L90); `<module> → field` (L90); `<module> → schema` (L94); `<module> → field` (L95); `<module> → field` (L95); `<module> → schema` (L96); `<module> → field` (L97); `<module> → field` (L97); `<module> → schema` (L100); `<module> → field` (L100); `<module> → field` (L100); `<module> → ROUTES.update` (L109); `<module> → SCHEMAS.append` (L129); `<module> → schema` (L129); `<module> → field` (L130); `<module> → field` (L131); `<module> → field` (L132); `<module> → field` (L132); `<module> → field` (L132); `<module> → field` (L132); `<module> → field` (L133); `<module> → field` (L134); `<module> → field` (L134); `<module> → field` (L135); `<module> → field` (L135)

## personal_memory/trace.py

Uniform, low-overhead call tracing for the personal-memory stack.

- **new_trace** (L33): `secrets.token_hex` (L35)
- **get_trace** (L38): `_trace_var.get` (L39)
- **sanitize_trace** (L42): `isinstance` (L44); `isinstance` (L46); `raw.decode` (L48); `''.join` (L51); `ch.isalnum` (L51)
- **bind_trace** (L55): `_trace_var.set` (L56); `_trace_var.reset` (L60)
- **TraceFilter.filter** (L66): `_trace_var.get` (L67)
- **configure_logging** (L71): `logging.getLogger` (L78); `getattr` (L79); `logger.setLevel` (L81); `_resolve_level` (L81); `logging.StreamHandler` (L83); `handler.addFilter` (L84); `TraceFilter` (L84); `handler.setFormatter` (L85); `logging.Formatter` (L85); `logger.addHandler` (L87); `logger.setLevel` (L88); `_resolve_level` (L88)
- **_resolve_level** (L94): `isinstance` (L95); `(level or os.environ.get('PERSONAL_MEMORY_LOG_LEVEL') or 'INFO').upper` (L97); `os.environ.get` (L97); `getattr` (L98); `isinstance` (L99)
- **_fmt** (L102): `' '.join` (L103); `fields.items` (L103)
- **log_call** (L106): `LOG.isEnabledFor` (L108); `round` (L110); `time.monotonic` (L110); `int` (L110); `base.update` (L111); `LOG.log` (L112); `_fmt` (L112)
- **debug_enabled** (L115): `LOG.isEnabledFor` (L116)
- **traced** (L119): No direct calls.
- **traced.decorator** (L122): No direct calls.
- **traced.decorator.wrapper** (L124): `_trace_var.get` (L125); `fn` (L126); `bind_trace` (L127); `new_trace` (L127); `time.monotonic` (L128); `fn` (L130); `log_call` (L132); `type` (L132); `LOG.isEnabledFor` (L134); `detail` (L134); `log_call` (L135); `wraps` (L123)

Module/class/lambda calls: `<module> → ContextVar` (L29); `<module> → logging.getLogger` (L30)

## personal_memory/workflows.py

Leased durable jobs, immutable suites and bounded operator-installed adapters.

- **run_adapter** (L27): `isinstance` (L28); `ValueError` (L28); `os.environ.items` (L31); `str` (L32); `Path(__file__).resolve` (L32); `Path` (L32); `document` (L33); `tempfile.TemporaryDirectory` (L34); `tempfile.TemporaryFile` (L36); `tempfile.TemporaryFile` (L36); `subprocess.Popen` (L37); `process.communicate` (L40); `payload.encode` (L40); `subprocess.CalledProcessError` (L41); `process.poll` (L43); `os.killpg` (L46); `process.kill` (L47); `process.wait` (L48); `output.seek` (L50); `output.read` (L50); `len` (L51); `ValueError` (L51); `strict_json` (L53); `document` (L54)
- **validate_intelligence_config** (L58): `isinstance` (L61); `set` (L61); `ValueError` (L61); `type` (L62); `config.get` (L62); `ValueError` (L62); `isinstance` (L63); `config.get` (L63); `ValueError` (L63); `config.get('learning_policies', {}).items` (L64); `config.get` (L64); `required_text` (L65); `isinstance` (L66); `set` (L66); `type` (L66); `ValueError` (L66); `required_text` (L67); `isinstance` (L69); `config.get` (L69); `ValueError` (L69); `config.get(name, {}).items` (L70); `config.get` (L70); `required_text` (L71); `isinstance` (L72); `set` (L72); `adapter.get` (L72); `ValueError` (L72); `config.get` (L73); `isinstance` (L75); `set` (L75); `adapter.get` (L75); `ValueError` (L75); `config.get` (L76); `type` (L77); `ValueError` (L77); `config.get` (L78); `adapters.items` (L79); `isinstance` (L80); `set` (L80); `ValueError` (L80); `required_text` (L81); `value.get` (L81)
- **Workflows.__init__** (L86): `Learning` (L87); `validate_intelligence_config` (L87); `threading.Event` (L89); `self.config.get` (L90); `self.config.get` (L91)
- **Workflows.suite** (L93): `required_text` (L94); `isinstance` (L95); `len` (L95); `ValueError` (L95); `set` (L96); `isinstance` (L98); `set` (L98); `ValueError` (L98); `required_text` (L99); `ValueError` (L100); `seen.add` (L101); `ValueError` (L102); `document` (L103); `self.store.connect` (L104); `db.execute` (L105); `self.learning._put` (L106); `strings` (L106); `digest` (L107); `len` (L107)
- **Workflows.enqueue** (L109): `ValueError` (L110); `ValueError` (L111); `self.store.connect` (L113); `db.execute` (L114); `self.learning._get` (L116); `db.execute('SELECT length(text) FROM records WHERE id=? AND deleted=0', (rid,)).fetchone` (L120); `db.execute` (L120); `ValueError` (L121); `segments.append` (L122); `isinstance` (L123); `len` (L123); `ValueError` (L123); `isinstance` (L126); `set` (L126); `ValueError` (L126); `builtins.type` (L127); `builtins.type` (L127); `ValueError` (L127); `db.execute('SELECT text FROM records WHERE id=? AND deleted=0', (segment['record_id'],)).fetchone` (L128); `db.execute` (L128); `len` (L129); `ValueError` (L129); `len` (L130); `text[0][segment['start']:segment['end']].encode` (L130); `ValueError` (L131); `ValueError` (L132); `self.learning._get` (L134); `ValueError` (L135); `ValueError` (L136); `self.learning._get` (L137); `digest` (L139); `self.learning._put` (L140); `db.execute` (L141); `db.execute('SELECT state FROM workflow_jobs WHERE id=?', (obj['id'],)).fetchone` (L142); `db.execute` (L142)
- **Workflows.procedure** (L144): `self.config.get` (L145); `ValueError` (L146); `isinstance` (L147); `ValueError` (L147); `self.store.connect` (L148); `db.execute` (L149); `self.learning._get` (L149); `ValueError` (L150); `digest` (L151); `self.learning._put` (L152)
- **Workflows.execute** (L154): `self.store.connect` (L155); `db.execute` (L156); `self.learning._get` (L156); `self.learning._put` (L158); `db.execute` (L159); `db.execute('SELECT state FROM workflow_jobs WHERE id=?', (job['id'],)).fetchone` (L160); `db.execute` (L160)
- **Workflows._procedure** (L162): `self.store.connect` (L163); `self.learning._get` (L164); `self.learning._get` (L165); `ValueError` (L166); `self.config.get('capabilities', {}).get` (L167); `self.config.get` (L167); `digest` (L168); `ValueError` (L168); `run_adapter` (L169); `adapter.get` (L169); `document` (L170); `document` (L170); `digest` (L170)
- **Workflows.get** (L172): `self.store.connect` (L173); `self.learning._get` (L174); `db.execute('SELECT id,type,state,attempts,result_id,error FROM workflow_jobs WHERE id=?', (job_id,)).fetchone` (L175); `db.execute` (L175); `dict` (L176); `self.learning._get` (L178)
- **Workflows.retry** (L181): `self.store.connect` (L182); `db.execute` (L183); `self.learning._get` (L183); `db.execute` (L184); `ValueError` (L185); `self.store.audit` (L186)
- **Workflows.tick** (L189): `self.stop.is_set` (L190); `self.store.connect` (L191); `db.execute` (L192); `time.time` (L192); `db.execute` (L193); `db.execute("SELECT j.* FROM workflow_jobs j JOIN learning_objects o ON o.id=j.id WHERE o.state='recorded'\n                AND ((j.state='pending' AND j.retry_at<=?) OR (j.state='running' AND j.lease_until<?)) ORDER BY o.created_at,j.id LIMIT 1", (clock, clock)).fetchone` (L194); `db.execute` (L194); `uuid.uuid4` (L197); `db.execute` (L200); `self.learning._get` (L201); `self._procedure` (L203); `digest` (L206); `ValueError` (L206); `self._consolidate` (L207); `self._evaluate` (L208); `self.store.connect` (L209); `db.execute` (L210); `self.learning._get` (L210); `db.execute('SELECT lease FROM workflow_jobs WHERE id=?', (job['id'],)).fetchone` (L211); `db.execute` (L211); `ValueError` (L212); `self.learning._put` (L213); `digest` (L213); `result.get` (L213); `db.execute` (L215); `db.execute` (L216); `self._auto_promote` (L218); `self.store.connect` (L220); `self.store.audit` (L220); `self.store.connect` (L222); `db.execute` (L223); `db.execute` (L224); `time.time` (L226); `min` (L226); `type` (L226)
- **Workflows._consolidate** (L229): `self.store.connect` (L230); `self.learning._get` (L231); `db.execute('SELECT id,text,fingerprint FROM records WHERE id=? AND deleted=0', (rid,)).fetchone` (L235); `db.execute` (L235); `ValueError` (L236); `evidence.append` (L237); `entities.extend` (L240); `dict` (L240); `db.execute` (L240); `list` (L241); `{e['id']: e for e in entities}.values` (L241); `run_adapter` (L242); `adapter.get` (L242); `isinstance` (L243); `set` (L243); `set` (L243); `ValueError` (L243); `required_text` (L244); `supplied.setdefault(segment['record_id'], []).append` (L246); `supplied.setdefault` (L246); `validate_supplied_quotes` (L255); `self.store.connect` (L257); `Intelligence` (L258); `set` (L259); `intelligence._quote` (L260); `set` (L261); `ValueError` (L261); `result.get` (L262); `isinstance` (L263); `len` (L263); `ValueError` (L263); `isinstance` (L268); `set` (L268); `ValueError` (L268); `validate_supplied_quotes` (L269); `intelligence._entity` (L270); `required_text` (L270); `scalar` (L270); `set` (L271); `intelligence._quote` (L271); `ValueError` (L271); `accepted.append` (L274); `len` (L275); `len` (L275); `digest` (L275)
- **Workflows._consolidate.validate_supplied_quotes** (L247): `isinstance` (L248); `ValueError` (L248); `isinstance` (L250); `set` (L250); `isinstance` (L251); `isinstance` (L252); `any` (L253); `supplied.get` (L253); `ValueError` (L254)
- **Workflows.accept_consolidation** (L277): `self.store.connect` (L281); `db.execute` (L282); `self.learning._get` (L282); `ValueError` (L283); `sorted` (L286); `result['payload'].get` (L286); `isinstance` (L286); `lifecycle.require_live_evidence` (L287); `enumerate` (L289); `result['payload'].get` (L289); `self.learning._put` (L291); `str` (L291); `created.append` (L292); `db.execute` (L293); `db.execute` (L294); `db.execute` (L295); `db.execute` (L296); `self.store.audit` (L297)
- **Workflows._evaluate** (L300): `self.store.connect` (L301); `self.learning._get` (L302); `ValueError` (L303); `self.learning._get` (L304); `db.execute("SELECT id,payload FROM learning_objects WHERE kind='candidate' AND state='active' AND json_extract(payload,'$.family')=?", (candidate['payload']['family'],)).fetchone` (L305); `db.execute` (L305); `json.loads` (L306); `self.stop.is_set` (L310); `RuntimeError` (L310); `self.stop.is_set` (L313); `RuntimeError` (L313); `self.store.connect` (L314); `db.execute` (L315); `self.learning._get` (L315); `db.execute` (L316); `time.time` (L316); `ValueError` (L317); `outputs.append` (L318); `run_adapter` (L318); `adapter.get` (L318); `cases.append` (L319); `document` (L319); `document` (L319); `document` (L319); `document` (L319); `digest` (L320); `digest` (L321); `digest` (L321); `all` (L322)
- **Workflows.scan** (L324): `self.config.get` (L332); `self.store.connect` (L335); `db.execute("SELECT watermark FROM workflow_cursor WHERE name='ingest'").fetchone` (L336); `db.execute` (L336); `db.execute("SELECT a.id,a.object_id FROM audit a JOIN records r ON r.id=a.object_id WHERE a.action='ingest' AND a.id>? AND r.deleted=0\n                AND NOT EXISTS(SELECT 1 FROM record_visibility v WHERE v.record_id=r.id AND v.hidden=1) ORDER BY a.id LIMIT 10", (watermark,)).fetchall` (L337); `db.execute` (L337); `self.stop.is_set` (L340); `self.store.connect` (L342); `lifecycle.live_and_visible` (L344); `ValueError` (L344); `Intelligence(self.store).snapshot` (L345); `Intelligence` (L345); `self.store.connect` (L346); `db.execute('SELECT length(text) FROM records WHERE id=?', (row['object_id'],)).fetchone` (L346); `db.execute` (L346); `range` (L347); `self.stop.is_set` (L348); `self.enqueue` (L349); `str` (L349); `min` (L350); `self._retired_in_flight` (L353); `self._advance_cursor` (L354); `len` (L355)
- **Workflows._retired_in_flight** (L357): `self.store.connect` (L359); `lifecycle.live_and_visible` (L360); `db.execute("SELECT 1 FROM learning_objects WHERE kind='snapshot' AND logical_key=? AND state IN ('invalidated','retracted')", ('auto/' + record_id,)).fetchone` (L363); `db.execute` (L363)
- **Workflows._advance_cursor** (L366): `self.store.connect` (L367); `db.execute` (L368)
- **Workflows.autolearn** (L370): `self.config.get` (L371); `policies.items` (L372); `digest` (L373); `self.store.connect` (L374); `db.execute("SELECT c.id FROM learning_objects c WHERE c.kind='candidate' AND c.state='candidate'\n                  AND json_extract(c.payload,'$.scope')=? AND NOT EXISTS(\n                    SELECT 1 FROM learning_objects j WHERE j.kind='job' AND j.logical_key=?||c.id) ORDER BY c.id LIMIT 10", (scope, key_prefix)).fetchall` (L375); `db.execute` (L375); `self.enqueue` (L379)
- **Workflows._auto_promote** (L381): `result.get` (L383); `result.get` (L383); `self.store.connect` (L384); `self.learning._get` (L385); `self.config.get('learning_policies', {}).get` (L386); `self.config.get` (L386); `policy.get` (L387); `policy.get` (L387); `self.learning.promote` (L388); `digest` (L388)
- **Workflows.reconcile_promotions** (L390): `self.config.get('learning_policies', {}).items` (L391); `self.config.get` (L391); `policy.get` (L392); `self.store.connect` (L393); `db.execute("SELECT e.id FROM learning_objects e JOIN learning_objects c ON c.id=json_extract(e.payload,'$.candidate_id')\n                 WHERE e.kind='evaluation' AND e.state='recorded' AND c.state='candidate' AND json_extract(c.payload,'$.scope')=?\n                 AND json_extract(e.payload,'$.suite_id')=? AND NOT EXISTS(SELECT 1 FROM audit a WHERE a.object_id=e.id AND a.action='auto_promotion_blocked') LIMIT 10", (scope, policy['suite_id'])).fetchall` (L394); `db.execute` (L394); `self.learning._get` (L397); `self._auto_promote` (L399); `self.store.connect` (L401); `self.store.audit` (L401)
- **Workflows.deliver_event** (L403): `self.config.get` (L404); `self.stop.is_set` (L405); `Intelligence` (L408); `intelligence.claim_event` (L409); `self.store.connect` (L411); `self.learning._get` (L411); `run_adapter` (L412); `handler.get` (L412); `min` (L412); `ValueError` (L413); `intelligence.ack_event` (L414)
- **Workflows.start** (L417): `threading.Thread` (L429); `self.thread.start` (L429)
- **Workflows.start.loop** (L419): `self.stop.is_set` (L420); `self.scan` (L423); `self.autolearn` (L423); `self.reconcile_promotions` (L423); `type` (L424); `self.tick` (L425); `self.deliver_event` (L425); `type` (L426); `self.stop.wait` (L428)
- **Workflows.close** (L431): `self.stop.set` (L432); `self.thread.join` (L433)
- **Workflows.status** (L435): `self.store.connect` (L436); `sorted` (L437); `bool` (L437); `self.config.get` (L437); `dict` (L438); `db.execute` (L438); `bool` (L439); `self.thread.is_alive` (L439)
