# Direct production dependencies exercised by this release; not a complete lockfile.
uvicorn==0.52.4
cryptography==50.0.1
jsonschema==4.26.0
fastembed==0.8.0
PyYAML==6.0.3
hindsight-api-slim==0.9.2
hindsight-embed==0.9.2
hnswlib==0.8.0
# Attachment text extraction (S1): PDF text-layer/render + image encode.
pypdfium2==5.13.0
pillow==12.3.0
# Embedding/ML stack exercised on the GPU (local-ml) path. torch's CUDA wheel carries a
# host-specific local label (cu128 on Blackwell, cu130 on Ampere), so only the public version
# is recorded here; doctor's embedding_stack check guards against a major-version jump that
# could silently change embeddings (pooling or a renamed sentence-transformers API).
sentence-transformers==6.0.1
numpy==2.4.6
onnxruntime==1.30.0
torch==2.14.0
